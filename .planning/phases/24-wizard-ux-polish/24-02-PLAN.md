---
phase: 24-wizard-ux-polish
plan: 02
type: execute
wave: 2
depends_on: [24-01]
files_modified:
  - .planning/phases/24-wizard-ux-polish/24-UAT.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [UX-02, UX-03]
tags: [uat, wizard, ux, verification]

must_haves:
  truths:
    - "User running the wizard in fresh-install mode on a vault with 2+ projects sees a single bulk prompt 'Install loop.md for all N new projects?' instead of N per-project y/N confirms"
    - "User running the wizard in fresh-install mode on a vault with 2+ projects sees a single bulk prompt 'Configure git conventions for all N projects?' instead of N per-project confirms"
    - "User running the wizard in re-install mode (already-installed loop.md) sees a bulk 'Overwrite loop.md for all N existing projects?' prompt"
    - "After UAT passes, UX-02 and UX-03 checkboxes in .planning/REQUIREMENTS.md are marked [x] with a note linking to the UAT result"
    - "UAT document records at least 4 explicit test cases with expected behavior and result (pass/fail)"
  artifacts:
    - path: ".planning/phases/24-wizard-ux-polish/24-UAT.md"
      provides: "Full UAT record for UX-02 and UX-03 with test cases and results"
      contains: "status:"
      min_lines: 40
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated with UX-02 and UX-03 marked complete"
      contains: "- [x] **UX-02**"
  key_links:
    - from: "24-UAT.md"
      to: "lib/install/components.mjs installLoopMd"
      via: "test case verifies bulk prompt appears"
      pattern: "Install loop.md for all"
    - from: "24-UAT.md"
      to: "lib/install/git-conventions.mjs installGitConventions"
      via: "test case verifies bulk prompt appears"
      pattern: "Configure git conventions for all"
---

<objective>
Verify UX-02 (loop.md bulk prompt) and UX-03 (git-conventions bulk prompt) work end-to-end. No code changes — the bulk prompts were already implemented by Phase 23 DX-13 (see lib/install/components.mjs:120-135 and lib/install/git-conventions.mjs:22-42). Phase 24 only needs to confirm the flows work on a real wizard run and update REQUIREMENTS.md.

This plan also runs a full wizard UAT covering both fresh-install and re-install paths to validate Plan 01 changes (UX-01, UX-04, UX-05, UX-06, UX-07) end-to-end.

Purpose: Close the UX-02/UX-03 requirements without redundant implementation work, and catch any regression from Plan 01 before marking the phase complete.
Output: 24-UAT.md with test results, REQUIREMENTS.md with UX-02/03 (and UX-01/04/05/06/07 if Plan 01 UAT passes) marked done.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/24-wizard-ux-polish/24-CONTEXT.md
@.planning/phases/24-wizard-ux-polish/24-01-PLAN.md
@lib/install/components.mjs
@lib/install/git-conventions.mjs
@.planning/phases/19-project-level-hooks-wizard-bug-fixes/19-UAT.md

<interfaces>
UAT document structure to model after 19-UAT.md. Required frontmatter fields (YAML):
- status (testing or complete)
- phase (24-wizard-ux-polish)
- source (list referencing 24-01-SUMMARY.md)
- started (ISO date)
- updated (ISO date)

Required body sections:
- "## Current Test" — pointer to test currently in progress (number, name, expected, awaiting)
- "## Tests" — one "### N. Test Name" subsection per test, each with "expected:" and "result:" fields
- "## Summary" — counts: total, passed, issues, pending, skipped
- "## Gaps" — list of failures to feed into a follow-up phase

Test result values: pass / fail / pending. Initial state: all pending.

Reference implementation: `.planning/phases/19-project-level-hooks-wizard-bug-fixes/19-UAT.md` lines 1-47.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold 24-UAT.md with test cases for UX-01 through UX-07</name>
  <files>.planning/phases/24-wizard-ux-polish/24-UAT.md</files>
  <read_first>
    - .planning/phases/19-project-level-hooks-wizard-bug-fixes/19-UAT.md (template shape)
    - .planning/REQUIREMENTS.md section UX-01 through UX-07 (requirement specs)
    - .planning/phases/24-wizard-ux-polish/24-CONTEXT.md (decisions)
    - lib/install/components.mjs lines 95-225 (installLoopMd bulk prompt — verify the expected literal)
    - lib/install/git-conventions.mjs lines 22-42 (installGitConventions bulk prompt — verify the expected literal)
  </read_first>
  <action>
    Create `.planning/phases/24-wizard-ux-polish/24-UAT.md` with the structure described below. Use 19-UAT.md as the shape reference — same YAML frontmatter keys, same "## Tests" / "## Summary" / "## Gaps" sections.

    Frontmatter keys to set:
    - status: testing
    - phase: 24-wizard-ux-polish
    - source: [24-01-SUMMARY.md]
    - started: <TODAY — use the date from CLAUDE.md #currentDate or `date -u +%Y-%m-%dT%H:%M:%SZ`>
    - updated: <TODAY same>

    Body:
    - "## Current Test" block pointing at test 1 with awaiting: user response
    - "## Tests" block with 9 subsections — one per test below
    - "## Summary" with total: 9, passed: 0, issues: 0, pending: 9, skipped: 0
    - "## Gaps" with placeholder "[none yet]"

    Nine tests to include (use exact titles and expected text):

    Test 1 — UX-02 — loop.md bulk prompt on fresh install
    expected: На wizard run с >=2 projects и GSD selected — шаг loop.md показывает один bulk select prompt "Install loop.md for all N new projects?" вместо per-project y/N.

    Test 2 — UX-03 — git-conventions bulk prompt on fresh install
    expected: На wizard run с >=2 projects — шаг git conventions показывает один bulk select prompt "Configure git conventions for all N projects?" (Yes all / Choose per project / Skip all) вместо per-project y/N.

    Test 3 — UX-01 — Git sync detection with existing remote
    expected: Wizard с уже настроенным origin в vault показывает "✓ Git sync: configured (origin → {url})" и select с Skip / Reconfigure / Remove. Не предлагает "Set up vault git sync?" confirm.

    Test 4 — UX-04 — Git sync select when no remote
    expected: Wizard без remote в vault показывает select prompt "Set up vault git sync? (backup + team sharing)" с вариантами "Yes, set up now" / "Skip". Не использует type: confirm.

    Test 5 — UX-05 — Step counter accuracy
    expected: Во всех путях wizard (fresh install, re-install, с/без GSD, с/без loop.md, skip hooks) строка "Step N of M" никогда не показывает N больше M. Все шаги, которые реально выполняются, нумеруются последовательно 1..M без дыр.

    Test 6 — UX-06 — Project count single source
    expected: Detect banner ("✔ Vault: ~/vault (N projects)") и vault step ("Vault setup — already at ~/vault (N projects)") показывают одинаковое число N. Оба читают из installState.projects.length.

    Test 7 — UX-07 — No type confirm in wizard scope
    expected: grep -rn для type confirm по bin/install.mjs и lib/install/ возвращает ноль совпадений. Все prompts в wizard — type select.

    Test 8 — Full wizard end-to-end (fresh install)
    expected: Wizard запускается с нуля на чистой системе (или удалённым vault), проходит все шаги без ошибок, создаёт vault + projects + hooks + CLAUDE.md. Summary в конце показывает все installed компоненты.

    Test 9 — Full wizard end-to-end (re-install)
    expected: Wizard запускается повторно на уже установленной системе, корректно определяет installState, pre-selects компоненты, offers skip/reconfigure на каждом шаге где применимо. Не дублирует конфигурацию.

    Derivation notes for executor: the expected literal strings for UX-02 and UX-03 tests MUST match the actual prompt messages in lib/install/components.mjs and lib/install/git-conventions.mjs. Verify by reading those files and copying the exact message template (e.g., "Install loop.md for all ${newProjects.length} new projects?").
  </action>
  <verify>
    <automated>test -f .planning/phases/24-wizard-ux-polish/24-UAT.md &amp;&amp; test $(grep -c "^### " .planning/phases/24-wizard-ux-polish/24-UAT.md) -eq 9</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/24-wizard-ux-polish/24-UAT.md` exists
    - `grep -c "^### " 24-UAT.md` returns 9 (9 test cases)
    - `grep -c "result: pending" 24-UAT.md` returns 9 initially
    - Frontmatter contains `status: testing` and `phase: 24-wizard-ux-polish`
    - All 7 UX requirements (UX-01 through UX-07) have at least one dedicated test case
  </acceptance_criteria>
  <done>24-UAT.md scaffolded with 9 pending tests covering all Phase 24 requirements plus 2 full-flow tests.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human UAT run — fresh install + re-install</name>
  <files>.planning/phases/24-wizard-ux-polish/24-UAT.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/phases/24-wizard-ux-polish/24-UAT.md (just scaffolded)
    - .planning/phases/24-wizard-ux-polish/24-01-SUMMARY.md (once Plan 01 completes — describes what was changed)
  </read_first>
  <action>
    Human tester runs the wizard twice (fresh install + re-install) and records evidence for each of the 9 tests in 24-UAT.md. Instructions below.
  </action>
  <what-built>
    Plan 01 delivered: git sync detection block (UX-01/04), dynamic step counter (UX-05), unified project count (UX-06), confirm→select sweep (UX-07). Plan 02 Task 1 scaffolded 24-UAT.md with 9 test cases.

    Phase 23 delivered (verified here only): loop.md bulk prompt (UX-02), git-conventions bulk prompt (UX-03).
  </what-built>
  <how-to-verify>
    ## Preparation

    1. Open terminal in `/Users/eugenenakoneschniy/Projects/claude-dev-stack`
    2. Checkout phase branch: `git status` — confirm you are on `gsd/phase-24-...`
    3. Ensure local node install is available: `node --version` (expect 18 or higher)

    ## Test A — Fresh install path (UX-01 Branch B, UX-04, UX-05, UX-06, UX-07 — tests 4, 5, 7)

    Setup:
    ```bash
    # Optional: back up real vault before testing
    mv ~/vault ~/vault.bak  # or temporarily rename
    # Remove any existing hooks to trigger fresh path
    cp ~/.claude/settings.json ~/.claude/settings.json.bak 2>/dev/null || true
    ```

    Run:
    ```bash
    node bin/install.mjs
    ```

    Verify during run (take notes, do NOT skip any):
    - [ ] Ready prompt is a SELECT with "Yes, start installation" / "Cancel" (NOT y/N) — UX-07 evidence
    - [ ] Step counter reads "Step 1 of M" at first step; M stays constant throughout run — UX-05 evidence (record M value here: ____)
    - [ ] Git sync step offers "Set up vault git sync? (backup + team sharing)" as a SELECT with "Yes, set up now" / "Skip" options (NOT y/N) — UX-04 evidence
    - [ ] Every prompt that was previously y/N is now a select — no `(y/N)` text anywhere — UX-07 evidence
    - [ ] Final step shows "Step M of M" (N == M at last step) — UX-05 evidence

    Record in 24-UAT.md: mark tests 4, 5, 7 as `pass` if all checkboxes pass; else describe issue under the test's `result:` field.

    ## Test B — Re-install path (UX-01 Branch A, UX-02, UX-03, UX-06)

    Restore vault if you backed it up:
    ```bash
    mv ~/vault.bak ~/vault  # restore
    # Ensure vault has git remote configured (required for UX-01 Branch A test)
    git -C ~/vault remote -v  # verify origin is set; if not: git -C ~/vault remote add origin <some-url>
    ```

    Run wizard again:
    ```bash
    node bin/install.mjs
    ```

    Verify during run:
    - [ ] Detect banner shows "Vault: ~/vault (N projects)" — record N: ____ — UX-06 evidence
    - [ ] Vault step message shows the SAME N count — UX-06 evidence
    - [ ] When wizard reaches git sync step, it shows `✓ Git sync: configured (origin → <your-url>)` and a select with Skip / Reconfigure / Remove — UX-01 evidence
    - [ ] Pick "Skip" — wizard prints "Vault git sync: keeping existing remote" and proceeds
    - [ ] If you have 2+ projects AND GSD installed AND loop.md not yet installed: step shows "Install loop.md for all N new projects?" ONE bulk select prompt (NOT N separate confirms) — UX-02 evidence
    - [ ] Git conventions step shows "Configure git conventions for all N projects?" with "Yes, all N projects" / "Choose per project" / "Skip all" — UX-03 evidence

    Record in 24-UAT.md: mark tests 1, 2, 3, 6 as `pass` if evidence matches; else describe issue.

    ## Test C — Full-flow integration tests 8 and 9

    Test 8 (fresh end-to-end): Did Test A complete all the way to `printSummary` output without errors? If yes → mark test 8 as `pass`.
    Test 9 (re-install end-to-end): Did Test B complete to `printSummary` without errors, with installed components correctly pre-detected? If yes → mark test 9 as `pass`.

    ## Recording results

    For each of the 9 tests in 24-UAT.md:
    - Change `result: pending` to `result: pass` or `result: fail`
    - For failures, add a sub-line `issue: <description>` below the result

    Update the Summary block:
    ```
    total: 9
    passed: <count>
    issues: <count>
    pending: 0
    skipped: 0
    ```

    Update frontmatter `status:` to `complete` if all 9 pass, or keep `testing` if any fail (those become gaps for a follow-up phase).

    Update frontmatter `updated:` to today's date.

    ## Mark REQUIREMENTS.md complete

    For each passed UX-XX test, edit `.planning/REQUIREMENTS.md`:
    - Change `- [ ] **UX-XX**:` to `- [x] **UX-XX**:` and append `(verified: 24-UAT.md test N, <date>)`
    - Mapping: UX-01 → test 3, UX-02 → test 1, UX-03 → test 2, UX-04 → test 4, UX-05 → test 5, UX-06 → test 6, UX-07 → test 7

    Also update the Traceability table at the bottom of REQUIREMENTS.md: change `pending` → `complete` for each passed UX-XX row, and fill the `Plan` column with `24-01` (code) or `24-02` (verification-only). UX-02 and UX-03 are Plan 24-02 (verification-only). UX-01, UX-04, UX-05, UX-06, UX-07 are Plan 24-01 (code).

    ## Cleanup

    - Restore ~/.claude/settings.json.bak if you backed it up
    - Confirm git status shows only 24-UAT.md and REQUIREMENTS.md modifications in .planning/

  </how-to-verify>
  <verify>
    <automated>test $(grep -c "result: pass\|result: fail" .planning/phases/24-wizard-ux-polish/24-UAT.md) -eq 9</automated>
  </verify>
  <acceptance_criteria>
    - All 9 tests in 24-UAT.md have `result: pass` or `result: fail` (no remaining `pending`)
    - 24-UAT.md Summary block totals match (passed + issues + skipped == 9)
    - .planning/REQUIREMENTS.md has `- [x]` for each UX-XX whose corresponding test passed
    - .planning/REQUIREMENTS.md Traceability table rows for each passed UX-XX have status `complete` and Plan `24-01` or `24-02` as appropriate
  </acceptance_criteria>
  <done>UAT complete, all 9 tests resolved, REQUIREMENTS.md reflects the verified state.</done>
  <resume-signal>Type "approved" if all 9 tests pass and REQUIREMENTS.md is updated. Type "gaps: <description>" if any test failed — the orchestrator will plan a follow-up closure phase.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UAT harness → user's actual vault + home dir | UAT instructions operate on real user state (~/vault, ~/.claude/settings.json) |
| tester (user) → REQUIREMENTS.md | Tester manually edits tracked file to mark requirements complete |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-06 | Information Disclosure | UAT instructions ask tester to rename ~/vault | accept | Operation is reversible (`mv ~/vault ~/vault.bak` + `mv` back). Instructions include cleanup step. Tester is the owner of the vault. |
| T-24-07 | Tampering | REQUIREMENTS.md manual edits | mitigate | Instructions spell out exact line numbers and text pattern. Git tracks changes — any accidental edit is reviewable via `git diff`. |
| T-24-08 | Repudiation | Tests marked pass without actual verification | mitigate | Each test has explicit checkbox items the tester must verify. Result entries link to specific evidence (e.g., "UX-04 evidence"). Orchestrator checker validates on post-merge review. |
</threat_model>

<verification>
- `test -f .planning/phases/24-wizard-ux-polish/24-UAT.md` → exists
- `grep -c "result: pass" .planning/phases/24-wizard-ux-polish/24-UAT.md` → equals total passed count
- `grep "^- \[x\] \*\*UX-02\*\*" .planning/REQUIREMENTS.md` → 1 match (if UX-02 passed)
- `grep "^- \[x\] \*\*UX-03\*\*" .planning/REQUIREMENTS.md` → 1 match (if UX-03 passed)
- Traceability table in REQUIREMENTS.md has `complete` for each verified UX-XX row
</verification>

<success_criteria>
- UAT document created with 9 test cases
- Human tester completed both Test A (fresh) and Test B (re-install) paths
- All 9 tests marked pass (OR failures captured as gaps for follow-up)
- UX-02 and UX-03 marked complete in REQUIREMENTS.md with traceability link to UAT
- If Plan 01 tests passed: UX-01, UX-04, UX-05, UX-06, UX-07 also marked complete
- Phase 24 frontmatter `status:` in 24-UAT.md reflects final state (complete vs gaps)
</success_criteria>

<output>
After completion, create `.planning/phases/24-wizard-ux-polish/24-02-SUMMARY.md` documenting:
- UAT results: passed/failed/gaps
- Which UX-XX requirements are now complete in REQUIREMENTS.md
- Any gaps found (to feed into a follow-up phase via /gsd-plan-phase --gaps if needed)
- Links to 24-UAT.md and the updated REQUIREMENTS.md commit
</output>
