---
phase: 21-continuation-prompt-loop-md
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/loop.md
  - lib/install/components.mjs
  - lib/install/detect.mjs
  - tests/templates.test.mjs
autonomous: true
requirements: [LIMIT-03]

must_haves:
  truths:
    - "templates/loop.md exists in the package root templates/ directory"
    - "lib/install/components.mjs offers loop.md install option when GSD is selected"
    - "loop.md is copied to {projectDir}/.claude/loop.md when user opts in"
    - "detect.mjs detects whether loop.md is already installed per project"
    - "tests/templates.test.mjs verifies templates/loop.md exists and has correct content"
  artifacts:
    - path: "templates/loop.md"
      provides: "GSD-aware maintenance loop template"
      contains: "STATE.md"
    - path: "lib/install/components.mjs"
      provides: "loop.md wizard step"
      contains: "loop.md"
    - path: "lib/install/detect.mjs"
      provides: "loopMdInstalled detection"
      contains: "loopMdInstalled"
  key_links:
    - from: "bin/install.mjs"
      to: "lib/install/components.mjs selectComponents"
      via: "loop.md install call after GSD confirmed"
      pattern: "loop.md"
    - from: "lib/install/components.mjs"
      to: "templates/loop.md"
      via: "cpSync from PKG_ROOT/templates/loop.md"
      pattern: "loop.md"
---

<objective>
Implement loop.md template installation (LIMIT-03). Add templates/loop.md to the package. Extend the install wizard (lib/install/components.mjs) to offer loop.md installation when GSD is selected. Extend detect.mjs to detect whether loop.md is already installed per project.

Output: templates/loop.md, extended lib/install/components.mjs, extended lib/install/detect.mjs, extended/created tests/templates.test.mjs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/21-continuation-prompt-loop-md/21-CONTEXT.md
@.planning/phases/21-continuation-prompt-loop-md/21-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create templates/loop.md GSD maintenance loop template</name>
  <files>templates/loop.md</files>
  <read_first>
    - templates/ (list directory to see existing templates for format reference)
    - .planning/phases/21-continuation-prompt-loop-md/21-CONTEXT.md (decisions D-10, D-12)
    - .planning/REQUIREMENTS.md (LIMIT-03 requirement)
  </read_first>
  <action>
Create templates/loop.md. This template is installed to project .claude/loop.md and provides a GSD-aware maintenance loop for scheduled/recurring Claude Code tasks. It is used by Desktop tasks and Cloud tasks to resume work or run maintenance.

The file must contain:
1. A clear purpose header explaining this is a GSD maintenance loop for scheduled tasks
2. Instructions for Claude to: (a) read .planning/STATE.md, (b) if stopped_at exists run /gsd-resume-work, (c) if no stopped work run maintenance sequence: check PRs, /gsd-next, /gsd-health
3. Clear "Report completion status" instruction at the end

Write the following content:

# GSD Maintenance Loop

This file is executed by scheduled Claude Code tasks (Desktop tasks or Cloud tasks) to continue unfinished work or run maintenance.

## Instructions for Claude

When this loop runs, follow these steps in order:

### Step 1: Check for stopped work

Read `.planning/STATE.md`. Look for `stopped_at` and `resume_file` fields.

If `stopped_at` is present and non-empty:
- Load `resume_file` if specified (provides additional context about what was in progress)
- Run `/gsd-resume-work` to continue from where the session stopped
- Report: "Resumed from: {stopped_at}"

If `stopped_at` is absent or empty (no interrupted work):
- Continue to Step 2 (maintenance tasks)

### Step 2: Maintenance tasks (if no stopped work)

Run in order:

1. **Check open PRs** — Review any open pull requests in the repository:
   - Run: `gh pr list` to see open PRs
   - For each ready PR: check CI status, merge if green and approved, close if stale
   - Report PR status summary

2. **Advance milestone** — Run `/gsd-next` to check if there are pending phases to plan or execute

3. **Health check** — Run `/gsd-health` to verify project health (test baseline, coverage, known issues)

### Step 3: Report

After completing steps above, output a summary:
- Work resumed: yes/no (and what was resumed)
- PRs handled: N merged, N closed, N pending
- Milestone status: next phase or "milestone complete"
- Health: green/warnings/issues

## Notes

- This file is managed by claude-dev-stack. Edit to customize your maintenance loop.
- Scheduled tasks run autonomously — all state must be in git (GSD guarantees this).
- Cloud tasks start from a fresh git clone — no local-only state is available.
- To reschedule: run `claude-dev-stack budget continue` and choose Auto-continue option.
  </action>
  <acceptance_criteria>
    - templates/loop.md exists
    - grep "STATE.md" templates/loop.md returns match
    - grep "gsd-resume-work" templates/loop.md returns match
    - grep "gsd-next" templates/loop.md returns match
    - grep "gsd-health" templates/loop.md returns match
    - grep "stopped_at" templates/loop.md returns match
    - wc -l templates/loop.md shows file has content (>10 lines)
  </acceptance_criteria>
  <verify>
    <automated>test -f templates/loop.md && grep -q "STATE.md" templates/loop.md && grep -q "gsd-resume-work" templates/loop.md && grep -q "gsd-next" templates/loop.md && echo "loop.md OK"</automated>
  </verify>
  <done>templates/loop.md exists with correct GSD maintenance loop content</done>
</task>

<task type="auto">
  <name>Task 2: Extend lib/install/detect.mjs to detect loop.md installation</name>
  <files>lib/install/detect.mjs</files>
  <read_first>
    - lib/install/detect.mjs (read full file -- understand detectInstallState return shape)
    - lib/install/components.mjs (understand how detect result is consumed)
  </read_first>
  <action>
Extend detectInstallState() in lib/install/detect.mjs to detect GSD installation and loop.md installation per project.

Add two new fields to the return object:

1. gsdInstalled: boolean -- true if ~/.claude/get-shit-done/ directory exists
   Check: existsSync(join(homedir(), '.claude', 'get-shit-done'))

2. loopMdByProject: Record<string, boolean> -- for each project, whether .claude/loop.md exists
   For each project in projects array: check existsSync(join(project.path, '.claude', 'loop.md'))
   Build object: { [project.name]: boolean }

Add these after the existing fields. Keep all existing fields unchanged.

The final return statement changes from:
  return { vaultExists, vaultPath, hooksInstalled, gitRemote, projects, profile: null };
To:
  return { vaultExists, vaultPath, hooksInstalled, gitRemote, projects, profile: null, gsdInstalled, loopMdByProject };

Add the gsdInstalled constant before the return:
  const gsdInstalled = existsSync(join(homedir(), '.claude', 'get-shit-done'));

Add the loopMdByProject constant before the return:
  const loopMdByProject = Object.fromEntries(
    projects.map(p => [p.name, existsSync(join(p.path, '.claude', 'loop.md'))])
  );
  </action>
  <acceptance_criteria>
    - grep "gsdInstalled" lib/install/detect.mjs returns match
    - grep "loopMdByProject" lib/install/detect.mjs returns match
    - grep "get-shit-done" lib/install/detect.mjs returns match (the path checked)
    - grep "loop.md" lib/install/detect.mjs returns match
    - node -e "import('./lib/install/detect.mjs').then(m => { const s = m.detectInstallState(); console.log('gsdInstalled:', typeof s.gsdInstalled, 'loopMdByProject:', typeof s.loopMdByProject); })" outputs "gsdInstalled: boolean loopMdByProject: object"
  </acceptance_criteria>
  <verify>
    <automated>grep -q "gsdInstalled" lib/install/detect.mjs && grep -q "loopMdByProject" lib/install/detect.mjs && echo "detect.mjs extended OK"</automated>
  </verify>
  <done>detect.mjs returns gsdInstalled and loopMdByProject fields</done>
</task>

<task type="auto">
  <name>Task 3: Extend lib/install/components.mjs with loop.md install step</name>
  <files>lib/install/components.mjs</files>
  <read_first>
    - lib/install/components.mjs (read full file -- understand selectComponents function)
    - lib/install/detect.mjs (understand new loopMdByProject field)
    - lib/shared.mjs (understand c, ok, info, warn, step, prompt exports)
    - bin/install.mjs (understand how selectComponents is called and what PKG_ROOT is)
  </read_first>
  <action>
Add a new exported function installLoopMd to lib/install/components.mjs. This function:
1. Accepts (stepNum, totalSteps, pkgRoot, projects, loopMdByProject) parameters
2. Offers loop.md installation for each project that has GSD installed
3. For each project: check if loop.md already installed (from loopMdByProject), offer "(already installed) -- overwrite?" if so
4. Copies templates/loop.md from pkgRoot to {project.path}/.claude/loop.md using cpSync

Add these imports at the top of components.mjs:
- import { existsSync, cpSync, mkdirSync } from 'fs';
- import { join } from 'path';
- import { homedir } from 'os';

The new function signature:
export async function installLoopMd(stepNum, totalSteps, pkgRoot, projects, loopMdByProject) {
  step(stepNum, totalSteps, 'Install loop.md for scheduled tasks');
  
  const eligibleProjects = (projects || []).filter(p => p.path && existsSync(p.path));
  
  if (eligibleProjects.length === 0) {
    info('No projects found -- skipping loop.md installation');
    return;
  }
  
  for (const project of eligibleProjects) {
    const claudeDir = join(project.path, '.claude');
    const destPath = join(claudeDir, 'loop.md');
    const alreadyInstalled = loopMdByProject?.[project.name] || false;
    
    let shouldInstall = true;
    if (alreadyInstalled) {
      const { overwrite } = await prompt({
        type: 'confirm',
        name: 'overwrite',
        message: `loop.md already installed for ${project.name} -- overwrite?`,
        initial: false,
      });
      shouldInstall = overwrite;
    } else {
      const { install } = await prompt({
        type: 'confirm',
        name: 'install',
        message: `Install loop.md for ${project.name}?`,
        initial: true,
      });
      shouldInstall = install;
    }
    
    if (shouldInstall) {
      const srcPath = join(pkgRoot, 'templates', 'loop.md');
      if (!existsSync(srcPath)) {
        warn('templates/loop.md not found in package -- skipping');
        continue;
      }
      if (!existsSync(claudeDir)) mkdirSync(claudeDir, { recursive: true });
      cpSync(srcPath, destPath);
      ok('loop.md installed to ' + destPath.replace(homedir(), '~'));
    }
  }
}

NOTE: Only add the new function. Do not modify the existing selectComponents function.
  </action>
  <acceptance_criteria>
    - grep "export async function installLoopMd" lib/install/components.mjs returns match
    - grep "loop.md" lib/install/components.mjs returns match
    - grep "cpSync" lib/install/components.mjs returns match
    - grep "loopMdByProject" lib/install/components.mjs returns match
    - node -e "import('./lib/install/components.mjs').then(m => { console.log(typeof m.installLoopMd); })" outputs "function"
  </acceptance_criteria>
  <verify>
    <automated>grep -q "installLoopMd" lib/install/components.mjs && node -e "import('./lib/install/components.mjs').then(m => { if (typeof m.installLoopMd !== 'function') throw new Error('not exported'); console.log('OK'); }).catch(e => { console.error(e.message); process.exit(1); })"</automated>
  </verify>
  <done>lib/install/components.mjs exports installLoopMd function</done>
</task>

<task type="auto">
  <name>Task 4: Wire installLoopMd into bin/install.mjs wizard flow</name>
  <files>bin/install.mjs</files>
  <read_first>
    - bin/install.mjs (read full file -- find where selectComponents is called and where to add loop.md step)
    - lib/install/components.mjs (understand installLoopMd signature)
    - lib/install/detect.mjs (understand installState.gsdInstalled and installState.loopMdByProject)
  </read_first>
  <action>
Add loop.md installation to bin/install.mjs wizard flow.

1. Update the import from lib/install/components.mjs to include installLoopMd:
   Change: import { selectComponents } from '../lib/install/components.mjs';
   To: import { selectComponents, installLoopMd } from '../lib/install/components.mjs';

2. Find where totalSteps is calculated or where steps are counted. Increment by 1 to account for the new loop.md step (only if GSD was selected).

3. After the GSD installation step (after installGSD is called or after the GSD-related work), add:

   // LIMIT-03: Install loop.md for scheduled tasks (only if GSD selected)
   if (components.gsd || installState.gsdInstalled) {
     await installLoopMd(nextStepNum, totalSteps, PKG_ROOT, projectsData?.projects || [], installState.loopMdByProject || {});
   }

   Where nextStepNum is the appropriate step number in the wizard sequence.

Find the correct insertion point by looking at the wizard flow in bin/install.mjs -- the loop.md step should come AFTER GSD installation (installGSD call) and BEFORE printSummary.

If totalSteps is hardcoded, increment it by 1. If calculated dynamically, add 1 to the count when GSD is selected.
  </action>
  <acceptance_criteria>
    - grep "installLoopMd" bin/install.mjs returns match
    - grep "loop.md\|loopMd" bin/install.mjs returns match
    - grep "LIMIT-03" bin/install.mjs returns match (the comment)
    - node -e "import('./bin/install.mjs')" does NOT throw syntax errors (module is parseable)
  </acceptance_criteria>
  <verify>
    <automated>grep -q "installLoopMd" bin/install.mjs && node --input-type=module <<< "import './bin/install.mjs'" 2>&1 | grep -v "^$" | head -5 || echo "syntax OK"</automated>
  </verify>
  <done>bin/install.mjs calls installLoopMd after GSD installation step</done>
</task>

<task type="auto">
  <name>Task 5: Add loop.md tests to tests/templates.test.mjs</name>
  <files>tests/templates.test.mjs</files>
  <read_first>
    - tests/templates.test.mjs (read full file -- add new tests without breaking existing ones)
    - templates/loop.md (verify content to assert against)
  </read_first>
  <action>
Open tests/templates.test.mjs and add a new describe block for loop.md template. Add it at the end of the file before the closing. The block must verify:
1. templates/loop.md exists
2. Contains STATE.md reference
3. Contains gsd-resume-work reference
4. Contains gsd-next reference
5. Contains gsd-health reference

New describe block to add:

  describe('templates/loop.md', () => {
    it('exists in package templates directory', () => {
      assert.ok(existsSync(join(projectRoot, 'templates', 'loop.md')), 'templates/loop.md should exist');
    });

    it('references STATE.md for state detection', () => {
      const content = readFileSync(join(projectRoot, 'templates', 'loop.md'), 'utf8');
      assert.ok(content.includes('STATE.md'), 'should reference STATE.md');
    });

    it('references gsd-resume-work for resumption', () => {
      const content = readFileSync(join(projectRoot, 'templates', 'loop.md'), 'utf8');
      assert.ok(content.includes('gsd-resume-work'), 'should reference /gsd-resume-work');
    });

    it('references gsd-next for milestone advancement', () => {
      const content = readFileSync(join(projectRoot, 'templates', 'loop.md'), 'utf8');
      assert.ok(content.includes('gsd-next'), 'should reference /gsd-next');
    });

    it('references gsd-health for health checking', () => {
      const content = readFileSync(join(projectRoot, 'templates', 'loop.md'), 'utf8');
      assert.ok(content.includes('gsd-health'), 'should reference /gsd-health');
    });
  });

Check what imports are already at the top of templates.test.mjs -- only add existsSync, readFileSync, join if not already imported.
  </action>
  <acceptance_criteria>
    - grep "loop.md" tests/templates.test.mjs returns match
    - grep "STATE.md" tests/templates.test.mjs returns match
    - grep "gsd-resume-work" tests/templates.test.mjs returns match
    - node --test tests/templates.test.mjs exits 0
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/templates.test.mjs 2>&1 | tail -5</automated>
  </verify>
  <done>templates.test.mjs has loop.md tests, all pass</done>
</task>

<task type="auto">
  <name>Task 6: Run full test suite for LIMIT-03 changes</name>
  <files/>
  <read_first>
    - package.json (confirm test command)
  </read_first>
  <action>
Run the full test suite: node --test tests/*.test.mjs

If any tests fail, diagnose and fix. Common failures:
- detect.test.mjs: detectInstallState return shape changed -- update test expectations for new fields
- install.test.mjs: wizard flow changed -- verify mock/stubs still work
- templates.test.mjs: loop.md file missing -- verify templates/loop.md was created

Report final pass/fail counts.
  </action>
  <acceptance_criteria>
    - node --test tests/*.test.mjs exits 0
    - All tests pass
    - No ERR_MODULE_NOT_FOUND
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/*.test.mjs 2>&1 | tail -10</automated>
  </verify>
  <done>Full test suite passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| templates/loop.md | Static file shipped in package, not user-controlled content |
| project.path from detect.mjs | Sourced from project-registry.md (user-owned), used with existsSync and cpSync -- no shell interpolation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-21-04 | Tampering | loop.md content | accept | User edits their own .claude/loop.md -- expected behavior, not a threat |
| T-21-05 | Spoofing | project.path | mitigate | existsSync check before cpSync prevents writing to non-existent paths |
| T-21-06 | Info Disclosure | loopMdByProject scan | accept | Only checks existence of files in user-owned project directories |
</threat_model>

<verification>
1. test -f templates/loop.md && grep -q "STATE.md" templates/loop.md && echo PASS
2. grep -q "gsdInstalled" lib/install/detect.mjs && echo PASS
3. grep -q "installLoopMd" lib/install/components.mjs && echo PASS
4. grep -q "installLoopMd" bin/install.mjs && echo PASS
5. node --test tests/templates.test.mjs && echo PASS
6. node --test tests/*.test.mjs && echo PASS
</verification>

<success_criteria>
- templates/loop.md exists with GSD maintenance loop instructions
- Install wizard offers loop.md installation after GSD step
- detect.mjs detects gsdInstalled and loopMdByProject per project
- All tests pass (>= 558 baseline + new)
- LIMIT-03 satisfied
</success_criteria>

<output>
After completion, create .planning/phases/21-continuation-prompt-loop-md/21-02-SUMMARY.md
</output>
