---
phase: 21-continuation-prompt-loop-md
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/continuation.mjs
  - lib/budget-cli.mjs
  - lib/budget.mjs
  - hooks/budget-check.mjs
  - skills/budget-continue/SKILL.md
  - tests/continuation.test.mjs
autonomous: true
requirements: [LIMIT-02]

must_haves:
  truths:
    - "lib/continuation.mjs exports showContinuationPrompt() and 4 option handlers: handleRemind, handleLocal, handleCloud, handleNow"
    - "budget-cli.mjs handles 'continue' subcommand by calling showContinuationPrompt()"
    - "hooks/budget-check.mjs appends suggestion line about running budget continue"
    - "tests/continuation.test.mjs exists and passes with node --test"
    - "skills/budget-continue/SKILL.md exists with description and invocation command"
  artifacts:
    - path: "lib/continuation.mjs"
      provides: "4-option continuation prompt logic"
      contains: "showContinuationPrompt"
    - path: "lib/budget-cli.mjs"
      provides: "budget continue subcommand"
      contains: "continue"
    - path: "tests/continuation.test.mjs"
      provides: "unit tests for continuation module"
      contains: "describe"
  key_links:
    - from: "lib/budget-cli.mjs case 'continue'"
      to: "lib/continuation.mjs showContinuationPrompt"
      via: "dynamic import"
      pattern: "showContinuationPrompt"
    - from: "hooks/budget-check.mjs"
      to: "lib/budget.mjs formatWarning"
      via: "formatWarning returns suggestion line"
      pattern: "claude-dev-stack budget continue"
---

<objective>
Implement the 4-option continuation prompt (LIMIT-02). When budget warning fires, the hook now suggests running claude-dev-stack budget continue. That command presents 4 options: Remind me later (CronCreate), Auto-continue locally (Desktop task), Auto-continue in cloud (Cloud task), Continue now (no action).

Output: lib/continuation.mjs, extended lib/budget-cli.mjs, extended lib/budget.mjs (formatWarning), skills/budget-continue/SKILL.md, tests/continuation.test.mjs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/21-continuation-prompt-loop-md/21-CONTEXT.md
@.planning/phases/21-continuation-prompt-loop-md/21-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Wave 0: Create tests/continuation.test.mjs stub</name>
  <files>tests/continuation.test.mjs</files>
  <read_first>
    - tests/budget.test.mjs (follow exact same test structure and import patterns)
  </read_first>
  <action>
Create tests/continuation.test.mjs as a stub test file that verifies lib/continuation.mjs exports. Follow the exact same import style as tests/budget.test.mjs (node:test, node:assert/strict). The file must import from ../lib/continuation.mjs and test that: showContinuationPrompt, handleRemind, handleLocal, handleCloud, handleNow are all exported as functions, and that handleNow() resolves without throwing.
  </action>
  <acceptance_criteria>
    - tests/continuation.test.mjs exists
    - File contains describe block for lib/continuation.mjs
    - File contains tests for showContinuationPrompt, handleRemind, handleLocal, handleCloud, handleNow
  </acceptance_criteria>
  <verify>
    <automated>grep -q "showContinuationPrompt" tests/continuation.test.mjs && grep -q "handleNow" tests/continuation.test.mjs && echo "stub OK"</automated>
  </verify>
  <done>tests/continuation.test.mjs stub exists</done>
</task>

<task type="auto">
  <name>Task 1: Create lib/continuation.mjs with 4-option prompt logic</name>
  <files>lib/continuation.mjs</files>
  <read_first>
    - lib/budget.mjs (understand available utilities)
    - lib/shared.mjs (understand prompt export, c, ok, info, warn)
    - lib/budget-cli.mjs (understand existing CLI pattern to extend)
    - .planning/phases/21-continuation-prompt-loop-md/21-CONTEXT.md
  </read_first>
  <action>
Create lib/continuation.mjs. Export these functions:
- showContinuationPrompt() - uses prompts (from lib/shared.mjs) single-select with 4 options
- handleRemind(delay) - prints CronCreate scheduling instruction
- handleLocal(schedule) - prints Desktop task scheduling instruction
- handleCloud(schedule, repoUrl) - prints Cloud task scheduling instruction
- handleNow() - prints "continuing now" message

Key design: The handlers print instructions for the scheduling action (CronCreate, Desktop task, Cloud task) because these Claude Code tools can only be called inside a Claude Code agent session, not from bare Node.js. The /budget-continue skill (run inside a session) can act on these instructions.

For getting git remote URL in handleCloud: use spawnSync from node:child_process with args ['git', 'remote', 'get-url', 'origin'] - this avoids shell injection. Do NOT use execSync with string interpolation.

Import prompt from lib/shared.mjs. Import c, ok, info from lib/shared.mjs.
  </action>
  <acceptance_criteria>
    - lib/continuation.mjs exists
    - grep "export async function showContinuationPrompt" lib/continuation.mjs returns match
    - grep "export async function handleRemind" lib/continuation.mjs returns match
    - grep "export async function handleLocal" lib/continuation.mjs returns match
    - grep "export async function handleCloud" lib/continuation.mjs returns match
    - grep "export async function handleNow" lib/continuation.mjs returns match
    - node --test tests/continuation.test.mjs exits 0
  </acceptance_criteria>
  <verify>
    <automated>node -e "import('./lib/continuation.mjs').then(m => { ['showContinuationPrompt','handleRemind','handleLocal','handleCloud','handleNow'].forEach(n => { if (typeof m[n] !== 'function') throw new Error('Missing: ' + n); }); console.log('ALL EXPORTS OK'); }).catch(e => { console.error(e.message); process.exit(1); })"</automated>
  </verify>
  <done>lib/continuation.mjs exists with all 5 exports, tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Add budget continue subcommand to lib/budget-cli.mjs</name>
  <files>lib/budget-cli.mjs</files>
  <read_first>
    - lib/budget-cli.mjs (read full file - add case before default, keep all existing cases)
  </read_first>
  <action>
Extend the switch statement in lib/budget-cli.mjs. Before the default: case, add:

  case 'continue': {
    const { showContinuationPrompt } = await import('./continuation.mjs');
    await showContinuationPrompt();
    break;
  }

Use dynamic import (await import) to match the existing module loading style. Do not add top-level imports. Do not change any existing cases.
  </action>
  <acceptance_criteria>
    - grep "case 'continue'" lib/budget-cli.mjs returns match
    - grep "showContinuationPrompt" lib/budget-cli.mjs returns match
    - node -e "import('./lib/budget-cli.mjs').then(() => console.log('OK'))" exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -q "case 'continue'" lib/budget-cli.mjs && grep -q "showContinuationPrompt" lib/budget-cli.mjs && echo "continue subcommand added"</automated>
  </verify>
  <done>lib/budget-cli.mjs handles budget continue subcommand</done>
</task>

<task type="auto">
  <name>Task 3: Extend formatWarning in lib/budget.mjs with continue suggestion</name>
  <files>lib/budget.mjs</files>
  <read_first>
    - lib/budget.mjs (locate formatWarning function - approximately lines 160-178)
  </read_first>
  <action>
In lib/budget.mjs, find the formatWarning function. It returns an array joined with '\n'. Add one new line to the array before the final empty string:

     '     Run: claude-dev-stack budget continue  -- to choose your next step.',

Insert this line after the "Consider wrapping up" line and before the final '' (empty string) in the array. The full array after change:
[
  '',
  '  BUDGET WARNING: Session context is ${percent}% full (threshold: ${threshold}%)',
  '     Used: ...',
  '     Remaining: ...',
  '     Consider wrapping up or starting a new session soon.',
  '     Run: claude-dev-stack budget continue  -- to choose your next step.',
  '',
]
  </action>
  <acceptance_criteria>
    - grep "claude-dev-stack budget continue" lib/budget.mjs returns match
    - node -e "import('./lib/budget.mjs').then(m => { const w = m.formatWarning(75, 70, 150000, 200000); if (!w.includes('claude-dev-stack budget continue')) throw new Error('missing'); console.log('OK'); }).catch(e => { console.error(e); process.exit(1); })" exits 0
  </acceptance_criteria>
  <verify>
    <automated>node -e "import('./lib/budget.mjs').then(m => { const w = m.formatWarning(75,70,150000,200000); process.exit(w.includes('claude-dev-stack budget continue') ? 0 : 1); })" && echo "PASS"</automated>
  </verify>
  <done>formatWarning includes continue suggestion line</done>
</task>

<task type="auto">
  <name>Task 4: Create skills/budget-continue/SKILL.md</name>
  <files>skills/budget-continue/SKILL.md</files>
  <read_first>
    - skills/ (list directory to see existing skill structure)
  </read_first>
  <action>
Create directory skills/budget-continue/ and write SKILL.md. The skill file must have frontmatter with name, description, and trigger fields. Body must document the 4 continuation options, usage command (claude-dev-stack budget continue), and notes about CronCreate, Desktop task, and Cloud task scheduling primitives.

Frontmatter:
---
name: budget-continue
description: Show 4-option continuation prompt when session budget is low. Options: remind later (CronCreate), auto-continue locally (Desktop task), auto-continue in cloud (Cloud task), continue now.
trigger: budget low, session limit, context full, budget warning
---
  </action>
  <acceptance_criteria>
    - skills/budget-continue/SKILL.md exists
    - grep "budget-continue" skills/budget-continue/SKILL.md returns match (frontmatter name)
    - grep "CronCreate" skills/budget-continue/SKILL.md returns match
    - grep "Desktop task" skills/budget-continue/SKILL.md returns match
    - grep "Cloud task" skills/budget-continue/SKILL.md returns match
  </acceptance_criteria>
  <verify>
    <automated>test -f skills/budget-continue/SKILL.md && grep -q "CronCreate" skills/budget-continue/SKILL.md && echo "skill file OK"</automated>
  </verify>
  <done>skills/budget-continue/SKILL.md exists with correct content</done>
</task>

<task type="auto">
  <name>Task 5: Add formatWarning test to tests/budget.test.mjs</name>
  <files>tests/budget.test.mjs</files>
  <read_first>
    - tests/budget.test.mjs (find the formatWarning describe block to add assertion)
  </read_first>
  <action>
Find the existing describe('formatWarning') block in tests/budget.test.mjs. Inside it, add a new it() assertion:

  it('includes continue suggestion line', () => {
    const w = formatWarning(75, 70, 150000, 200000);
    assert.ok(w.includes('claude-dev-stack budget continue'), 'should include continue suggestion');
  });

Add it after existing assertions in the formatWarning block. Do not change existing tests.
  </action>
  <acceptance_criteria>
    - grep "claude-dev-stack budget continue" tests/budget.test.mjs returns match
    - node --test tests/budget.test.mjs exits 0
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/budget.test.mjs 2>&1 | tail -3</automated>
  </verify>
  <done>budget.test.mjs has continue suggestion test, all budget tests pass</done>
</task>

<task type="auto">
  <name>Task 6: Run full test suite</name>
  <files/>
  <read_first>
    - package.json (confirm test command)
  </read_first>
  <action>
Run the full test suite: node --test tests/*.test.mjs

If any tests fail, diagnose and fix. Report final pass/fail counts.
  </action>
  <acceptance_criteria>
    - node --test tests/*.test.mjs exits 0
    - All tests pass, none fail
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/*.test.mjs 2>&1 | tail -10</automated>
  </verify>
  <done>Full test suite passes with no failures</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI stdin | User selects from menu -- no arbitrary string injection |
| git remote URL | Used as display text only, obtained via spawnSync with args array (no shell injection) |
| Budget state file | Parsed with JSON.parse, never eval |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-21-01 | Tampering | budget-state.json | accept | User-owned file, corrupt state handled gracefully |
| T-21-02 | Info Disclosure | git remote URL | mitigate | Shown as display text only, obtained with spawnSync args array not shell string |
| T-21-03 | Denial of Service | prompts in non-TTY | mitigate | try/catch around prompt; falls back to handleNow() |
</threat_model>

<verification>
1. grep -q "showContinuationPrompt" lib/continuation.mjs && echo PASS
2. grep -q "case 'continue'" lib/budget-cli.mjs && echo PASS
3. grep -q "claude-dev-stack budget continue" lib/budget.mjs && echo PASS
4. test -f skills/budget-continue/SKILL.md && echo PASS
5. node --test tests/continuation.test.mjs && echo PASS
6. node --test tests/*.test.mjs && echo PASS
</verification>

<success_criteria>
- lib/continuation.mjs exports showContinuationPrompt and 4 option handlers
- claude-dev-stack budget continue invokes the prompt
- budget warning includes continue suggestion line
- skill file exists for in-session /budget-continue invocation
- All tests pass (>= 558 baseline + new tests)
- LIMIT-02 satisfied
</success_criteria>

<output>
After completion, create .planning/phases/21-continuation-prompt-loop-md/21-01-SUMMARY.md
</output>
