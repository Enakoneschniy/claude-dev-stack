---
phase: 25-budget-aware-execution-gate
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - LIMIT-05
response_language: ru
tags: [docs, requirements, traceability, backfill]

user_setup: []

must_haves:
  truths:
    - ".planning/REQUIREMENTS.md Limit Management section contains an `LIMIT-05` entry whose description matches the Phase 25 success criteria from ROADMAP.md (5 numbered criteria covering pre-exec gate, tight-budget prompt, Schedule after reset CronCreate, statusline plan usage, statusline reads from 60s cache)"
    - ".planning/REQUIREMENTS.md Traceability table has a row `| LIMIT-05 | 25 | — | pending |`"
    - "Total requirement count in REQUIREMENTS.md header increments from 10 to 11 (plus 13 non-v1 requirements already listed, so total rows in Traceability becomes 25)"
    - "No existing requirement entries modified or removed — purely additive backfill"
    - "Change is documented in a conventional commit `docs(25): backfill LIMIT-05 into REQUIREMENTS.md`"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "LIMIT-05 requirement definition + traceability entry (D-11 closure)"
      contains: "LIMIT-05"
  key_links:
    - from: ".planning/REQUIREMENTS.md Limit Management section"
      to: ".planning/ROADMAP.md Phase 25 Success Criteria"
      via: "verbatim copy of the 5 success criteria as the LIMIT-05 description"
      pattern: "LIMIT-05"
    - from: ".planning/REQUIREMENTS.md Traceability table"
      to: "Phase 25"
      via: "new row mapping LIMIT-05 → Phase 25"
      pattern: "| LIMIT-05 | 25 |"
---

<objective>
Backfill `LIMIT-05` into `.planning/REQUIREMENTS.md` so the milestone requirements registry matches the ROADMAP.md phase coverage. Currently LIMIT-05 exists only in ROADMAP.md Phase 25, creating a silent drift between the two canonical documents.

Purpose: D-11 from CONTEXT.md explicitly requires closing this drift before Phase 25 ships. Without this backfill, `/gsd-audit-milestone` and similar tooling will flag REQUIREMENTS.md as out-of-date at milestone close, and the traceability table is incomplete.

Output: One file modified, one requirement entry added under "Limit Management (LIMIT)", one row added to the Traceability table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/25-budget-aware-execution-gate/25-CONTEXT.md

<interfaces>
<!-- Source of truth for the LIMIT-05 description: ROADMAP.md Phase 25 Success Criteria -->
<!-- Executor MUST copy the 5 numbered criteria verbatim as the LIMIT-05 definition body. -->

Current REQUIREMENTS.md Limit Management section ends at LIMIT-04 (line 27). Insert LIMIT-05 directly after LIMIT-04 as the fifth bullet.

Current Traceability table structure (lines 76–102):
```
| REQ-ID | Phase | Plan | Status |
|--------|-------|------|--------|
| BUG-01 | 19 | — | pending |
...
| LIMIT-04 | 22 | — | pending |
| DX-07 | 23 | — | pending |
...
```

Insertion point: insert `| LIMIT-05 | 25 | — | pending |` row AFTER LIMIT-04 and BEFORE DX-07 (maintains REQ-ID grouping order).

Header "Total requirements: 10 v1 requirements." at line 7 must be updated to "11 v1 requirements." (LIMIT-05 is a v1 requirement for the Hooks & Limits milestone — it ships in v0.12 alongside other LIMIT-* requirements).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add LIMIT-05 to REQUIREMENTS.md (definition + traceability + header count)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (full file — 103 lines — read before editing)
    - .planning/ROADMAP.md lines 134–145 (Phase 25 definition — the 5 success criteria are the source of truth for LIMIT-05's description)
    - .planning/phases/25-budget-aware-execution-gate/25-CONTEXT.md D-11
  </read_first>
  <acceptance_criteria>
    - `grep -c "LIMIT-05" .planning/REQUIREMENTS.md` ≥ 2 (one in definition, one in traceability).
    - `grep -n "LIMIT-05" .planning/REQUIREMENTS.md` shows LIMIT-05 appearing AFTER LIMIT-04 in the Limit Management section AND AFTER LIMIT-04 in the Traceability table (both sections maintain ordered grouping).
    - `grep -c "^| LIMIT-05 | 25 | — | pending |" .planning/REQUIREMENTS.md` === 1 (exact traceability row format matches existing rows).
    - Header line updated: `grep -c "11 v1 requirements" .planning/REQUIREMENTS.md` === 1 (was "10 v1 requirements").
    - `git diff .planning/REQUIREMENTS.md` shows only additions (5 lines: 1 requirement bullet, 1 traceability row, 1 header word change — plus any surrounding blank lines).
    - No other requirement entries touched — `diff` should be purely additive.
    - File passes markdown lint / frontmatter check equivalent (it's a plain markdown file — just ensure pipe-table alignment matches existing rows).
  </acceptance_criteria>
  <action>
    Edit `.planning/REQUIREMENTS.md`:

    **Change 1** (line 7 — header):
    Replace `**Total requirements**: 10 v1 requirements.` with `**Total requirements**: 11 v1 requirements.`

    **Change 2** (after current LIMIT-04 bullet, line 27 — within "Limit Management (LIMIT)" section):
    Insert as a new bullet directly after LIMIT-04 and before the blank line separating LIMIT from the next subsection. The bullet format must match the existing LIMIT-* entries (checkbox, bold ID, colon, prose):

    ```markdown
    - [ ] **LIMIT-05**: Budget-aware execution gate. Before `/gsd-execute-phase`, `/gsd-plan-phase`, or any expensive GSD subagent spawn, a PreToolUse Skill hook queries plan usage and estimates operation cost from rolling-average history. If projected `5h` utilization exceeds the gate threshold (default 80%), the hook surfaces a 3-option prompt: Execute now / Schedule after reset (creates CronCreate task timed for `five_hour.resets_at`) / Cancel — with concrete numbers (current %, estimate %, projected %, reset time). Claude Code statusline shows real-time plan usage (`5h:XX% 7d:XX%` plus optional `extra:$N`) alongside existing context progress bar. Statusline reads from the Phase 20 60-second cache — no new API calls. Gate fails open on cache miss / stale data to never block a session.
    ```

    Use the exact wording above (derived verbatim from ROADMAP.md Phase 25 Success Criteria 1–5 with D-01, D-06, D-10 implementation details inlined). This matches the granularity of neighboring LIMIT-* bullets.

    **Change 3** (Traceability table — after line containing `| LIMIT-04 | 22 | — | pending |`, before line containing `| DX-07 | 23 | — | pending |`):
    Insert new row:
    ```markdown
    | LIMIT-05 | 25 | — | pending |
    ```

    Maintain pipe-table alignment — use the same column widths as neighboring rows. The existing rows use compact formatting (single space padding around values) — match that style.

    **Do NOT modify:**
    - Any other requirement bullet or row.
    - The "Future Requirements" or "Out of Scope" sections.
    - Section heading text.
    - File-level frontmatter (there is none — plain markdown).

    Implements D-11.
  </action>
  <verify>
    <automated>test "$(grep -c '^- \[ \] \*\*LIMIT-05\*\*' .planning/REQUIREMENTS.md)" = "1" &amp;&amp; test "$(grep -c '^| LIMIT-05 | 25 | — | pending |' .planning/REQUIREMENTS.md)" = "1" &amp;&amp; test "$(grep -c '11 v1 requirements' .planning/REQUIREMENTS.md)" = "1"</automated>
  </verify>
  <done>
    REQUIREMENTS.md contains LIMIT-05 as a first-class v1 requirement with matching traceability row and updated total count. The silent drift between ROADMAP.md and REQUIREMENTS.md is closed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan touches only `.planning/REQUIREMENTS.md` (a documentation file under user-controlled version control). There are no external inputs, no code paths, no runtime surface, and no new data flows. The STRIDE register is intentionally minimal — this is a pure documentation backfill.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-14 | Tampering | REQUIREMENTS.md edits could accidentally delete existing requirement entries | mitigate | Acceptance criteria asserts `git diff` is purely additive (no deletions). Executor must verify via `git diff .planning/REQUIREMENTS.md` before commit. Test assertion via `grep -c` counts on unrelated existing rows (BUG-01..06, LIMIT-01..04, DX-07..13, UX-01..07, WF-01, BUG-07) — if any drop from their expected count of 1, executor knows the edit corrupted state. |
| T-25-15 | Repudiation | Doc change without clear commit message loses traceability of why LIMIT-05 was backfilled | mitigate | Commit message format specified: `docs(25): backfill LIMIT-05 into REQUIREMENTS.md` — conventional commit with phase reference and D-11 provenance described in the body. |
</threat_model>

<verification>
- `grep -c "LIMIT-05" .planning/REQUIREMENTS.md` === 2
- `git diff .planning/REQUIREMENTS.md | grep -c "^-"` ≤ 3 (only the "10 v1" header line removal counts; anything more means accidental deletion — investigate)
- `grep -c "^- \[ \] \*\*LIMIT-0[1-5]\*\*" .planning/REQUIREMENTS.md` === 5 (all five LIMIT requirements now present)
- `grep -c "^| LIMIT-0[1-5] |" .planning/REQUIREMENTS.md` === 5 (all five in traceability)
- `npm test` baseline still green (no code changes — regression unlikely but run anyway).
</verification>

<success_criteria>
- D-11 satisfied: REQUIREMENTS.md and ROADMAP.md are in sync for Phase 25 / LIMIT-05.
- Traceability count: Coverage table now maps 11 v1 requirements + 14 non-v1 = 25 total entries.
- No regression in any other requirement entry.
</success_criteria>

<output>
After completion, create `.planning/phases/25-budget-aware-execution-gate/25-03-SUMMARY.md` covering:
- Single file modified (.planning/REQUIREMENTS.md)
- Diff summary: 3 additions (header count update, LIMIT-05 bullet, LIMIT-05 traceability row)
- Confirmation that no other requirement bullets / rows were modified
- Link to ROADMAP.md Phase 25 as source of truth for the LIMIT-05 description
</output>
