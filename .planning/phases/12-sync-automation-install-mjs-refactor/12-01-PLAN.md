---
phase: 12-sync-automation-install-mjs-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hooks/session-end-check.sh
  - hooks/notebooklm-sync-trigger.mjs
  - hooks/notebooklm-sync-runner.mjs
  - tests/sync-automation.test.mjs
autonomous: true
requirements:
  - SYNC-01

must_haves:
  truths:
    - "Session-end hook triggers notebooklm sync via notebooklm-sync-trigger.mjs"
    - "Sync runs in a detached background process (parent exits immediately)"
    - "Failure exits 0 — session end is never interrupted by sync failure"
    - "Log output goes to ~/vault/.notebooklm-sync.log"
  artifacts:
    - path: "tests/sync-automation.test.mjs"
      provides: "Structural verification tests for SYNC-01 hook chain"
      min_lines: 40
  key_links:
    - from: "hooks/session-end-check.sh"
      to: "hooks/notebooklm-sync-trigger.mjs"
      via: "node invocation with || true"
      pattern: "notebooklm-sync-trigger"
    - from: "hooks/notebooklm-sync-trigger.mjs"
      to: "hooks/notebooklm-sync-runner.mjs"
      via: "spawn detached child"
      pattern: "spawn.*notebooklm-sync-runner"
---

<objective>
Verify that the SYNC-01 session-end sync automation (implemented in v0.9) meets all four success criteria from REQUIREMENTS.md, and write structural tests to lock in this behavior.

Purpose: SYNC-01 was already implemented in Phase 9. This plan confirms it works and adds regression tests so future changes cannot silently break the hook chain. Per D-01 and D-02, no new implementation is expected — only verification and test creation.

Output: `tests/sync-automation.test.mjs` with structural verification tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-sync-automation-install-mjs-refactor/12-RESEARCH.md

@hooks/session-end-check.sh
@hooks/notebooklm-sync-trigger.mjs
@hooks/notebooklm-sync-runner.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify SYNC-01 criteria and write structural tests</name>
  <files>tests/sync-automation.test.mjs</files>
  <read_first>
    hooks/session-end-check.sh
    hooks/notebooklm-sync-trigger.mjs
    hooks/notebooklm-sync-runner.mjs
    tests/install.test.mjs (for existing test patterns)
  </read_first>
  <action>
Read the three hook-chain files and confirm against SYNC-01 four success criteria (per D-02):

1. **Trigger**: `hooks/session-end-check.sh` invokes `node "$TRIGGER"` where TRIGGER is `notebooklm-sync-trigger.mjs`, with `|| true` for non-blocking
2. **Background**: `notebooklm-sync-trigger.mjs` uses `spawn(..., {detached: true})` + `child.unref()` + `process.exit(0)` — parent exits immediately
3. **Failure non-blocking**: trigger has outer `try/catch { process.exit(0) }`, runner has `uncaughtException` + `unhandledRejection` handlers both calling `process.exit(0)`
4. **Log path**: runner uses `join(VAULT_ROOT, '.notebooklm-sync.log')` with `appendFileSync`

Then create `tests/sync-automation.test.mjs` with structural tests using `node:test` and `node:assert`:

- `describe('SYNC-01: session-end sync automation')` with tests:
  - Test: session-end-check.sh contains reference to notebooklm-sync-trigger.mjs (grep source)
  - Test: session-end-check.sh uses `|| true` after node invocation (non-blocking)
  - Test: notebooklm-sync-trigger.mjs contains `detached: true` in spawn options
  - Test: notebooklm-sync-trigger.mjs calls `child.unref()` and `process.exit(0)`
  - Test: notebooklm-sync-trigger.mjs has try/catch with `process.exit(0)` in catch
  - Test: notebooklm-sync-runner.mjs has `uncaughtException` handler
  - Test: notebooklm-sync-runner.mjs has `unhandledRejection` handler
  - Test: notebooklm-sync-runner.mjs references `.notebooklm-sync.log` path
  - Test: notebooklm-sync-runner.mjs uses `appendFileSync` for logging

Use the same grep-based structural test pattern as `tests/install.test.mjs` — read source files with `readFileSync` and assert they contain expected patterns.

File paths in tests should use `join(dirname(fileURLToPath(import.meta.url)), '..', 'hooks', 'filename')` for resolution.
  </action>
  <verify>
    <automated>node --test tests/sync-automation.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `tests/sync-automation.test.mjs` exists and passes all tests
    - Tests verify all 4 SYNC-01 criteria via source text assertions
    - `npm test` still passes (new file picked up by `tests/*.test.mjs` glob)
  </acceptance_criteria>
  <done>
    All 4 SYNC-01 success criteria verified by structural tests. SYNC-01 marked as satisfied.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries — this plan only reads existing files and creates tests.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | T (Tampering) | Hook chain files | accept | Read-only verification; hook files unchanged. Structural tests lock in expected patterns. |
</threat_model>

<verification>
- `node --test tests/sync-automation.test.mjs` passes
- `npm test` passes (full suite including new test file)
- All 4 SYNC-01 criteria have corresponding test assertions
</verification>

<success_criteria>
- tests/sync-automation.test.mjs exists with 9+ structural assertions covering all 4 SYNC-01 criteria
- npm test passes with 0 failures
- SYNC-01 requirement is satisfied (verification-only, no new implementation)
</success_criteria>

<output>
After completion, create `.planning/phases/12-sync-automation-install-mjs-refactor/12-01-SUMMARY.md`
</output>
