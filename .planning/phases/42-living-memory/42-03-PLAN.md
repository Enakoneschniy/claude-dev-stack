---
phase: 42-living-memory
plan: 03
type: execute
wave: 3
depends_on: ["42-02"]
files_modified:
  - skills/cds-search/SKILL.md
  - skills/cds-stats/SKILL.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "/cds-search skill dispatches MCP tool sessions.search with user query"
    - "/cds-search skill falls back to CLI when MCP unavailable"
    - "/cds-stats skill calls MCP tools for planning status and session counts"
  artifacts:
    - path: "skills/cds-search/SKILL.md"
      provides: "/cds-search skill with MCP + CLI fallback"
      contains: "mcp__cds__sessions.search"
    - path: "skills/cds-stats/SKILL.md"
      provides: "/cds-stats skill with MCP tools"
      contains: "mcp__cds__planning.status"
  key_links:
    - from: "skills/cds-search/SKILL.md"
      to: "MCP server sessions.search"
      via: "mcp__cds__sessions.search tool call"
      pattern: "mcp__cds__sessions.search"
    - from: "skills/cds-search/SKILL.md"
      to: "bin/cli.mjs search"
      via: "npx claude-dev-stack search fallback"
      pattern: "npx claude-dev-stack search"
---

<objective>
Create `/cds-search` (D-142) and `/cds-stats` (D-143) Claude Code skills that make session memory queryable via slash commands during a session.

Purpose: Skills bridge MCP tools (Phase 37) into the user-facing slash command interface. `/cds-search` wraps `sessions.search` with a CLI fallback. `/cds-stats` combines `planning.status` and session counts into a dashboard.

Output: Two skill directories with SKILL.md files following the `cds-quick` skill pattern.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/42-living-memory/42-RESEARCH.md
@.planning/phases/42-living-memory/42-PATTERNS.md

<interfaces>
<!-- Skill YAML frontmatter pattern from cds-quick -->

From skills/cds-quick/SKILL.md:
```markdown
---
name: cds-quick
description: |
  Run a quick one-shot task using a fast model (Haiku). Single-dispatch,
  no multi-turn. For complex work, use Claude Code normally instead.
trigger_phrases:
  - /cds-quick
  - cds-quick
  - quick task:
---
```

<!-- MCP tool naming convention (double underscore) -->
MCP tool call format in Claude Code: `mcp__cds__sessions.search({ query: "..." })`
MCP server name in settings.json: `cds` (registered by wizard)
Available MCP tools: sessions.search, sessions.timeline, sessions.get_observations, docs.search, planning.status
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /cds-search skill</name>
  <read_first>
    - skills/cds-quick/SKILL.md
  </read_first>
  <files>skills/cds-search/SKILL.md</files>
  <action>
Create `skills/cds-search/SKILL.md` per D-142:

```markdown
---
name: cds-search
description: |
  Search past session observations using the sessions.search MCP tool.
  Falls back to CLI if the MCP server is not available.
trigger_phrases:
  - /cds-search
  - cds-search
---

# /cds-search -- Search session memory

Search for past decisions, bugs, patterns, and observations stored in the project's SQLite session database.

## How to execute

1. Extract the search query from `$ARGUMENTS` (everything after `/cds-search`).

2. **Primary path (MCP tool):**
   Call `mcp__cds__sessions.search` with the query:
   ```
   mcp__cds__sessions.search({ query: "$ARGUMENTS" })
   ```
   Format the returned hits as a readable list showing:
   - Observation type (decision, bug, pattern, etc.)
   - Content snippet (first 2-3 lines)
   - Session date
   - Session ID (abbreviated)

3. **Fallback path (CLI):**
   If `mcp__cds__sessions.search` is not available (MCP server not registered), use Bash:
   ```
   Bash("npx claude-dev-stack search \"$ARGUMENTS\"")
   ```
   Display the CLI output verbatim.

## When to use

- User asks about past decisions ("what did we decide about X")
- User asks about past bugs ("did we see this bug before")
- User asks about patterns ("how did we implement X")
- User explicitly invokes `/cds-search`

## Notes

- Results are ranked by FTS5 BM25 relevance
- Only searches the current project's sessions.db
- For cross-project search, use the CLI directly on each project
```

Key requirements per D-142:
- MCP tool name uses double-underscore format: `mcp__cds__sessions.search`
- Fallback uses `npx claude-dev-stack search` (not bare `claude-dev-stack`) per RESEARCH Pitfall 6
- YAML frontmatter matches `cds-quick` pattern exactly
  </action>
  <verify>
    <automated>cd /Users/eugenenakoneschniy/Projects/claude-dev-stack && test -f skills/cds-search/SKILL.md && grep -c 'mcp__cds__sessions.search' skills/cds-search/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `skills/cds-search/SKILL.md` exists
    - File has YAML frontmatter with `name: cds-search`
    - File has `trigger_phrases` including `/cds-search` and `cds-search`
    - File contains `mcp__cds__sessions.search({ query: "$ARGUMENTS" })`
    - File contains `npx claude-dev-stack search` as fallback
    - File does NOT use bare `claude-dev-stack search` (uses `npx` prefix)
  </acceptance_criteria>
  <done>/cds-search skill created with MCP primary path and CLI fallback.</done>
</task>

<task type="auto">
  <name>Task 2: Create /cds-stats skill</name>
  <read_first>
    - skills/cds-quick/SKILL.md
    - skills/cds-search/SKILL.md
  </read_first>
  <files>skills/cds-stats/SKILL.md</files>
  <action>
Create `skills/cds-stats/SKILL.md` per D-143:

```markdown
---
name: cds-stats
description: |
  Show project memory statistics: session count, observation counts by type,
  top entities, and project planning status.
trigger_phrases:
  - /cds-stats
  - cds-stats
---

# /cds-stats -- Project memory dashboard

Display a summary of the project's session memory and planning status.

## How to execute

1. **Get planning status (MCP tool):**
   ```
   mcp__cds__planning.status()
   ```
   This returns the current ROADMAP phase counts and STATE position.

2. **Get session memory stats:**
   Use Bash to call the mem-stats CLI command:
   ```
   Bash("npx claude-dev-stack mem-stats")
   ```
   This returns: project name, session count (total + this week), observation counts by type, last activity.

3. **Combine and present:**
   Format both outputs into a single dashboard:
   ```
   Project Memory Dashboard
   ========================

   [planning.status output -- phase progress, current position]

   [mem-stats output -- session count, observations, last activity]
   ```

## Fallback

If `mcp__cds__planning.status` is not available, skip the planning section and show only the mem-stats CLI output.

## When to use

- User asks "how's the project going"
- User asks about session/memory statistics
- User wants to see project health overview
- User explicitly invokes `/cds-stats`
```

Key requirements per D-143:
- Calls `mcp__cds__planning.status` (no args) for planning data
- Calls `npx claude-dev-stack mem-stats` via Bash for session stats (not MCP -- the stats data is not exposed via MCP tool, it is a CLI-only command)
- Combines both outputs into a single dashboard
- Graceful fallback if MCP unavailable
  </action>
  <verify>
    <automated>cd /Users/eugenenakoneschniy/Projects/claude-dev-stack && test -f skills/cds-stats/SKILL.md && grep -c 'mcp__cds__planning.status' skills/cds-stats/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `skills/cds-stats/SKILL.md` exists
    - File has YAML frontmatter with `name: cds-stats`
    - File has `trigger_phrases` including `/cds-stats` and `cds-stats`
    - File contains `mcp__cds__planning.status()`
    - File contains `npx claude-dev-stack mem-stats` for session stats
    - File describes fallback behavior when MCP unavailable
  </acceptance_criteria>
  <done>/cds-stats skill created with MCP planning status + CLI mem-stats combination.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user query -> MCP tool args | User's search text passed as query parameter to MCP tool |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-07 | Tampering | search query injection | mitigate | sessionsSearch (Phase 37) handles FTS5 syntax errors; MCP server validates input schema |
| T-42-08 | Denial of Service | large query string | accept | FTS5 MATCH has built-in limits; local-only tool, no remote attack surface |
</threat_model>

<verification>
1. `test -f skills/cds-search/SKILL.md` -- file exists
2. `test -f skills/cds-stats/SKILL.md` -- file exists
3. `grep 'mcp__cds__sessions.search' skills/cds-search/SKILL.md` -- MCP tool reference
4. `grep 'npx claude-dev-stack search' skills/cds-search/SKILL.md` -- CLI fallback
5. `grep 'mcp__cds__planning.status' skills/cds-stats/SKILL.md` -- MCP tool reference
6. `grep 'npx claude-dev-stack mem-stats' skills/cds-stats/SKILL.md` -- CLI stats
</verification>

<success_criteria>
- Both SKILL.md files exist with valid YAML frontmatter
- /cds-search dispatches MCP tool with fallback to CLI
- /cds-stats combines planning status and session stats
- MCP tool names use correct double-underscore format
</success_criteria>

<output>
After completion, create `.planning/phases/42-living-memory/42-03-SUMMARY.md`
</output>
