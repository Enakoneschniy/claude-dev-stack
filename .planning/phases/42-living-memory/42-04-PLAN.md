---
phase: 42-living-memory
plan: 04
type: execute
wave: 2
depends_on: ["42-01"]
files_modified:
  - lib/install/claude-md.mjs
autonomous: true
requirements: []

must_haves:
  truths:
    - "CLAUDE.md template includes a Memory section instructing Claude to use MCP tools"
    - "Memory section is placed between Session Protocol and Code Style"
    - "Memory section references sessions.search, docs.search, and planning.status MCP tools"
  artifacts:
    - path: "lib/install/claude-md.mjs"
      provides: "Updated managedBody template with Memory section"
      contains: "mcp__cds__sessions.search"
  key_links:
    - from: "lib/install/claude-md.mjs"
      to: "MCP server tools"
      via: "Template text referencing mcp__cds__ tool names"
      pattern: "mcp__cds__"
---

<objective>
Update the CLAUDE.md template generator to include a "Memory" section (D-141) that instructs Claude to automatically use MCP tools for session queries, documentation search, and planning status.

Purpose: This is the "passive activation" of session memory -- every new or re-installed project gets CLAUDE.md instructions that make Claude proactively query past sessions when users ask about past work. Without this, MCP tools exist but Claude never knows to use them.

Output: Updated `managedBody` template literal in `lib/install/claude-md.mjs` with a `## Memory` section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/42-living-memory/42-RESEARCH.md
@.planning/phases/42-living-memory/42-PATTERNS.md

<interfaces>
<!-- Current managedBody template structure in claude-md.mjs -->

From lib/install/claude-md.mjs (lines 68-109):
```javascript
const managedBody = `## Language
${langLine}

## Auto-Routing (IMPORTANT)
Do NOT ask which tool to use. Determine automatically:
...

## Knowledge Base
Project context is auto-loaded at session start by the SessionStart hook
...

## Session Protocol
- Start: read context + propose continuation from last TODO
- During: ADR for decisions, Known Issues for bugs, shared/patterns.md for reusables
- End ("done"/"всё"/"хватит"): create session log, update context.md

## Code Style
- Commits: conventional commits (feat:, fix:, chore:)
- Code and comments in ${profile.codeLang}
- Communication in ${profile.lang}

## Rules
...

## References
...

${generateSkillsSection({ withMarkers: false })}`;
```

The Memory section must be inserted BETWEEN `## Session Protocol` and `## Code Style`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Memory section to CLAUDE.md template</name>
  <read_first>
    - lib/install/claude-md.mjs
  </read_first>
  <files>lib/install/claude-md.mjs</files>
  <action>
In `lib/install/claude-md.mjs`, find the `managedBody` template literal. Locate the `## Session Protocol` section and the `## Code Style` section that follows it.

Insert a new `## Memory` section between them. The current template has (approximately lines 88-93):

```
## Session Protocol
- Start: read context + propose continuation from last TODO
- During: ADR for decisions, Known Issues for bugs, shared/patterns.md for reusables
- End ("done"/"всё"/"хватит"): create session log, update context.md

## Code Style
```

Change it to:

```
## Session Protocol
- Start: read context + propose continuation from last TODO
- During: ADR for decisions, Known Issues for bugs, shared/patterns.md for reusables
- End ("done"/"всё"/"хватит"): create session log, update context.md

## Memory
When the user asks about past decisions, work done, bugs found, or patterns established:
- Call \`mcp__cds__sessions.search\` with the topic as query
- Never say "I don't have access to past sessions" -- always try the MCP tool first
When the user asks about project documentation:
- Call \`mcp__cds__docs.search\` with the query
When the user asks about project progress:
- Call \`mcp__cds__planning.status\` (no args needed)

## Code Style
```

Important details per D-141:
- Section title is `## Memory` (H2)
- Backticks around MCP tool names must be escaped with backslash inside the template literal: `\`mcp__cds__sessions.search\``
- Use double-dash `--` not em-dash per project style
- Placement is AFTER `## Session Protocol` and BEFORE `## Code Style`
- Do NOT modify any other sections
- The text must match exactly -- Claude will follow these instructions literally
  </action>
  <verify>
    <automated>cd /Users/eugenenakoneschniy/Projects/claude-dev-stack && grep -c 'mcp__cds__sessions.search' lib/install/claude-md.mjs && grep -c '## Memory' lib/install/claude-md.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `lib/install/claude-md.mjs` contains `## Memory` section in managedBody template
    - Memory section contains `mcp__cds__sessions.search` (escaped as `\`mcp__cds__sessions.search\``)
    - Memory section contains `mcp__cds__docs.search` (escaped)
    - Memory section contains `mcp__cds__planning.status` (escaped)
    - Memory section contains "Never say" instruction about past sessions
    - Memory section appears AFTER `## Session Protocol` and BEFORE `## Code Style` in the template
    - No other sections are modified
    - `node lib/install/claude-md.mjs` does not crash (syntax check -- the file is a module so cannot run standalone, but `node -c lib/install/claude-md.mjs` syntax check should pass)
  </acceptance_criteria>
  <done>CLAUDE.md template includes Memory section instructing Claude to use MCP tools for session queries, doc search, and planning status.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Template -> CLAUDE.md | Template content becomes part of Claude's system instructions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-09 | Tampering | template injection | accept | Template is developer-controlled source code, not user input; no injection vector |
| T-42-10 | Elevation of Privilege | MCP tool instructions | accept | MCP tools are read-only (search/status); no write operations instructed |
</threat_model>

<verification>
1. `grep -A 10 '## Memory' lib/install/claude-md.mjs` -- section content visible
2. `grep -B 2 '## Memory' lib/install/claude-md.mjs` -- preceded by Session Protocol section
3. `grep -A 2 'planning.status' lib/install/claude-md.mjs` -- followed by Code Style section
4. `node -e "import('./lib/install/claude-md.mjs')" 2>&1` -- no syntax errors (may fail on missing deps but shouldn't have syntax errors)
</verification>

<success_criteria>
- CLAUDE.md template includes Memory section with exact MCP tool references
- Section ordering: Session Protocol -> Memory -> Code Style
- Re-running wizard on a project produces CLAUDE.md with the Memory section
</success_criteria>

<output>
After completion, create `.planning/phases/42-living-memory/42-04-SUMMARY.md`
</output>
