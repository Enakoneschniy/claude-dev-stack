---
phase: 31-skills-to-hooks-migration
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - SKL-01
  - SKL-02
  - SKL-03
  - SKL-04
response_language: ru

must_haves:
  truths:
    - ".planning/REQUIREMENTS.md contains a new top-level section '### Skills→Hooks (SKL)' with 4 v1 requirement entries (SKL-01..SKL-04) following the same checklist + bold-ID + description format used for BUG/LIMIT/DX/UX/WF sections"
    - "SKL-01 entry describes: dev-router skill replaced by UserPromptSubmit hook (regex routing → additionalContext)"
    - "SKL-02 entry describes: session-manager start-path migrated to SessionStart hook; skill retains /end + /handoff + /resume only"
    - "SKL-03 entry describes: project-switcher skill replaced by UserPromptSubmit hook (regex against vault/project-map.json)"
    - "SKL-04 entry describes: git-conventions enforcement replaced by PreToolUse Bash(git commit*) hook (warn-only default, strict mode opt-in via .planning/config.json)"
    - ".planning/REQUIREMENTS.md Traceability table gains 4 new rows mapping SKL-01..SKL-04 to Phase 31 with status 'pending' (per existing convention — flips to 'complete' on phase summary, see Phase 19 precedent)"
    - "Existing requirements (BUG-01..07, LIMIT-01..04, DX-07..13, UX-01..07, WF-01) are NOT modified"
    - "REQUIREMENTS.md remains valid markdown (no broken tables, no orphaned headings)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "D-20 — SKL-01..04 backfill in new section + Traceability rows"
      contains: "Skills→Hooks (SKL)"
  key_links:
    - from: ".planning/REQUIREMENTS.md '### Skills→Hooks (SKL)' section"
      to: ".planning/REQUIREMENTS.md Traceability table"
      via: "4 new rows: | SKL-0N | 31 | — | pending |"
      pattern: "SKL-01"
---

<objective>
Backfill the 4 new Skills→Hooks requirements (SKL-01..04) into REQUIREMENTS.md. This
plan is a pure documentation edit — no code change. It is **wave 1** because it
neither produces nor consumes any code artifact from other Plan 01/02/03 work; it
captures the requirement intent that those plans implement.

Per CONTEXT D-20, add a new section `### Skills→Hooks (SKL)` containing 4 v1 requirements
in the same checklist format the file already uses for BUG/LIMIT/DX/UX/WF sections,
and add 4 corresponding rows to the Traceability table at the bottom of the file.

Why wave 1 (parallel with Plan 01 and Plan 03):
- Touches only `.planning/REQUIREMENTS.md` — no overlap with Plan 01 (hooks/, tests/),
  Plan 02 (lib/install/, tests/), or Plan 03 (skills/session-manager/, vault/).
- Captures the requirements that the other plans implement; useful to land FIRST so
  later plan summaries can reference SKL-XX status flips.

Per quality gate: "SKL-01..04 in plan requirements frontmatter" — every plan in this
phase already lists its SKL-XX in `requirements:`. This plan's role is to make those
IDs resolvable to actual REQUIREMENTS.md entries.

Output:
- New `### Skills→Hooks (SKL)` section added before Future Requirements
- 4 new Traceability table rows
- One commit
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-skills-to-hooks-migration/31-CONTEXT.md

<interfaces>
<!-- Exact text to insert (use Edit tool — surgical, do not rewrite the whole file). -->

Insertion point #1: AFTER the `### Wizard UX Polish (UX)` section (the UX-07 line is
the last bullet) and BEFORE the `---` separator that precedes `## Future Requirements`.

Block to insert:

```markdown
### Skills→Hooks (SKL)

- [ ] **SKL-01**: `dev-router` skill replaced by `hooks/dev-router.mjs` UserPromptSubmit hook. Hook reads prompt from stdin JSON, regex-matches dev/research/session/end keywords, emits a routing hint as `additionalContext` (≤200 chars). Fail-silent on empty stdin / malformed JSON. Skill file removed from `skills/` and from `lib/install/skills.mjs` skillNames; deprecated install at `~/.claude/skills/dev-router/` cleaned up by wizard re-run.
- [ ] **SKL-02**: `session-manager` skill start-path (auto-load context.md + last sessions on first message) fully migrated to SessionStart hook (`hooks/session-start-context.sh`, owned by Phase 28). Phase 31 removes the `### /resume or /start` section and `## Automatic Behavior` block from the skill body. Skill retains `/end` (session log + ADR), `/handoff`, `/status`, `## ADR Creation`, `## Best Practices`. New note line at top of skill body directs readers to the hook.
- [ ] **SKL-03**: `project-switcher` skill replaced by `hooks/project-switcher.mjs` UserPromptSubmit hook. Hook parses project names from `vault/project-map.json` (NOT project-registry.md — JSON is reliable), uses word-boundary regex to match prompt against known projects, emits switch hint only when matched project differs from current cwd's project. Fail-silent when registry absent. Skill file removed from `skills/` and skillNames; deprecated install cleaned up by wizard.
- [ ] **SKL-04**: `git-conventions` enforcement migrated to `hooks/git-conventions-check.mjs` PreToolUse hook with `matcher: "Bash"` and per-hook `if: "Bash(git commit*)"` scope-narrowing. Hook validates the `-m "..."` message against conventional commits regex `/^(feat|fix|chore|docs|refactor|test|ci|build|perf|style|revert)(\(.+\))?!?:\s.+/`. Default mode: warn-only (exit 0 with stdout suggestion). Strict mode (exit 2 blocking) opt-in via `.planning/config.json` → `workflow.commit_validation: "strict"`. Coexists with GSD's `gsd-validate-commit.sh` (per research finding #3 — GSD is opt-in via community flag).
```

Insertion point #2: APPEND 4 rows to the Traceability table at the bottom of the file
(after the existing `| WF-01 | 29 | — | pending |` row, BEFORE `| BUG-07 | 30 | — | pending |`
if BUG-07 is the last row, or simply append after the last row — preserve the existing
final-row order).

Rows to add:

```markdown
| SKL-01 | 31 | — | pending |
| SKL-02 | 31 | — | pending |
| SKL-03 | 31 | — | pending |
| SKL-04 | 31 | — | pending |
```

(Plan column will be filled to `31-01`/`31-02`/`31-03` by the phase summary process —
existing convention per Phase 19/20 precedent leaves Plan column as `—` until summary.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append SKL section + 4 Traceability rows to REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (whole file — confirm exact line numbers of insertion points before editing; the file may have grown since CONTEXT was gathered)
  </read_first>
  <action>
    1. Read .planning/REQUIREMENTS.md to confirm:
       - The line containing the last UX bullet (currently `- [ ] **UX-07**:`)
       - The `---` separator line that precedes `## Future Requirements`
       - The last row of the Traceability table

    2. Use the Edit tool to make TWO surgical insertions:

       Insertion A — between the last UX bullet and the `---` separator:
       Insert the `### Skills→Hooks (SKL)` block from the interfaces section above
       (preserving a blank line before and after for readability).

       Insertion B — append to the Traceability table:
       Insert the 4 new rows at the end of the table (after the last existing row).

    3. Verify with grep:
       ```bash
       grep -c "### Skills→Hooks (SKL)" .planning/REQUIREMENTS.md          # 1
       grep -c "\*\*SKL-01\*\*" .planning/REQUIREMENTS.md                   # 1
       grep -c "\*\*SKL-02\*\*" .planning/REQUIREMENTS.md                   # 1
       grep -c "\*\*SKL-03\*\*" .planning/REQUIREMENTS.md                   # 1
       grep -c "\*\*SKL-04\*\*" .planning/REQUIREMENTS.md                   # 1
       grep -c "| SKL-01 | 31 |" .planning/REQUIREMENTS.md                  # 1
       grep -c "| SKL-04 | 31 |" .planning/REQUIREMENTS.md                  # 1
       ```

    4. Verify markdown structure is intact (no broken tables, no extra blank rows in
       the table). Inspect manually with `head -200 .planning/REQUIREMENTS.md` and
       `tail -40 .planning/REQUIREMENTS.md`.

    Commit: `docs(31): backfill SKL-01..04 requirements + Traceability (D-20)`
  </action>
  <acceptance_criteria>
    - `grep -c "### Skills→Hooks (SKL)" .planning/REQUIREMENTS.md` == 1
    - `grep -c "\*\*SKL-0[1-4]\*\*" .planning/REQUIREMENTS.md` == 4 (all 4 IDs present as bold IDs)
    - `grep -cE "^\| SKL-0[1-4] \| 31 \|" .planning/REQUIREMENTS.md` == 4 (all 4 traceability rows present)
    - `grep -c "## Future Requirements" .planning/REQUIREMENTS.md` == 1 (existing section preserved)
    - `grep -c "\*\*BUG-01\*\*" .planning/REQUIREMENTS.md` == 1 (existing requirements untouched)
    - `grep -c "\*\*WF-01\*\*" .planning/REQUIREMENTS.md` == 1 (existing requirements untouched)
    - File is syntactically valid markdown (manual inspection: no orphaned `|` rows, no broken headings)
  </acceptance_criteria>
  <verify>
    <automated>grep -c "### Skills→Hooks (SKL)" .planning/REQUIREMENTS.md | grep -q "^1$" && grep -cE "^\| SKL-0[1-4] \| 31 \|" .planning/REQUIREMENTS.md | grep -q "^4$"</automated>
  </verify>
  <done>
    REQUIREMENTS.md contains the new SKL section with 4 well-formed entries and 4
    corresponding Traceability rows. All other content unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo edit → .planning/REQUIREMENTS.md | Local doc edit; no executable surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-17 | Tampering (accidental overwrite of existing requirements) | REQUIREMENTS.md edit | mitigate | Task explicitly uses Edit tool (surgical insertion) and includes acceptance criteria asserting that BUG-01 and WF-01 still appear exactly once. Read-first step confirms current line ranges before editing |
| T-31-18 | Information Disclosure | requirement description | accept | Document is project-internal planning content already committed to git. SKL descriptions reveal hook implementation details — same as existing BUG/LIMIT entries. No secret material |
</threat_model>

<verification>
```bash
# Section + entries
grep -A1 "### Skills→Hooks (SKL)" .planning/REQUIREMENTS.md | head -5
grep "\*\*SKL-0" .planning/REQUIREMENTS.md

# Traceability
grep -E "^\| SKL-0[1-4] \| 31 \|" .planning/REQUIREMENTS.md

# Regression: existing entries intact
grep -c "\*\*BUG-01\*\*" .planning/REQUIREMENTS.md
grep -c "\*\*WF-01\*\*" .planning/REQUIREMENTS.md
grep -c "## Future Requirements" .planning/REQUIREMENTS.md

# Markdown structure
head -200 .planning/REQUIREMENTS.md | tail -50
tail -30 .planning/REQUIREMENTS.md
```
</verification>

<success_criteria>
- `### Skills→Hooks (SKL)` section exists with 4 well-formed checklist entries
- Traceability table has 4 new rows (SKL-01..04 → Phase 31, status pending)
- All existing requirements (BUG/LIMIT/DX/UX/WF) untouched
- One commit on phase branch
</success_criteria>

<output>
After completion, create `.planning/phases/31-skills-to-hooks-migration/31-04-SUMMARY.md`
covering: insertion line numbers, exact SKL section text inserted, 4 new traceability
rows, decisions implemented (D-20).
</output>
