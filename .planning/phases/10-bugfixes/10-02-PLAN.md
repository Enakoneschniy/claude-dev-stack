---
phase: 10-bugfixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/shared.mjs
  - lib/git-conventions.mjs
  - lib/git-scopes.mjs
  - bin/install.mjs
  - tests/helpers/fixtures.mjs
  - tests/shared.test.mjs
  - tests/git-conventions.test.mjs
  - tests/git-scopes.test.mjs
  - tests/install.test.mjs
  - tests/helpers/fixtures.test.mjs
autonomous: true
requirements:
  - FIX-03

must_haves:
  truths:
    - "hasCommand uses spawnSync with array args, not shell string interpolation"
    - "git-conventions --full mode runs all prompts 1-7 without duplicating the main branch prompt"
    - "Go multi-module detector in git-scopes skips node_modules, vendor, and .git directories"
    - "installSessionHook warns and aborts on corrupt settings.json instead of silently overwriting"
    - "withStubBinary properly handles async callback functions"
  artifacts:
    - path: "lib/shared.mjs"
      provides: "Safe hasCommand using spawnSync"
      contains: "spawnSync"
    - path: "lib/git-conventions.mjs"
      provides: "Fixed quick/full prompt flow"
    - path: "lib/git-scopes.mjs"
      provides: "Go detector with directory filter"
      contains: "node_modules"
    - path: "bin/install.mjs"
      provides: "installSessionHook with JSON parse error handling"
      contains: "warn"
    - path: "tests/helpers/fixtures.mjs"
      provides: "Async-safe withStubBinary"
      contains: "async"
  key_links:
    - from: "lib/shared.mjs"
      to: "all modules calling hasCommand"
      via: "exported function"
      pattern: "spawnSync.*which"
    - from: "tests/helpers/fixtures.mjs"
      to: "all test files using withStubBinary"
      via: "exported helper"
      pattern: "await.*withStubBinary"
---

<objective>
Fix 5 code review warnings from Phase 6 review (WR-01 through WR-05): shell injection in hasCommand, prompt duplication in git-conventions --full mode, performance issue in Go scope detector, silent error swallowing in installSessionHook, and sync/async mismatch in withStubBinary test helper.

Purpose: Eliminate security, correctness, and performance issues flagged in code review.
Output: 5 patched source files with corresponding test updates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@lib/shared.mjs
@lib/git-conventions.mjs
@lib/git-scopes.mjs
@bin/install.mjs
@tests/helpers/fixtures.mjs
@tests/shared.test.mjs
@tests/git-conventions.test.mjs
@tests/git-scopes.test.mjs
@tests/install.test.mjs
@tests/helpers/fixtures.test.mjs

<interfaces>
<!-- Key contracts the executor needs -->

From lib/shared.mjs (current hasCommand at line 48-49):
The current implementation uses shell string interpolation: `which ${name}`.
This must be replaced with spawnSync('which', [name]) using array args to prevent
shell metacharacter interpretation.

The file imports execSync from node:child_process. spawnSync must be added to the import.

From lib/git-conventions.mjs cmdInit (line 116):
```javascript
async function cmdInit({ quick = false, full = false } = {}) {
```
Line 133: `const useQuick = quick || !full;`
Line 134: `if (useQuick) { /* prompts 1-4: name, scopes, main branch, commitlint */ }`
Line 194: `if (full) { /* prompts 5-7: ticket prefix, branch format, co-authored-by */ }`
Bug: --full runs both blocks. Fix: always run prompts 1-4, conditionally add 5-7.

From lib/git-scopes.mjs Go detector (lines 230-255):
Top-level readdirSync has no filter for node_modules, vendor, .git.
These dirs can be huge and cause performance issues on sync readdirSync.

From bin/install.mjs installSessionHook (lines 1117-1121):
Empty catch block silently swallows JSON parse errors on settings.json.
Fix: warn user and return early (abort hook installation).

From tests/helpers/fixtures.mjs withStubBinary (lines 100-117):
Sync function calls fn(dir) which may be async. The finally block runs
immediately, removing the stub binary before the async fn finishes.
Fix: make withStubBinary async and await fn(dir).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix WR-01 (hasCommand shell safety) and WR-05 (withStubBinary async)</name>
  <files>lib/shared.mjs, tests/helpers/fixtures.mjs, tests/shared.test.mjs, tests/helpers/fixtures.test.mjs</files>
  <read_first>
    - lib/shared.mjs (lines 1-60: imports and hasCommand/runCmd)
    - tests/helpers/fixtures.mjs (lines 99-117: withStubBinary)
    - tests/shared.test.mjs
    - tests/helpers/fixtures.test.mjs
  </read_first>
  <behavior>
    - Test: hasCommand('node') returns true (known binary)
    - Test: hasCommand('definitely-nonexistent-binary-xyz') returns false
    - Test: hasCommand with shell metacharacters in name returns false (not interpreted as shell)
    - Test: withStubBinary with async fn properly awaits, PATH restored AFTER fn completes, stub dir still exists during async fn execution
  </behavior>
  <action>
**WR-01: Fix hasCommand in lib/shared.mjs**

1. Add `spawnSync` to the import from `node:child_process` (alongside existing execSync).

2. Replace hasCommand implementation (line 48-49). Instead of using runCmd with shell string interpolation, use spawnSync with array arguments:
   - Call spawnSync('which', [name]) with encoding 'utf8' and stdio 'pipe'
   - Return true if result.status === 0, false otherwise
   - Wrap in try/catch returning false on any error

This prevents shell metacharacters in `name` from being interpreted. Do NOT change runCmd — it is only used with hardcoded command strings.

**WR-05: Fix withStubBinary in tests/helpers/fixtures.mjs**

1. Change `export function withStubBinary` to `export async function withStubBinary`
2. Change the body to use `return await fn(dir)` in the try block
3. The finally block runs after the awaited promise resolves, so PATH and cleanup happen in correct order

After making withStubBinary async, search all test files for callers:
```bash
grep -rn 'withStubBinary' tests/
```
Ensure every caller uses `await`. If any callers lack await, add it.

Add test for hasCommand in tests/shared.test.mjs verifying it returns boolean for known and unknown commands.
  </action>
  <verify>
    <automated>cd /Users/eugenenakoneschniy/Projects/claude-dev-stack && node --test tests/shared.test.mjs tests/helpers/fixtures.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'spawnSync' lib/shared.mjs confirms spawnSync is used
    - No occurrence of shell string interpolation with 'which' in hasCommand function
    - grep -q 'async function withStubBinary' tests/helpers/fixtures.mjs confirms async
    - grep -q 'await fn' tests/helpers/fixtures.mjs confirms await inside withStubBinary
    - node --test tests/shared.test.mjs passes
    - node --test tests/helpers/fixtures.test.mjs passes
  </acceptance_criteria>
  <done>hasCommand uses spawnSync with array args (no shell injection risk), withStubBinary is async-safe</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix WR-02 (--full double prompt), WR-03 (Go dir filter), WR-04 (settings.json parse)</name>
  <files>lib/git-conventions.mjs, lib/git-scopes.mjs, bin/install.mjs, tests/git-conventions.test.mjs, tests/git-scopes.test.mjs, tests/install.test.mjs</files>
  <read_first>
    - lib/git-conventions.mjs (lines 114-230: cmdInit function with quick/full logic)
    - lib/git-scopes.mjs (lines 229-255: Go multi-module detector)
    - bin/install.mjs (lines 1113-1135: installSessionHook)
    - tests/git-conventions.test.mjs
    - tests/git-scopes.test.mjs
    - tests/install.test.mjs
  </read_first>
  <behavior>
    - Test: git-conventions cmdInit with full=true runs prompts 1-7 (all quick prompts plus full-only prompts), main branch prompt appears exactly once
    - Test: git-scopes detectStack Go detector filters out node_modules, vendor, .git from top-level directory scan
    - Test: installSessionHook with corrupt (non-JSON) settings.json emits a warning and does not proceed to modify hooks
  </behavior>
  <action>
**WR-02: Fix git-conventions.mjs --full mode**

Restructure cmdInit() so prompts 1-4 ALWAYS run, and prompts 5-7 only run when `--full`:

1. Remove the `const useQuick = quick || !full;` line (133)
2. Remove the `if (useQuick) {` wrapper around prompts 1-4 — they always run
3. Keep `if (full) {` wrapper around prompts 5-7
4. The `quick` parameter becomes a no-op (keep in signature for backward compat)

Result: `scopes init` runs prompts 1-4. `scopes init --full` runs prompts 1-4 then 5-7. No duplication.

**WR-03: Fix git-scopes.mjs Go detector**

In the Go multi-module detector (around line 231), add a directory filter. Define a Set of directories to skip:

```javascript
const SKIP_DIRS = new Set(['node_modules', 'vendor', '.git']);
```

Apply to both the top-level scan (line 231-232) and the nested scan (line 242):
- Top-level: `.filter(e => e.isDirectory() && !SKIP_DIRS.has(e.name))`
- Nested: `.filter(e => e.isDirectory() && !SKIP_DIRS.has(e.name))`

Place the SKIP_DIRS constant at module scope or just before the Go detector block.

**WR-04: Fix bin/install.mjs installSessionHook**

In installSessionHook() around line 1118-1121, replace the empty catch block. On JSON parse error:
1. Call warn() with a message explaining settings.json is corrupt
2. Return early (abort hook installation) — do not proceed with an empty settings object

Check if bin/install.mjs has its own warn function or imports from shared.mjs. If it has local equivalents (look for console.log with colored warning output), use those. If it imports from shared.mjs, use warn directly.

Add tests for each fix in the corresponding test files.
  </action>
  <verify>
    <automated>cd /Users/eugenenakoneschniy/Projects/claude-dev-stack && node --test tests/git-conventions.test.mjs tests/git-scopes.test.mjs tests/install.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'SKIP_DIRS' lib/git-scopes.mjs confirms directory filter is present
    - grep -q 'node_modules' lib/git-scopes.mjs confirms node_modules is in the filter
    - The quick/full logic in git-conventions.mjs no longer wraps prompts 1-4 in a conditional (they always run)
    - grep -q 'warn\|corrupt\|skipping' bin/install.mjs confirms error handling in installSessionHook
    - The catch block in installSessionHook is NOT empty
    - node --test tests/git-conventions.test.mjs passes
    - node --test tests/git-scopes.test.mjs passes
    - node --test tests/install.test.mjs passes
  </acceptance_criteria>
  <done>--full mode runs all 7 prompts without duplication, Go detector skips heavy dirs, installSessionHook warns on corrupt settings.json</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hasCommand input | Command name string from config/code — could contain shell metacharacters |
| settings.json | User-controlled file on disk — could be corrupt or maliciously crafted |
| projectDir scan | Directory listing of user project — contains untrusted directory names |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | T (Tampering) | hasCommand shell injection | mitigate | Replace shell string interpolation with spawnSync('which', [name]) array args — no shell interpretation of metacharacters (WR-01) |
| T-10-04 | I (Info Disclosure) | installSessionHook silent overwrite | mitigate | Warn and abort on corrupt settings.json instead of silently proceeding with empty settings object (WR-04) |
| T-10-05 | D (Denial of Service) | Go detector scanning node_modules | mitigate | Filter node_modules/vendor/.git from directory scan to prevent blocking event loop (WR-03) |
| T-10-06 | T (Tampering) | withStubBinary async race | mitigate | Make withStubBinary async so PATH and stub cleanup happen after async fn completes (WR-05) |
</threat_model>

<verification>
1. `npm test` passes (all existing + new tests green)
2. `grep 'spawnSync' lib/shared.mjs` confirms WR-01 fix
3. `grep 'SKIP_DIRS' lib/git-scopes.mjs` confirms WR-03 fix
4. installSessionHook catch block contains warn call (not empty)
5. `grep 'async function withStubBinary' tests/helpers/fixtures.mjs` confirms WR-05 fix
</verification>

<success_criteria>
- WR-01: hasCommand uses spawnSync, no shell string interpolation
- WR-02: --full mode prompts 1-7 exactly once each, no double main-branch prompt
- WR-03: Go detector skips node_modules, vendor, .git
- WR-04: installSessionHook warns and aborts on corrupt settings.json
- WR-05: withStubBinary is async-safe
- All tests pass (`npm test`)
</success_criteria>

<output>
After completion, create `.planning/phases/10-bugfixes/10-02-SUMMARY.md`
</output>
