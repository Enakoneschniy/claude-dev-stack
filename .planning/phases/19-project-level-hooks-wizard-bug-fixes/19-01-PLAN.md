---
phase: 19-project-level-hooks-wizard-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/install.test.mjs
  - tests/hooks.test.mjs
  - lib/install/hooks.mjs
  - hooks/gsd-auto-reapply-patches.sh
autonomous: true
requirements:
  - BUG-01
  - BUG-02
  - BUG-06
response_language: ru

must_haves:
  truths:
    - "After wizard runs, ~/.claude/gsd-local-patches/transition.md exists and is byte-identical to the package-shipped patches/transition.md"
    - "gsd-auto-reapply-patches.sh resolves PATCHES_DIR from ~/.claude/gsd-local-patches first, before npm or dev-location fallbacks"
    - "When GSD transition.md hash differs from ~/.claude/gsd-local-patches/transition.md, SessionStart reapplies the patch and prints 'GSD patches auto-reapplied'"
    - "When no drift exists between GSD and gsd-local-patches, SessionStart is silent (no reapply message)"
    - "installSessionHook writes hooks to project .claude/settings.json (BUG-01) and permissions.allow entries covering vault read/write + safe git bash (BUG-02) — both covered by automated tests"
    - "Full test suite remains green (baseline 716+ tests)"
  artifacts:
    - path: "tests/install.test.mjs"
      provides: "D-07 wizard patch copy coverage"
      contains: "gsd-local-patches"
    - path: "tests/hooks.test.mjs"
      provides: "D-07 hook resolution precedence coverage"
      contains: "gsd-local-patches"
    - path: "lib/install/hooks.mjs"
      provides: "D-07 implementation — cpSync patches → ~/.claude/gsd-local-patches/"
      contains: "gsd-local-patches"
    - path: "hooks/gsd-auto-reapply-patches.sh"
      provides: "D-07 runtime — prefers ~/.claude/gsd-local-patches over npm resolution"
      contains: "gsd-local-patches"
  key_links:
    - from: "lib/install/hooks.mjs installSessionHook()"
      to: "~/.claude/gsd-local-patches/"
      via: "cpSync(patchesSrc, patchesDest, { recursive: true })"
      pattern: "gsd-local-patches"
    - from: "hooks/gsd-auto-reapply-patches.sh"
      to: "~/.claude/gsd-local-patches/transition.md"
      via: "PATCHES_DIR resolution (highest priority)"
      pattern: "HOME/.claude/gsd-local-patches"
    - from: "hooks/session-start-context.sh"
      to: "hooks/gsd-auto-reapply-patches.sh"
      via: "bash $HOOKS_DIR/gsd-auto-reapply-patches.sh (already wired)"
      pattern: "gsd-auto-reapply-patches"
---

<objective>
Close Phase 19 by (a) verifying BUG-01 and BUG-02 acceptance criteria are fully covered
by automated tests (adding functional tests only if a gap is found) and (b) implementing
the last missing piece of BUG-06 — D-07: the install wizard must copy the shipped
`patches/` directory to `~/.claude/gsd-local-patches/`, and
`hooks/gsd-auto-reapply-patches.sh` must prefer that wizard-pinned location over the
runtime npm / dev-location resolution chain.

Purpose:
- BUG-01 and BUG-02: code already lives in `lib/install/hooks.mjs`; we guarantee the
  acceptance criteria in REQUIREMENTS.md are test-covered (add gap-filling functional
  tests only if the audit finds a miss).
- BUG-06 D-07: makes the GSD TeamCreate patch survive `/gsd-update` in a
  version-pinned, deterministic way — no dependence on npm cache layout.

Output:
- 2 new D-07 tests (Wave 1) that fail cleanly before implementation.
- `installSessionHook` copies `patches/` to `~/.claude/gsd-local-patches/` (Wave 2).
- `gsd-auto-reapply-patches.sh` checks `~/.claude/gsd-local-patches/` first (Wave 3).
- BUG-01/BUG-02 acceptance audit (Wave 4) — add functional test(s) only if coverage gap.
- All tests green, one commit per wave.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-project-level-hooks-wizard-bug-fixes/19-CONTEXT.md
@.planning/phases/19-project-level-hooks-wizard-bug-fixes/19-RESEARCH.md
@.planning/phases/19-project-level-hooks-wizard-bug-fixes/19-VALIDATION.md

@lib/install/hooks.mjs
@hooks/gsd-auto-reapply-patches.sh
@hooks/session-start-context.sh
@tests/install.test.mjs
@tests/hooks.test.mjs

<interfaces>
<!-- Contracts the executor must honor. Extracted from verified source files. -->

From lib/install/hooks.mjs:
```javascript
// Current exported signature — DO NOT change
export function installSessionHook(stepNum, totalSteps, pkgRoot, vaultPath, projectsData)

// Internal helper
function _writeSettingsFile(settingsPath, startDest, endDest, pushDest, vaultPath)

// Existing npm imports already in file (reuse — NO new deps):
import { existsSync, readFileSync, writeFileSync, cpSync, chmodSync } from 'fs';
import { join } from 'path';
import { homedir } from 'os';
import { c, ok, warn, info, step, mkdirp } from '../shared.mjs';
```

From hooks/gsd-auto-reapply-patches.sh (current PATCHES_DIR resolution order):
```
1. $PATCHES_DIR env var (tests/dev override) — kept as highest priority
2. $(npm root -g)/claude-dev-stack/patches
3. $HOME/.npm/_npx/*/node_modules/claude-dev-stack/patches
4. $HOME/.local/share/npm/lib/node_modules/claude-dev-stack/patches
5. $HOME/Projects|projects|code/claude-dev-stack/patches (dev checkouts)
```

New desired order (D-07):
```
1. $PATCHES_DIR env var (unchanged — must still win for tests)
2. $HOME/.claude/gsd-local-patches  <-- NEW: wizard-pinned, version-authoritative
3. npm global / npx cache / dev checkouts (existing fallbacks)
```

From hooks/session-start-context.sh (already wired — do NOT modify):
```bash
HOOKS_DIR="$(cd "$(dirname "$0")" && pwd)"
if [ -f "$HOOKS_DIR/gsd-auto-reapply-patches.sh" ]; then
  bash "$HOOKS_DIR/gsd-auto-reapply-patches.sh" 2>/dev/null || true
fi
```

From patches/transition.md (package-shipped source of truth — already in npm `files`):
- Exists at pkgRoot/patches/transition.md
- Contains TeamCreate parallel execution content
- Verified by existing test: "patches/transition.md contains TeamCreate parallel execution content"
</interfaces>
</context>

<tasks>

<!-- ──────────────────────────────────────────────────────────── -->
<!-- WAVE 1 — Wave 0 tests (TDD RED): write failing tests first.  -->
<!-- ──────────────────────────────────────────────────────────── -->

<task type="auto" tdd="true">
  <name>Task 1 (Wave 1): Add D-07 wizard copy test to tests/install.test.mjs</name>
  <files>tests/install.test.mjs</files>
  <behavior>
    Add ONE new test block under the existing BUG-06 coverage section. The test MUST:
    - Create a temp HOME (mkdtempSync) and a fake pkgRoot containing `patches/transition.md`
      with distinctive content (e.g. "TEST_PATCH_MARKER_{nonce}").
    - Create a minimal projectsData with one project path that exists on disk.
    - Call `installSessionHook(1, 1, pkgRoot, vaultPath, projectsData)` with HOME
      redirected (set `process.env.HOME` to the temp dir; restore in finally).
    - Assert `~/.claude/gsd-local-patches/transition.md` exists after the call.
    - Assert its content equals the fake patch content byte-for-byte (readFileSync + equal).
    - Assert that calling `installSessionHook` a second time is idempotent — file still
      exists, still matches source. No throw.
    - Clean up: restore HOME, rmSync temp dir with { recursive: true, force: true }.

    Naming: `installSessionHook copies patches/ to ~/.claude/gsd-local-patches/ (BUG-06 D-07)`.
    Follow the existing test style in install.test.mjs (`test(...)` from `node:test`,
    `assert` from `node:assert/strict`).
  </behavior>
  <action>
    Implement the test described in <behavior>. Do NOT modify `lib/install/hooks.mjs`
    in this task — the test MUST fail against current code (RED). Reuse the fixture
    helpers already present at the top of `tests/install.test.mjs` (look for existing
    mkdtempSync / tmpdir / HOME-override patterns; mirror them). Do NOT add any new
    npm dependency. Keep the test self-contained (no shared state with other tests).
  </action>
  <verify>
    <automated>node --test tests/install.test.mjs 2>&amp;1 | grep -E "(BUG-06 D-07|gsd-local-patches)" | head -5</automated>
  </verify>
  <done>
    New test exists in tests/install.test.mjs with name containing "BUG-06 D-07" and
    "gsd-local-patches". Running `node --test tests/install.test.mjs` shows this one
    new test FAILING (expected — TDD RED); all previously-green tests remain green.
    No other test names changed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (Wave 1): Add D-07 hook precedence test to tests/hooks.test.mjs</name>
  <files>tests/hooks.test.mjs</files>
  <behavior>
    Add ONE new test that verifies `hooks/gsd-auto-reapply-patches.sh` prefers
    `~/.claude/gsd-local-patches/` over the runtime npm / dev-location chain. The test MUST:
    - Create a temp HOME and a temp GSD_DIR with `workflows/transition.md` containing
      OLD content (e.g. "OLD_UNPATCHED").
    - Create `$HOME/.claude/gsd-local-patches/transition.md` containing NEW content
      (e.g. "NEW_WIZARD_PINNED_{nonce}").
    - Also create a DIFFERENT patches dir (e.g. `$HOME/.npm/_npx/fake/node_modules/claude-dev-stack/patches/transition.md`)
      with a THIRD, distinct content (e.g. "NPM_CACHE_CONTENT") to prove precedence.
    - Invoke the script via `execFileSync('bash', [scriptPath])` with env:
      `HOME=<temp>`, `GSD_DIR=<temp GSD>`, `PATCHES_DIR=""` (unset to force resolution).
    - Assert stdout contains "GSD patches auto-reapplied".
    - Assert `<temp GSD>/workflows/transition.md` now equals the NEW wizard-pinned
      content (proves gsd-local-patches won over the npm cache candidate).
    - Clean up temp dirs.

    Naming: `gsd-auto-reapply-patches.sh prefers ~/.claude/gsd-local-patches over npm resolution (BUG-06 D-07)`.
    Follow existing test style in tests/hooks.test.mjs.
  </behavior>
  <action>
    Implement the test described in <behavior>. Do NOT modify
    `hooks/gsd-auto-reapply-patches.sh` yet — the test MUST fail against current code
    because the script's resolution chain does not yet check `~/.claude/gsd-local-patches`.
    If the existing hooks.test.mjs already uses a helper for setting up a fake GSD
    directory, reuse it. Use `execFileSync` from `node:child_process` with
    `{ encoding: 'utf8', env: { ...process.env, HOME, GSD_DIR, PATCHES_DIR: '' } }`.
  </action>
  <verify>
    <automated>node --test tests/hooks.test.mjs 2>&amp;1 | grep -E "(BUG-06 D-07|gsd-local-patches)" | head -5</automated>
  </verify>
  <done>
    New test exists in tests/hooks.test.mjs with name containing "BUG-06 D-07" and
    "gsd-local-patches". Running `node --test tests/hooks.test.mjs` shows this one
    new test FAILING (expected — TDD RED); all previously-green tests remain green.
  </done>
</task>

<!-- Wave 1 gate: commit the two failing tests. Per planning constraints, commit per wave. -->
<task type="auto">
  <name>Task 3 (Wave 1 gate): Run full suite + commit Wave 1</name>
  <files>(git commit only — no file edits in this task)</files>
  <action>
    Run `node --test tests/install.test.mjs tests/hooks.test.mjs` and confirm exactly
    2 new failures (Task 1 + Task 2) with the expected test names. All other tests must
    still pass. Then commit both test files with a conventional commit message on the
    current branch (branching_strategy="phase", so branch is already
    `gsd/phase-19-project-level-hooks-wizard-bug-fixes`).

    Commit message:
    ```
    test(19): add D-07 wizard copy + hook precedence tests (RED)

    BUG-06 D-07: wizard must copy patches/ to ~/.claude/gsd-local-patches/
    and gsd-auto-reapply-patches.sh must prefer that location over npm
    resolution. Tests fail until Wave 2/3 implementation lands.
    ```

    Do NOT include "Co-Authored-By" (per MEMORY.md feedback_no_coauthor).
  </action>
  <verify>
    <automated>git log -1 --format=%s | grep -q "test(19): add D-07"</automated>
  </verify>
  <done>
    Two new RED tests committed on branch gsd/phase-19-project-level-hooks-wizard-bug-fixes.
    `git status` shows a clean tree. No other files touched.
  </done>
</task>

<!-- ──────────────────────────────────────────────────────────── -->
<!-- WAVE 2 — Implement D-07 wizard side (GREEN for Task 1).     -->
<!-- ──────────────────────────────────────────────────────────── -->

<task type="auto" tdd="true">
  <name>Task 4 (Wave 2): Implement D-07 patch copy in lib/install/hooks.mjs</name>
  <files>lib/install/hooks.mjs</files>
  <behavior>
    `installSessionHook` MUST, after `mkdirp(hooksDir)` and before the projects loop,
    copy `${pkgRoot}/patches/` (if it exists) to `${homedir()}/.claude/gsd-local-patches/`
    recursively. Copy is unconditional (overwrites) — patches are small and
    version-pinned; per RESEARCH.md Open Question #2 we use unconditional cpSync for
    simplicity. Silent (no ok/info message — matches RESEARCH recommendation).

    Idempotency: a second call with the same pkgRoot leaves the destination in the
    same state (cpSync with recursive:true handles this natively).

    Must NOT be called when `patches/` does not exist in pkgRoot (guard with existsSync).
  </behavior>
  <action>
    In `lib/install/hooks.mjs`, inside `installSessionHook`:

    After the `mkdirp(hooksDir);` line (line ~14) and BEFORE the existing hook-script
    copy loop, add:

    ```javascript
    // BUG-06 D-07: Copy shipped patches/ to ~/.claude/gsd-local-patches/ so
    // gsd-auto-reapply-patches.sh has a version-pinned, wizard-authoritative source
    // that survives /gsd-update and does not depend on npm cache layout.
    const patchesSrc = join(pkgRoot, 'patches');
    const patchesDest = join(homedir(), '.claude', 'gsd-local-patches');
    if (existsSync(patchesSrc)) {
      mkdirp(patchesDest);
      cpSync(patchesSrc, patchesDest, { recursive: true });
    }
    ```

    All imports needed (`existsSync`, `cpSync`, `join`, `homedir`, `mkdirp`) are
    already at the top of the file — do NOT duplicate imports. Do NOT touch the
    fallback-to-global branch or the projects loop logic.

    Run the Wave 1 test to confirm it now PASSES (GREEN).
  </action>
  <verify>
    <automated>node --test tests/install.test.mjs</automated>
  </verify>
  <done>
    The "BUG-06 D-07 ... gsd-local-patches" test in install.test.mjs passes. All other
    install.test.mjs tests still pass. `lib/install/hooks.mjs` diff shows ONLY the 6-ish
    new lines inside installSessionHook — no unrelated edits.
  </done>
</task>

<task type="auto">
  <name>Task 5 (Wave 2 gate): Full suite + commit Wave 2</name>
  <files>(git commit only)</files>
  <action>
    Run `node --test` (full suite). All 716+ tests must pass, plus the install.test.mjs
    D-07 test now green. The hooks.test.mjs D-07 test stays RED (Wave 3 fixes it).

    Commit with message:
    ```
    feat(19): installSessionHook copies patches to gsd-local-patches (D-07)

    BUG-06 D-07: wizard now copies pkgRoot/patches/ to
    ~/.claude/gsd-local-patches/ so auto-reapply has a stable, version-pinned
    source independent of npm cache layout.
    ```
  </action>
  <verify>
    <automated>node --test tests/install.test.mjs 2>&amp;1 | tail -5 &amp;&amp; git log -1 --format=%s | grep -q "feat(19): installSessionHook"</automated>
  </verify>
  <done>
    install.test.mjs fully green. hooks.test.mjs D-07 test still RED (expected).
    Commit is on the phase branch with the exact subject line above.
  </done>
</task>

<!-- ──────────────────────────────────────────────────────────── -->
<!-- WAVE 3 — Implement D-07 hook side (GREEN for Task 2).        -->
<!-- ──────────────────────────────────────────────────────────── -->

<task type="auto" tdd="true">
  <name>Task 6 (Wave 3): Prefer ~/.claude/gsd-local-patches/ in gsd-auto-reapply-patches.sh</name>
  <files>hooks/gsd-auto-reapply-patches.sh</files>
  <behavior>
    After the existing `$PATCHES_DIR` env-var check (lines ~13–21) and BEFORE the
    npm/dev resolution loops, add a check for `$HOME/.claude/gsd-local-patches`. If
    it exists and is a directory, set PATCHES_DIR to it and skip further resolution.

    The env-var override MUST still win (so tests that set `PATCHES_DIR=/tmp/foo`
    keep working). Order after change:
      1. PATCHES_DIR env var (if valid dir)
      2. $HOME/.claude/gsd-local-patches   <-- NEW
      3. npm global / npx cache / .local/share
      4. dev checkouts (~/Projects, etc.)

    All existing behavior preserved: silent on no-op, prints "GSD patches
    auto-reapplied (N file(s) updated)" only when APPLIED > 0, shasum-based
    comparison unchanged.
  </behavior>
  <action>
    Edit `hooks/gsd-auto-reapply-patches.sh`. After the existing env-var block
    (the `if [ -z "${PATCHES_DIR:-}" ]; then PATCHES_DIR=""; else ... fi`
    structure ending around line 21) and BEFORE the "Locate the claude-dev-stack
    package patches/ directory" comment, insert:

    ```bash
    # BUG-06 D-07: Prefer wizard-pinned ~/.claude/gsd-local-patches before any
    # runtime resolution. This is the authoritative copy written by the install
    # wizard and is version-pinned to the installed claude-dev-stack.
    if [ -z "$PATCHES_DIR" ] && [ -d "$HOME/.claude/gsd-local-patches" ]; then
      PATCHES_DIR="$HOME/.claude/gsd-local-patches"
    fi
    ```

    Do NOT remove or reorder the existing npm/dev fallbacks — they remain as
    last-resort resolution for edge cases (fresh clone before first wizard run).

    Preserve the shebang, comments, shasum logic, and exit-0-silent-on-no-GSD guard.
  </action>
  <verify>
    <automated>node --test tests/hooks.test.mjs</automated>
  </verify>
  <done>
    The "BUG-06 D-07 ... prefers ~/.claude/gsd-local-patches" test in hooks.test.mjs
    passes. All other hooks.test.mjs tests still pass.
    `bash -n hooks/gsd-auto-reapply-patches.sh` prints nothing (valid bash syntax).
    Env-var override path still works (verified by the pre-existing test
    "gsd-auto-reapply-patches.sh prints reapply message when patch differs").
  </done>
</task>

<task type="auto">
  <name>Task 7 (Wave 3 gate): Full suite + commit Wave 3</name>
  <files>(git commit only)</files>
  <action>
    Run `node --test` (full suite). All tests, including both new D-07 tests, must pass.

    Commit with message:
    ```
    feat(19): gsd-auto-reapply-patches prefers gsd-local-patches (D-07)

    BUG-06 D-07: runtime hook now checks ~/.claude/gsd-local-patches before
    falling back to npm / dev-location resolution. Makes patch reapply
    deterministic and version-pinned to the installed wizard run.
    ```
  </action>
  <verify>
    <automated>node --test 2>&amp;1 | tail -3 &amp;&amp; git log -1 --format=%s | grep -q "feat(19): gsd-auto-reapply-patches"</automated>
  </verify>
  <done>
    Full suite green. Commit on phase branch. BUG-06 D-07 fully closed end-to-end
    (wizard writes → hook reads).
  </done>
</task>

<!-- ──────────────────────────────────────────────────────────── -->
<!-- WAVE 4 — BUG-01 / BUG-02 acceptance audit.                   -->
<!-- ──────────────────────────────────────────────────────────── -->

<task type="auto">
  <name>Task 8 (Wave 4): Audit BUG-01/BUG-02 acceptance criteria vs existing tests</name>
  <files>tests/install.test.mjs (only if gap found — see action)</files>
  <action>
    Audit the BUG-01 and BUG-02 acceptance criteria from REQUIREMENTS.md against
    existing tests in tests/install.test.mjs. Produce a mental checklist:

    BUG-01 criteria:
    (a) Wizard writes SessionStart + Stop hooks to project `.claude/settings.json`
        → existing test: "writes to project .claude/settings.json not global settings (BUG-01)"
    (b) Hooks only run for projects configured via claude-dev-stack
        → implied by (a); global settings is not written to unless projects.length===0
    (c) Existing global hooks preserved (untouched)
        → needs verification: is there a test that calls installSessionHook and asserts
          ~/.claude/settings.json is NOT modified when projects are provided?

    BUG-02 criteria:
    (a) permissions.allow includes vault read/write patterns
        → existing test: "writes permissions.allow with vault patterns (BUG-02)"
    (b) permissions.allow includes safe bash (git status, git branch -d, git remote prune)
        → existing test: "writes safe bash permissions.allow entries (BUG-02)"
    (c) Persist across sessions (idempotent — not overwritten by reruns)
        → needs verification: is idempotency tested (call installSessionHook twice,
          assert permissions.allow has no duplicates)?

    Process:
    1. Grep tests/install.test.mjs for BUG-01 and BUG-02 test names.
    2. For each acceptance criterion above, confirm at least one test covers it.
    3. If (1c) "global settings untouched when projects present" is NOT covered →
       add ONE functional test:
       `installSessionHook does NOT write to ~/.claude/settings.json when projects provided (BUG-01)`
       Behavior: temp HOME, create pre-existing ~/.claude/settings.json with marker
       content ({ "preserved": true }). Call installSessionHook with a valid project.
       Assert ~/.claude/settings.json content is byte-identical to the marker after the call.
    4. If (2c) idempotency for permissions.allow is NOT covered → add ONE test:
       `installSessionHook permissions.allow is idempotent across reruns (BUG-02)`
       Behavior: call installSessionHook twice with same project; assert
       `permissions.allow.filter(p => p === 'Bash(git status)').length === 1`.
    5. If both are already covered → make NO code change; note coverage in the
       commit message in Task 9.

    IMPORTANT: do NOT add tests that duplicate existing coverage. If in doubt,
    read the existing test body before adding.
  </action>
  <verify>
    <automated>node --test tests/install.test.mjs</automated>
  </verify>
  <done>
    Either (a) audit confirms all BUG-01/BUG-02 acceptance criteria are test-covered
    and NO new tests were added, or (b) one or two targeted functional tests were
    added and they pass. Full install.test.mjs suite green. A brief audit note is
    ready for the Wave 4 commit message.
  </done>
</task>

<task type="auto">
  <name>Task 9 (Wave 4 gate): Final full suite + commit Wave 4 + update ROADMAP</name>
  <files>.planning/ROADMAP.md (plan list only), git commit</files>
  <action>
    1. Run `node --test` — full suite must be green (716+ plus any Wave 4 additions).
    2. Update `.planning/ROADMAP.md` Phase 19 "Plans" section to reflect this PLAN:
       Under "### Phase 19: Project-Level Hooks & Wizard Bug Fixes", replace
       "**Plans**: TBD" (or similar) with:
       ```
       **Plans**: 1 plan
       Plans:
       - [ ] 19-01-PLAN.md — BUG-06 D-07 (wizard copy + hook precedence) + BUG-01/02 audit
       ```
    3. Commit:
       - If Task 8 added functional tests:
         ```
         test(19): close BUG-01/02 acceptance audit gaps

         Add functional tests for (BUG-01) global settings untouched when
         projects provided and/or (BUG-02) permissions.allow idempotency.
         Phase 19 requirements (BUG-01, BUG-02, BUG-06) all covered.
         ```
       - If Task 8 added no tests (coverage already sufficient):
         ```
         docs(19): finalize phase 19 plan — BUG-01/02 coverage audit OK

         Audit confirms existing tests cover BUG-01 (project-level hooks,
         global untouched) and BUG-02 (permissions.allow vault + safe git)
         acceptance criteria. ROADMAP updated with 19-01 plan entry.
         ```
    4. Do NOT include "Co-Authored-By".
  </action>
  <verify>
    <automated>node --test 2>&amp;1 | tail -3 &amp;&amp; git log --oneline -4 | grep -E "(test|feat|docs)\(19\)" | wc -l | tr -d ' ' | grep -qE "^[34]$"</automated>
  </verify>
  <done>
    Full suite green. ROADMAP Phase 19 "Plans" entry points to 19-01-PLAN.md.
    Phase branch has 3 or 4 commits (RED tests, D-07 wizard, D-07 hook, optional audit).
    Phase 19 requirements BUG-01, BUG-02, BUG-06 all satisfied end-to-end.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wizard → ~/.claude/ | Install-time write of hook scripts and settings.json into user HOME |
| SessionStart hook → GSD workflows | Hook overwrites `~/.claude/get-shit-done/workflows/transition.md` when hash differs |
| Package `patches/` → ~/.claude/gsd-local-patches/ | Trusted npm-shipped content copied to user HOME |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering | ~/.claude/gsd-local-patches/transition.md | accept | Directory lives inside user HOME with standard filesystem permissions; attacker with HOME write already has full account compromise. No additional integrity check warranted at this scope. |
| T-19-02 | Elevation of Privilege | gsd-auto-reapply-patches.sh executes `cp` with user permissions | accept | Hook runs as the Claude Code user; cp target (`~/.claude/get-shit-done/workflows/*`) is in user-owned directory. No privilege boundary crossed. |
| T-19-03 | Information Disclosure | permissions.allow patterns (BUG-02) | mitigate | Patterns follow least-privilege per D-05: `Write` restricted to `${vault}/**/sessions/*.md` only, `Bash` restricted to specific subcommands (`git status`, `git branch *`, etc.) — no `Bash(*)` wildcard. Already implemented in `_writeSettingsFile`; audit in Wave 4 confirms no regression. |
| T-19-04 | Denial of Service | Corrupt user `.claude/settings.json` | mitigate | `_writeSettingsFile` already wraps `JSON.parse` in try/catch and returns with `warn()` — hook install is skipped for that project, other projects continue. Preserve this guard (no edits to that block). |
| T-19-05 | Tampering | Malicious `~/.claude/gsd-local-patches/transition.md` replaced by third-party tool | accept | Same trust boundary as T-19-01. If user's HOME is compromised, GSD workflow integrity is already out of scope. Mitigation would require signing, which exceeds phase budget and single-dep constraint. |
</threat_model>

<verification>
Phase-level gates (all must hold before `/gsd-verify-work`):

1. `node --test` — full suite green, including:
   - All pre-existing BUG-01/02/06 tests
   - NEW: `installSessionHook copies patches/ to ~/.claude/gsd-local-patches/ (BUG-06 D-07)`
   - NEW: `gsd-auto-reapply-patches.sh prefers ~/.claude/gsd-local-patches over npm resolution (BUG-06 D-07)`
   - Optional NEW (Wave 4 if gap found): BUG-01 global-untouched and/or BUG-02 idempotency tests
2. `bash -n hooks/gsd-auto-reapply-patches.sh` — valid shell syntax.
3. `git log --oneline gsd/phase-19-project-level-hooks-wizard-bug-fixes` shows
   per-wave commits (RED tests, D-07 wizard impl, D-07 hook impl, audit/docs).
4. `.planning/ROADMAP.md` Phase 19 section lists `19-01-PLAN.md`.
5. Manual verification (captured in 19-VALIDATION.md §Manual-Only):
   - Run `/gsd-update` in a configured project, start a new Claude Code session,
     observe "GSD patches auto-reapplied (1 file(s) updated)" on SessionStart.
     (This is a checkpoint for the user after the plan executes — not a blocking
     automated gate.)
</verification>

<success_criteria>
Phase 19 is complete when:

- [ ] BUG-01 acceptance criteria covered by automated tests (project hooks written,
      global untouched when projects provided).
- [ ] BUG-02 acceptance criteria covered by automated tests (vault + safe git
      permissions.allow entries, idempotent across reruns).
- [ ] BUG-06 D-07 wizard side: `installSessionHook` copies `patches/` to
      `~/.claude/gsd-local-patches/` — test green.
- [ ] BUG-06 D-07 hook side: `gsd-auto-reapply-patches.sh` checks
      `~/.claude/gsd-local-patches/` before npm/dev resolution — test green.
- [ ] Full test suite green (baseline 716+, plus 2–4 new tests).
- [ ] No new npm dependencies added (`package.json` unchanged, single-dep constraint
      preserved).
- [ ] Per-wave commits on branch `gsd/phase-19-project-level-hooks-wizard-bug-fixes`
      using conventional commits, NO Co-Authored-By lines.
- [ ] `.planning/ROADMAP.md` Phase 19 entry references `19-01-PLAN.md`.
- [ ] User-facing progress messages during execution remain in Russian
      (response_language: ru) — code, comments, commit messages in English.
</success_criteria>

<output>
After completion, create:
`.planning/phases/19-project-level-hooks-wizard-bug-fixes/19-01-SUMMARY.md`

Include: what shipped per wave, test delta (new test names + count), files modified,
commit SHAs, and confirmation that BUG-01/BUG-02/BUG-06 requirements are closed.
Flag the manual verification step (SessionStart message after `/gsd-update`) for
the user to spot-check in a real Claude Code session.
</output>
