---
phase: 27-gsd-workflow-customization-via-patches
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - GSD-01
user_setup: []

must_haves:
  truths:
    - "`.planning/REQUIREMENTS.md` contains a new `### GSD Workflow (GSD)` subsection before `## Future Requirements`, with a GSD-01 entry describing the patches mechanism."
    - "The Traceability table includes a new row `| GSD-01 | 27 | 27-01..04 | pending |` (or equivalent) added after the last existing row (ADR-02)."
    - "Header total count is updated to reflect the new requirement."
    - "No existing requirement entries, section ordering, or traceability rows are modified — change is strictly additive."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "GSD-01 requirement + traceability entry"
      contains: "GSD-01"
  key_links:
    - from: ".planning/REQUIREMENTS.md GSD-01 entry"
      to: ".planning/ROADMAP.md Phase 27 Success Criteria (narrowed cut in CONTEXT)"
      via: "formalization scope (patches mechanism)"
      pattern: "GSD-01"
---

<objective>
Backfill GSD-01 into `.planning/REQUIREMENTS.md` so the milestone's canonical
requirements file includes the requirement owned by Phase 27. Mirrors the
ADR-02 / LIMIT-05 backfill precedent established in Phases 25 and 26.

Purpose: Traceability integrity. Plan frontmatter across Phase 27 references
`requirements: [GSD-01]` and that ID must resolve to a real requirement entry.

Scope note: The requirement text reflects the *narrowed formalization cut*
from `27-CONTEXT.md` — the shipped patches mechanism. The extended criteria
from ROADMAP (three-tier resolver, `gsd customize` CLI, diff patches,
config-aware gates) are explicitly deferred to backlog and MUST NOT appear
as success criteria for GSD-01.

Output: Additive edit to `.planning/REQUIREMENTS.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/27-gsd-workflow-customization-via-patches/27-CONTEXT.md

<interfaces>
Current REQUIREMENTS.md structure (from Read):
- v1 Requirements
  - Bug Fixes (BUG) — BUG-01..06
  - Limit Management (LIMIT) — LIMIT-01..04, + BUG-07 (orphan placement, leave alone)
  - Smart Re-install (DX) — DX-07..13
  - Wizard UX Polish (UX) — UX-01..07
  - Decisions (ADR) — ADR-02
- Future Requirements
- Out of Scope
- Traceability table (ends with `ADR-02 | 26 | — | pending`)

Header line 7: `**Total requirements**: 11 v1 requirements (+ 15 DX/UX/WF/BUG-07 backfills + 1 ADR-02 backfill).`

Insert new section `### GSD Workflow (GSD)` AFTER `### Decisions (ADR)` and
BEFORE the `---` separator that precedes `## Future Requirements`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert GSD-01 section and traceability row in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (whole — need exact current structure)
    - .planning/ROADMAP.md Phase 27 block (for goal-language consistency)
    - .planning/phases/27-gsd-workflow-customization-via-patches/27-CONTEXT.md (for narrowed scope)
  </read_first>
  <action>
    1. Open `.planning/REQUIREMENTS.md`.

    2. Locate the end of the `### Decisions (ADR)` section — it is the last
       subsection in `## v1 Requirements`, ending with the line:
       `  5. `claude-dev-stack decisions` CLI lists all decisions for current project with dates and status.`

       Directly after that line (keeping the blank line that precedes `---`),
       insert the new section:

       ```markdown

       ### GSD Workflow (GSD)

       - [ ] **GSD-01**: Projects using claude-dev-stack + GSD can override GSD workflow behavior (e.g., `workflows/manager.md`, `workflows/transition.md`) via package-shipped patches that survive `/gsd-update`. Implementation: shipped `patches/*.md`, install wizard copies to `~/.claude/gsd-local-patches/`, SessionStart hook re-applies any patch whose SHA-256 differs from the upstream workflow file. Formalized scope; extended features (per-project `.planning/gsd-overrides/`, `gsd customize` CLI, diff-based patches, `workflow.auto_push`/`auto_pr`/`merge_strategy` config gates) are deferred to backlog.

         Success Criteria (formalization cut):
         1. Package-shipped `patches/*.md` files replace same-named files under `~/.claude/get-shit-done/workflows/` whenever SHAs differ.
         2. Install wizard copies shipped `patches/` to `~/.claude/gsd-local-patches/` (wizard-pinned, authoritative source).
         3. `gsd-auto-reapply-patches.sh` SessionStart hook resolves patches in order: `$PATCHES_DIR` → `~/.claude/gsd-local-patches/` → npm global → dev checkout.
         4. Hook exits 0 silently when GSD is not installed or no patches source resolves.
         5. Hook is idempotent — re-running on already-patched workflows produces no change and no output.
         6. Regression tests under `tests/` cover hook behavior (`gsd-auto-reapply-patches.test.mjs`) and wizard copy (`install-patches-copy.test.mjs`).
         7. Pattern documented in `vault/shared/patterns.md` so other GSD-using projects can adopt it.
       ```

    3. Update the Traceability table. Locate the last row
       `| ADR-02 | 26 | — | pending |` and append directly after it:

       ```
       | GSD-01 | 27 | 27-01..04 | pending |
       ```

    4. Update header line 7 from
       `**Total requirements**: 11 v1 requirements (+ 15 DX/UX/WF/BUG-07 backfills + 1 ADR-02 backfill).`
       to
       `**Total requirements**: 12 v1 requirements (+ 15 DX/UX/WF/BUG-07 backfills + 1 ADR-02 backfill + 1 GSD-01 backfill).`

    5. Do NOT modify any existing requirement entries, statuses, or rows.
       Strictly additive.

    6. Verify by re-reading the file and confirming:
       - `### GSD Workflow (GSD)` section present
       - `**GSD-01**:` entry contains 7 success criteria
       - Traceability table has `| GSD-01 | 27 | 27-01..04 | pending |` row
       - No existing content deleted or reordered
  </action>
  <verify>
    <automated>grep -q "^### GSD Workflow (GSD)" .planning/REQUIREMENTS.md && grep -q "\*\*GSD-01\*\*" .planning/REQUIREMENTS.md && grep -q "^| GSD-01 | 27 |" .planning/REQUIREMENTS.md && grep -c "^| [A-Z]\+-[0-9]\+ |" .planning/REQUIREMENTS.md | awk '{if ($1 < 26) {print "FAIL: row count too low"; exit 1} else {print "OK:", $1, "rows"}}'</automated>
  </verify>
  <done>
    `.planning/REQUIREMENTS.md` contains the GSD-01 entry with 7 success
    criteria, the Traceability table includes the new row, the header count is
    updated, and no pre-existing content was modified.
    `git diff --stat .planning/REQUIREMENTS.md` shows only additions (other
    than the single header-line count update).
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

None — local planning document edit, no runtime or network exposure.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-06 | Tampering (accidental deletion of existing requirements) | REQUIREMENTS.md edit | mitigate | Use Edit tool with unique anchors; verify row count grep before/after. |

</threat_model>

<verification>
  - `grep -c "^| [A-Z]\+-[0-9]\+ |" .planning/REQUIREMENTS.md` returns one more row than before (was 26, expect 27)
  - `grep "GSD-01" .planning/REQUIREMENTS.md | wc -l` ≥ 2 (one in section, one in table)
  - `git diff --numstat .planning/REQUIREMENTS.md` shows only additions (second column should be 1 for the header-count single-line replacement, or 0 if Edit can do a pure insert)
</verification>

<success_criteria>
  - GSD-01 is discoverable from REQUIREMENTS.md, not just ROADMAP.md and plan frontmatter
  - Traceability preserved: every Phase 27 plan's `requirements: [GSD-01]` frontmatter resolves to a real requirement entry
  - Narrowed scope locked in — extended criteria listed as deferred, not as hidden TODO
</success_criteria>

<output>
After completion, create `.planning/phases/27-gsd-workflow-customization-via-patches/27-04-SUMMARY.md` documenting:
- Before/after row count of Traceability table
- Confirmation that no existing entries were modified
</output>
