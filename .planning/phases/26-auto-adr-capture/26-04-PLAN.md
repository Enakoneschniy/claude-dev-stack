---
phase: 26-auto-adr-capture
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - ADR-02
user_setup: []

must_haves:
  truths:
    - "`.planning/REQUIREMENTS.md` contains an ADR-02 requirement entry under a Decisions (ADR) section with the full 5 success criteria copied from ROADMAP.md Phase 26."
    - "The Traceability table in `.planning/REQUIREMENTS.md` includes a row for ADR-02 mapping to Phase 26."
    - "No other requirement IDs are modified — change is additive only."
    - "Milestone total requirement count in the header is updated from `Total requirements: 10` to reflect the new count."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "ADR-02 requirement + traceability entry"
      contains: "ADR-02"
  key_links:
    - from: ".planning/REQUIREMENTS.md ADR-02 entry"
      to: ".planning/ROADMAP.md Phase 26 Success Criteria"
      via: "verbatim copy of the 5 criteria"
      pattern: "ADR-02"
---

<objective>
Backfill ADR-02 into `.planning/REQUIREMENTS.md` so the milestone's canonical requirements file matches ROADMAP.md (D-14). Currently ADR-02 is only in ROADMAP.md Phase 26 — requirements file stops at LIMIT-04 + DX/UX/WF/BUG-07. This plan closes the gap, following the exact precedent of Phase 25's D-11 LIMIT-05 backfill.

Purpose: Traceability integrity. Every requirement ID referenced from plan frontmatter must be discoverable in REQUIREMENTS.md.

Output: Additive edit to `.planning/REQUIREMENTS.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-auto-adr-capture/26-CONTEXT.md

<interfaces>
Source of truth (from ROADMAP.md Phase 26 section):

```markdown
### Phase 26: Auto-ADR Capture
**Goal**: Architectural decisions are captured in vault automatically — not only from GSD discuss-phase, but from any session where significant decisions are made (new dependencies, API changes, architecture shifts).
**Depends on**: Nothing
**Requirements**: ADR-02
**Success Criteria** (what must be TRUE):
  1. Session-end hook scans session transcript for architectural decisions (new dependencies added, API endpoints changed, data model changes, significant refactors) and creates ADR files in `vault/projects/{project}/decisions/`.
  2. ADR bridge runs on session end in addition to GSD discuss-phase — decisions from any workflow (manual coding, bug fixes, hotfixes) are captured.
  3. Duplicate detection: if a decision about the same topic already exists, it updates the existing ADR instead of creating a duplicate.
  4. Each ADR includes: context (why), decision (what), consequences (tradeoffs), and source (session log link or commit hash).
  5. `claude-dev-stack decisions` CLI lists all decisions for current project with dates and status.
```

Current REQUIREMENTS.md structure (loaded) ends sections at:
- `### Workflow Enforcement (WF)` — WF-01
- `### Wizard UX Polish (UX)` — UX-01..UX-07
followed by Future Requirements, Out of Scope, and Traceability table.

Traceability table currently ends at `BUG-07 | 30 | — | pending`. ADR-02 row must be inserted after WF-01.

Header line 7: `**Total requirements**: 10 v1 requirements.` — This counts only the original v1. Since DX-07..DX-13 (7), UX-01..UX-07 (7), WF-01 (1), BUG-07 (1) were already backfilled without updating this header, and ADR-02 adds one more, update the header to reflect actual current count. Count rows in Traceability table: 10 (original v1) + 7 DX + 7 UX + 1 WF + 1 BUG-07 = 26. Adding ADR-02 → 27.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert ADR-02 section and traceability row in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (whole — need exact current structure)
    - .planning/ROADMAP.md lines 154–165 (Phase 26 block — source of truth for ADR-02 criteria)
    - .planning/phases/25-budget-aware-execution-gate/ if a similar backfill plan was done (precedent for placement)
  </read_first>
  <action>
    1. Open `.planning/REQUIREMENTS.md`. Locate the section ordering. The expected final section order after edit:
       - Bug Fixes (BUG) — unchanged
       - Limit Management (LIMIT) — unchanged
       - Smart Re-install (DX) — unchanged
       - Workflow Enforcement (WF) — unchanged
       - Wizard UX Polish (UX) — unchanged
       - **NEW:** `### Decisions (ADR)` with ADR-02 entry + the 5 success criteria from ROADMAP.md

       Place the new section AFTER `### Wizard UX Polish (UX)` and BEFORE `## Future Requirements`. Use this exact content:

       ```markdown
       ### Decisions (ADR)

       - [ ] **ADR-02**: Session-end hook scans session transcript (not just GSD discuss-phase) for architectural decisions — new dependencies, API changes, data model changes, significant refactors — and creates ADR files in `vault/projects/{project}/decisions/`. Includes duplicate detection (same topic → update, not duplicate), YAML frontmatter with source (session log path + commit SHA), and `claude-dev-stack decisions` CLI (list/show/search) for browsing.

         Success Criteria:
         1. Session-end hook scans session transcript for architectural decisions (new dependencies added, API endpoints changed, data model changes, significant refactors) and creates ADR files in `vault/projects/{project}/decisions/`.
         2. ADR bridge runs on session end in addition to GSD discuss-phase — decisions from any workflow (manual coding, bug fixes, hotfixes) are captured.
         3. Duplicate detection: if a decision about the same topic already exists, it updates the existing ADR instead of creating a duplicate.
         4. Each ADR includes: context (why), decision (what), consequences (tradeoffs), and source (session log link or commit hash).
         5. `claude-dev-stack decisions` CLI lists all decisions for current project with dates and status.
       ```

    2. Update the Traceability table. Locate the row `| BUG-07 | 30 | — | pending |` (last row). Append a new row directly after it:
       ```
       | ADR-02 | 26 | — | pending |
       ```

    3. Update header line 7 from `**Total requirements**: 10 v1 requirements.` → `**Total requirements**: 11 v1 requirements (+ 15 DX/UX/WF/BUG-07 backfills).` Count check: the Traceability table should have 27 rows after this edit (26 existing + ADR-02). If the current count differs from this assumption (e.g., header already was updated previously), adjust the header to match actual row count `{n} v1 requirements (+ {m} backfills)` where n = BUG + LIMIT + ADR rows (v1 from ROADMAP milestones) and m = DX + UX + WF + BUG-07.

    4. Do NOT modify any existing requirement entries, statuses, or rows. This edit is strictly additive.

    5. Verify by re-reading the file and confirming:
       - `### Decisions (ADR)` section present
       - `**ADR-02**:` entry contains all 5 success criteria
       - Traceability table has `ADR-02 | 26 | — | pending` row
       - No existing content was deleted or reordered
  </action>
  <verify>
    <automated>grep -q "^### Decisions (ADR)" .planning/REQUIREMENTS.md && grep -q "\*\*ADR-02\*\*" .planning/REQUIREMENTS.md && grep -q "^| ADR-02 | 26 |" .planning/REQUIREMENTS.md && grep -cE "^\| [A-Z]+-[0-9]+ \|" .planning/REQUIREMENTS.md | awk '{if ($1 < 26) {print "FAIL: traceability row count too low"; exit 1} else {print "OK:", $1, "rows"}}'</automated>
  </verify>
  <done>
    `.planning/REQUIREMENTS.md` contains the ADR-02 entry with 5 success criteria verbatim from ROADMAP.md, the Traceability table includes the new row, the header count is updated, and no pre-existing content was modified. `git diff --stat .planning/REQUIREMENTS.md` shows only additions (no deletions).
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

None — this plan edits a local planning document with no runtime or network exposure. Standard editorial change.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-15 | Tampering (accidental deletion of existing requirements) | REQUIREMENTS.md edit | mitigate | Use Edit tool (additive insertion only) or read-then-write with explicit diff review. Verification grep counts existing rows before and after to detect regressions. |

</threat_model>

<verification>
  - `grep -c "^| [A-Z]\+-[0-9]\+ |" .planning/REQUIREMENTS.md` returns at least 26 rows (27 expected)
  - `grep "ADR-02" .planning/REQUIREMENTS.md | wc -l` ≥ 2 (one in section, one in table)
  - `git diff --numstat .planning/REQUIREMENTS.md` shows only additions (second column 0 deletions ideally, acceptable if only header count line changed)
</verification>

<success_criteria>
  - ADR-02 is discoverable from REQUIREMENTS.md, not just ROADMAP.md
  - Traceability preserved: every plan's `requirements: [ADR-02]` frontmatter resolves to a real requirement entry
  - Decision coverage: D-14 (ADR-02 backfill mirrors Phase 25 D-11 LIMIT-05 pattern)
</success_criteria>

<output>
After completion, create `.planning/phases/26-auto-adr-capture/26-04-SUMMARY.md` documenting:
- Before/after row count of Traceability table
- Confirmation that no existing entries were modified (git diff evidence)
</output>
