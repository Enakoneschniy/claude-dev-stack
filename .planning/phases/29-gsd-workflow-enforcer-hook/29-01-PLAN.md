---
phase: 29-gsd-workflow-enforcer-hook
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hooks/gsd-workflow-enforcer.mjs
  - tests/workflow-enforcer.test.mjs
autonomous: true
requirements:
  - WF-01
response_language: ru
tags: [hooks, workflow, gsd, posttooluse, enforcer]

user_setup: []

must_haves:
  truths:
    - "When `/gsd-plan-phase` skill finishes and 2+ pending phases still need planning (no CONTEXT.md file yet) in .planning/ROADMAP.md, gsd-workflow-enforcer.mjs writes a single-line `NEXT: /gsd-discuss-phase M — do NOT run /gsd-execute-phase; use /gsd-manager only after all pending phases are planned` (M = next unplanned phase number) to stdout"
    - "When all pending phases in ROADMAP have CONTEXT.md files present, enforcer writes `NEXT: /gsd-manager — all N pending phases have plans; spawn parallel execute team` to stdout"
    - "When 0 or 1 pending phases remain in ROADMAP.md, enforcer exits 0 silently with no stdout — normal `/gsd-execute-phase` flow uninterrupted"
    - "When `tool_input.skill` is anything other than `gsd-plan-phase` (e.g. `gsd-discuss-phase`, `gsd-execute-phase`, non-GSD skills), enforcer exits 0 silently — no parsing, no output"
    - "When `.planning/ROADMAP.md` does not exist in cwd (non-GSD project), enforcer exits 0 silently — never fails, never logs visibly"
    - "Enforcer reads `tool_input.skill` (NOT `tool_input.skill_name`) matching Phase 25 research finding — if wrong field is sent, enforcer stays silent"
    - "Phase numbers parsed from ROADMAP are integers (1–999, with optional `.N` suffix like `28.1`) and are written to stdout verbatim without shell-interpolation — no injection vector via malicious ROADMAP content"
    - "Full test suite (baseline 716+) remains green with new tests/workflow-enforcer.test.mjs covering every must-have truth above"
  artifacts:
    - path: "hooks/gsd-workflow-enforcer.mjs"
      provides: "PostToolUse Skill hook — reads tool_input.skill, filters to gsd-plan-phase, counts pending phases from ROADMAP.md, emits next-step guidance when 2+ pending remain, fail-silent on non-GSD / malformed / missing ROADMAP"
      contains: "tool_input?.skill"
    - path: "tests/workflow-enforcer.test.mjs"
      provides: "WF-01 SC#1..SC#6 coverage — silent on non-plan-phase skill, silent on missing ROADMAP, emits discuss-next on ≥2 unplanned, emits manager on all-planned, silent on 0–1 pending, ignores skill_name field, handles regex DoS payload safely"
      contains: "workflow-enforcer"
  key_links:
    - from: "hooks/gsd-workflow-enforcer.mjs"
      to: "process.cwd() + /.planning/ROADMAP.md"
      via: "readFileSync with utf8, wrapped in try/catch → fail-silent"
      pattern: "ROADMAP\\.md"
    - from: "hooks/gsd-workflow-enforcer.mjs"
      to: "process.cwd() + /.planning/phases/{NN}-*/"
      via: "readdirSync to test presence of `{NN}-CONTEXT.md` per pending phase"
      pattern: "\\.planning/phases"
    - from: "hooks/gsd-workflow-enforcer.mjs stdin"
      to: "tool_input.skill === 'gsd-plan-phase' filter"
      via: "JSON.parse → data?.tool_input?.skill string compare"
      pattern: "gsd-plan-phase"
---

<objective>
Ship the PostToolUse workflow-enforcer hook that fires after `/gsd-plan-phase` completes, counts pending phases in the project roadmap, and emits next-step guidance so Claude cannot suggest `/gsd-execute-phase` mid-batch when the user still has unplanned phases in the milestone.

Purpose: Without this hook, Claude routinely violates the GSD batching design (discuss+plan for all pending phases, then `/gsd-manager` for parallel execute) by jumping to `/gsd-execute-phase` after each single plan. WF-01 encodes the behavior as a runtime gate — the CLAUDE.md instruction (Phase 19 era) proved insufficient as a prompt-only safeguard.

Output: One ESM hook script with TDD test file. No wizard changes yet (Plan 02 wires install).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-gsd-workflow-enforcer-hook/29-CONTEXT.md
@.planning/phases/25-budget-aware-execution-gate/25-01-PLAN.md
@hooks/budget-check.mjs
@hooks/budget-reset.mjs

<interfaces>
<!-- Key contracts extracted for the executor — no codebase exploration required. -->

PostToolUse Skill hook stdin (verified in Phase 25 research and production hook behavior):
```ts
interface HookInput {
  session_id: string;
  transcript_path: string;
  cwd: string;                // absolute cwd — but hook can also use process.cwd()
  hook_event_name: "PostToolUse";
  tool_name: "Skill";          // capital S
  tool_input: {
    skill: string;             // "gsd-plan-phase", "gsd-discuss-phase", etc. — NOT skill_name
    args?: string;
  };
  tool_use_id: string;
}
```

Existing ROADMAP.md pending-phase format (verified via grep on current file):
```
- [ ] **Phase 21: Continuation Prompt & loop.md** — 4-option continuation prompt ... (LIMIT-02, LIMIT-03)
- [ ] **Phase 22: Post-Reset Handoff** — Load STATE.md on scheduled task fire and continue from stopped_at (LIMIT-04)
- [ ] **Phase 29: GSD Workflow Enforcer Hook** — PostToolUse hook ... (WF-01)
- [ ] **Phase 30: CLAUDE.md Idempotent Merge** — Wizard preserves user content ...
```
Only lines under the `## Phases` list section use `- [ ]` + `**Phase N:` prefix.

Phase completion detection — a phase is "planned" when its phase-slug directory contains `{NN}-CONTEXT.md`:
- `.planning/phases/29-gsd-workflow-enforcer-hook/29-CONTEXT.md` → phase 29 is planned (has context).
- `.planning/phases/30-claude-md-idempotent-merge/` → if no `30-CONTEXT.md`, phase 30 is unplanned.
- Directory names have flexible slug suffix after the number (e.g., `28-silent-session-start-move-vault-context-loading-to-sessionst`). Match by numeric prefix `^NN(\.M)?-` via `readdirSync`.

Fail-silent pattern (mirror `hooks/budget-check.mjs`):
```js
async function main() { /* ... */ }
main().catch(() => {}).finally(() => process.exit(0));
```

stdin read helper (copy from `hooks/budget-check.mjs` lines 38–49 — same pattern):
```js
async function readStdin(timeoutMs = 500) {
  const chunks = [];
  await new Promise((resolve) => {
    const timer = setTimeout(resolve, timeoutMs);
    process.stdin.on('data', c => chunks.push(c));
    process.stdin.on('end', () => { clearTimeout(timer); resolve(); });
    process.stdin.on('error', () => { clearTimeout(timer); resolve(); });
  });
  return Buffer.concat(chunks).toString('utf8').trim();
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement hooks/gsd-workflow-enforcer.mjs (PostToolUse Skill hook)</name>
  <files>hooks/gsd-workflow-enforcer.mjs, tests/workflow-enforcer.test.mjs</files>
  <read_first>
    - hooks/budget-check.mjs (lines 1–60) — stdin parsing + fail-silent pattern
    - hooks/budget-reset.mjs — shortest fail-silent hook example
    - .planning/phases/25-budget-aware-execution-gate/25-01-PLAN.md Task 1 (budget-gate) — sibling architecture for Skill-filtered hook
    - .planning/phases/29-gsd-workflow-enforcer-hook/29-CONTEXT.md (all decisions D-01..D-08)
    - .planning/ROADMAP.md lines 40–65 — actual phase list format to parse against
  </read_first>
  <behavior>
    Filter (per D-01, D-02, D-03):
    - `tool_name !== 'Skill'` → exit 0 silently, no output.
    - `tool_input.skill !== 'gsd-plan-phase'` → exit 0 silently (covers `gsd-discuss-phase`, `gsd-execute-phase`, `gsd-manager`, all non-GSD skills).
    - Missing `tool_input` or missing `skill` field → exit 0 silently.
    - Use `data?.tool_input?.skill` — NEVER `tool_input.skill_name` (per Phase 25 research finding, D-03).

    Fail-silent fallback (per D-07, D-08):
    - `.planning/ROADMAP.md` absent in `process.cwd()` → exit 0 silently (non-GSD project).
    - `readFileSync` throws → exit 0 silently.
    - Any exception anywhere in main() → caught by `.catch(() => {}).finally(() => process.exit(0))`.

    Pending-phase counter (per D-04, D-05, D-06):
    - Read ROADMAP.md as utf8 string.
    - Match lines against exact regex `^- \[ \] \*\*Phase (\d+(?:\.\d+)?):/gm` — extract the phase number as a string (preserve `.N` suffix like `28.1`).
    - IMPORTANT: the regex must be anchored (`^` with `/m` flag) and limited to matching at the start of each line, reducing regex-DoS risk. Bound line count processed to first 10_000 lines (slice after split) before match — hard cap against adversarial ROADMAP content.
    - Sanitize each extracted phase number: must satisfy `/^\d{1,4}(\.\d{1,2})?$/`; reject and skip any that do not.
    - For each pending phase number N, check whether a "planned" marker exists:
      * Planned = a directory under `.planning/phases/` whose basename starts with exactly `${N}-` (numeric prefix) AND contains a file named `${N}-CONTEXT.md`.
      * Use `readdirSync('.planning/phases', { withFileTypes: true })` and filter by `entry.isDirectory()` and `entry.name.match(new RegExp('^' + escapeForRegex(N) + '-'))`.
    - unplanned = count of pending phases with NO matching CONTEXT.md file.
    - planned_but_pending = count of pending phases that DO have CONTEXT.md.

    Three output paths (per D-06):
    - `unplanned >= 1 AND (unplanned + planned_but_pending) >= 2` → Output path 1: emit `NEXT: /gsd-discuss-phase M — do NOT run /gsd-execute-phase; use /gsd-manager only after all pending phases are planned` where `M` is the FIRST unplanned phase number in ROADMAP order.
    - `unplanned === 0 AND planned_but_pending >= 2` → Output path 2: emit `NEXT: /gsd-manager — all ${planned_but_pending} pending phases have plans; spawn parallel execute team`.
    - `(unplanned + planned_but_pending) <= 1` → Output path 3: exit 0 silently (no stdout).

    Output formatting (per D-08, security):
    - Write a single line terminated by `\n` using `process.stdout.write`.
    - The phase number M is interpolated as a string — but it has been validated by regex `/^\d{1,4}(\.\d{1,2})?$/`, so it CANNOT contain shell metacharacters, newlines, or ANSI escapes.
    - `${planned_but_pending}` is likewise an integer, safe.
    - The directive begins with literal `NEXT:` as first token on a new line (per specifics — Claude recognizes the pattern).
  </behavior>
  <action>
    Create `hooks/gsd-workflow-enforcer.mjs` with:

    ```
    #!/usr/bin/env node
    /**
     * hooks/gsd-workflow-enforcer.mjs — PostToolUse hook: GSD batching enforcement
     *
     * Fires after `/gsd-plan-phase` completes. Reads .planning/ROADMAP.md to count
     * pending phases; emits NEXT directive preventing premature /gsd-execute-phase
     * suggestion when 2+ pending phases remain.
     *
     * WF-01: Phase 29. Fail-silent on non-GSD projects.
     */
    ```

    Constants:
    - `ROADMAP_PATH = join(process.cwd(), '.planning', 'ROADMAP.md')`
    - `PHASES_DIR = join(process.cwd(), '.planning', 'phases')`
    - `MAX_LINES = 10_000` (regex-DoS guard)
    - `PHASE_NUM_REGEX = /^- \[ \] \*\*Phase (\d{1,4}(?:\.\d{1,2})?):/`  (per-line match, bounded quantifiers, no backtracking hazards)
    - `VALID_PHASE_NUM = /^\d{1,4}(\.\d{1,2})?$/`  (validation of extracted capture)

    Helpers:
    - `readStdin(timeoutMs = 500)` — copy verbatim from `hooks/budget-check.mjs`.
    - `escapeForRegex(s)` — returns `s.replace(/[.*+?^${}()|[\]\\]/g, '\\$&')`.
    - `isPhasePlanned(phaseNum)` — `readdirSync(PHASES_DIR, { withFileTypes: true })`, find entry where `entry.isDirectory()` and name matches `new RegExp('^' + escapeForRegex(phaseNum) + '-')`. If found, check `existsSync(join(PHASES_DIR, entry.name, `${phaseNum}-CONTEXT.md`))`. Return boolean. Wrap in try/catch → return false.
    - `parsePendingPhases(roadmapText)` — split on `\n`, slice(0, MAX_LINES), filter match against PHASE_NUM_REGEX, map to capture group, filter by VALID_PHASE_NUM. Return array of phase-number strings in file order.

    `main()`:
    1. `raw = await readStdin()`. If empty → return.
    2. `let data; try { data = JSON.parse(raw); } catch { return; }`.
    3. If `data?.tool_name !== 'Skill'` → return.
    4. If `data?.tool_input?.skill !== 'gsd-plan-phase'` → return.
    5. If `!existsSync(ROADMAP_PATH)` → return.
    6. Read `roadmapText = readFileSync(ROADMAP_PATH, 'utf8')`.
    7. `pending = parsePendingPhases(roadmapText)`. If `pending.length <= 1` → return.
    8. Compute `unplanned = pending.filter(n => !isPhasePlanned(n))`.
    9. Compute `plannedButPending = pending.length - unplanned.length`.
    10. Decide output path:
        - If `unplanned.length >= 1 && pending.length >= 2`:
          `process.stdout.write(`NEXT: /gsd-discuss-phase ${unplanned[0]} — do NOT run /gsd-execute-phase; use /gsd-manager only after all pending phases are planned\n`);`
        - Else if `unplanned.length === 0 && plannedButPending >= 2`:
          `process.stdout.write(`NEXT: /gsd-manager — all ${plannedButPending} pending phases have plans; spawn parallel execute team\n`);`
        - Else: return (no output).

    End with `main().catch(() => {}).finally(() => process.exit(0));`.

    ---

    Create `tests/workflow-enforcer.test.mjs` using Node built-in test runner. Mirror the isolation pattern of `tests/budget.test.mjs` — use `mkdtempSync(join(tmpdir(), 'wfe-'))` for a temp cwd, seed `.planning/ROADMAP.md` + `.planning/phases/{NN}-.../{NN}-CONTEXT.md` fixture dirs, and run the hook via `execFileSync('node', [hookPath], { cwd: tmpDir, input: JSON.stringify(stdin), encoding: 'utf8' })`.

    Required test cases (names are the `it()` titles):
    1. `it('exits silently on non-Skill tool_name')` — stdin `{tool_name:"Bash",tool_input:{skill:"gsd-plan-phase"}}` → expect stdout === "".
    2. `it('exits silently when skill is not gsd-plan-phase')` — stdin `{tool_name:"Skill",tool_input:{skill:"gsd-discuss-phase"}}` (plus valid ROADMAP with 3 unplanned phases) → expect stdout === "".
    3. `it('ignores tool_input.skill_name field (wrong key)')` — stdin `{tool_name:"Skill",tool_input:{skill_name:"gsd-plan-phase"}}` → expect stdout === "".
    4. `it('exits silently when ROADMAP.md is missing')` — tmpDir with no `.planning/`, valid stdin → expect stdout === "".
    5. `it('emits discuss-next when 2+ unplanned phases pending')` — seed ROADMAP with pending phases 30, 31, no CONTEXT.md files for either → expect stdout starts with "NEXT: /gsd-discuss-phase 30 — do NOT run /gsd-execute-phase" and ends with newline.
    6. `it('picks the FIRST unplanned phase as M')` — seed phases 22 (has CONTEXT.md), 30 (no CONTEXT.md), 31 (no CONTEXT.md) → expect stdout contains "discuss-phase 30" (not 22, not 31).
    7. `it('emits manager directive when all pending phases are planned')` — seed phases 25, 26, 27, each with its NN-CONTEXT.md file present → expect stdout startsWith "NEXT: /gsd-manager — all 3 pending phases have plans".
    8. `it('exits silently when only 1 pending phase remains')` — seed ROADMAP with single `- [ ] **Phase 30:...` line → expect stdout === "".
    9. `it('exits silently when 0 pending phases remain')` — seed ROADMAP with only `- [x]` entries, no `- [ ]` pending → expect stdout === "".
    10. `it('handles decimal phase numbers like 28.1')` — seed phase 28.1 (unplanned) + 29 (unplanned) → expect stdout contains "discuss-phase 28.1".
    11. `it('rejects malformed phase numbers (regex-DoS guard)')` — seed ROADMAP containing a crafted line `- [ ] **Phase 9999999999999999999999:** overflow` plus valid phases 30, 31 → expect hook completes under 500 ms AND stdout references 30 (not the overflow number). Use `const t = Date.now(); execFileSync(...); assert.ok(Date.now() - t < 500)`.
    12. `it('bounds scan to MAX_LINES')` — generate a ROADMAP with 20_000 junk lines + valid phases 30, 31 at the top → expect completion under 500 ms.
    13. `it('outputs single line terminated by newline')` — discuss-next case → assert stdout ends with `\n` and contains exactly one `\n`.

    Each test should:
    - Create tmp cwd via `mkdtempSync`.
    - Register `after` / `t.after` cleanup with `rmSync(tmpDir, { recursive: true, force: true })`.
    - Invoke hook via `execFileSync('node', [join(repoRoot, 'hooks/gsd-workflow-enforcer.mjs')], { cwd: tmpDir, input: JSON.stringify(stdin), encoding: 'utf8' })` — capture stdout.

    Implements WF-01 SC#1..SC#5 runtime behavior. SC#6 (wizard install) is Plan 02.
  </action>
  <verify>
    <automated>node --test tests/workflow-enforcer.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `hooks/gsd-workflow-enforcer.mjs` exists with `#!/usr/bin/env node` shebang as line 1.
    - `grep -c "tool_input?.skill" hooks/gsd-workflow-enforcer.mjs` ≥ 1.
    - `grep -c "tool_input.skill_name\|tool_input?.skill_name" hooks/gsd-workflow-enforcer.mjs` === 0 (per D-03).
    - `grep -c "process.exit(0)" hooks/gsd-workflow-enforcer.mjs` ≥ 1 (fail-silent invariant).
    - `grep -c "MAX_LINES\|slice(0, 10" hooks/gsd-workflow-enforcer.mjs` ≥ 1 (regex-DoS guard).
    - `grep -c "VALID_PHASE_NUM\|^\\\\d{1,4}" hooks/gsd-workflow-enforcer.mjs` ≥ 1 (phase-number validation).
    - `node --check hooks/gsd-workflow-enforcer.mjs` passes.
    - `node --test tests/workflow-enforcer.test.mjs` reports all 13 cases passing.
    - Full `npm test` suite green (baseline 716+ — tests/workflow-enforcer.test.mjs adds ~13).
  </acceptance_criteria>
  <done>
    gsd-workflow-enforcer.mjs implements PostToolUse hook with three output paths; all 13 test cases pass; regex-DoS guard in place; fail-silent on non-GSD / missing ROADMAP. No wizard wiring yet.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude Code → hook stdin | Untrusted JSON payload (session_id, tool_input.skill) crosses here |
| Project ROADMAP.md → hook regex | Untrusted file content (user-writable) is regex-matched — regex-DoS vector |
| Hook → stdout | Phase number interpolated into a directive — injection vector if not validated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | Denial of Service | Malicious or corrupted ROADMAP.md triggers catastrophic-backtracking regex on phase-line scan | mitigate | Regex anchored with `^` + `/m` + bounded quantifiers `\d{1,4}(\.\d{1,2})?` — no nested quantifiers, no `.*` — linear time. Additional cap: slice text to first 10_000 lines before matching (MAX_LINES constant). Test case 11/12 assert <500 ms completion even with adversarial input. |
| T-29-02 | Injection (Output) | Phase number from ROADMAP.md is interpolated into stdout directive — could a crafted "phase number" inject newlines, ANSI escapes, or fake `NEXT:` lines that confuse Claude? | mitigate | After regex capture, each phase number is re-validated against strict `/^\d{1,4}(\.\d{1,2})?$/` — any value failing this is discarded. Digits + optional `.` cannot contain newlines, ANSI, or shell metacharacters. Test case 11 covers overflow rejection. |
| T-29-03 | Information Disclosure | stdin's `session_id` / `transcript_path` could be logged inadvertently | accept | The hook does not write any log file in this plan (D-08 mentions debug log — deferred to Plan 02 if needed). No log path is constructed from stdin. |
| T-29-04 | Tampering | Another hook or process writes a fake `NEXT:` directive via a different code path | accept | Out of scope — Claude's prompt-routing already trusts hook stdout globally. This is a platform-level concern, not a Phase 29 issue. |
| T-29-05 | Denial of Service | Hook blocks `/gsd-plan-phase` on slow filesystem (readdirSync on large phases/ dir) | mitigate | `readdirSync` is synchronous but bounded by actual directory content (< 50 phases typical, each with small file count). Hook `timeout: 10` seconds is configured in Plan 02 settings.json — a runaway read fails open via top-level `.catch`. |
| T-29-06 | Elevation of Privilege | `matcher: "Skill"` hook runs `node` as user | accept | Same privilege level as Claude Code itself, equivalent to existing budget-check.mjs / vault-auto-push.sh. No new escalation surface. |
</threat_model>

<verification>
- `node --test tests/workflow-enforcer.test.mjs` — all 13 cases green.
- `npm test` full suite — baseline 716+ still green.
- Manual: `echo '{"tool_name":"Skill","tool_input":{"skill":"gsd-plan-phase"}}' | node hooks/gsd-workflow-enforcer.mjs` from repo root (which has real ROADMAP.md with pending phases 21, 22, 24, 26, 27, 30, 31) → expect `NEXT: /gsd-discuss-phase 21 — do NOT run /gsd-execute-phase; ...` on stdout.
- Manual: `echo '{"tool_name":"Skill","tool_input":{"skill":"gsd-discuss-phase"}}' | node hooks/gsd-workflow-enforcer.mjs` → expect empty stdout (silent filter).
- Manual: run from `/tmp` (no .planning/) → expect empty stdout (fail-silent).
</verification>

<success_criteria>
- WF-01 SC#1 satisfied: hook fires after `/gsd-plan-phase N` completes (PostToolUse on Skill tool with filter — wired by Plan 02).
- WF-01 SC#2 satisfied: hook reads `.planning/ROADMAP.md` and counts pending phases via CONTEXT.md presence check.
- WF-01 SC#3 satisfied: when 2+ unplanned remain, emits `NEXT: /gsd-discuss-phase M — ...` with next unplanned phase number.
- WF-01 SC#4 satisfied: when 0–1 pending, stays silent (no output, exit 0).
- WF-01 SC#5 satisfied: exits 0 silently on missing/malformed ROADMAP.md.
- WF-01 SC#6: hook script now exists in `hooks/` — wizard install wiring follows in Plan 02.
- All 13 test cases pass; full suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/29-gsd-workflow-enforcer-hook/29-01-SUMMARY.md`.
</output>
