---
phase: 28-silent-session-start-move-vault-context-loading-to-sessionst
plan: 03
type: execute
wave: 2
depends_on:
  - 28-01
  - 28-02
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - SSR-01
user_setup: []

must_haves:
  truths:
    - ".planning/REQUIREMENTS.md gains a new '### Session Start/Resume (SSR)' section placed after '### GSD Workflow (GSD)' (line ~62) and before the '---' that precedes 'Future Requirements'."
    - "The new section contains exactly one bullet: '- [ ] **SSR-01**: ...' with text matching the requirement body in 28-CONTEXT.md D-08."
    - "The Traceability table gains exactly one new row: '| SSR-01 | 28 | — | pending |' appended after the GSD-01 row (line ~122)."
    - "The total-requirements count at the top of the file (currently '12 v1 requirements (+ 15 DX/UX/WF/BUG-07 backfills + 1 ADR-02 backfill + 1 GSD-01 backfill).') is updated to reflect the new SSR-01 backfill."
    - "No other content in REQUIREMENTS.md is modified (git diff should show a clean 3-location change: header count, new SSR section, new traceability row)."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated v0.12 requirements with SSR-01"
      contains: "SSR-01"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/28-silent-session-start-move-vault-context-loading-to-sessionst/28-CONTEXT.md"
      via: "REQ-ID SSR-01 → Phase 28"
      pattern: "SSR-01 traceability backfill"
---

<objective>
Backfill SSR-01 into the milestone v0.12 requirements document so the
phase has a formal, tracked requirement ID matching the
REQUIREMENTS/Traceability convention used by every other phase.

Covers SSR-01 Success Criterion #6.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/28-silent-session-start-move-vault-context-loading-to-sessionst/28-CONTEXT.md

<interfaces>
**Header update (line 7):**

Current:
```
**Total requirements**: 12 v1 requirements (+ 15 DX/UX/WF/BUG-07 backfills + 1 ADR-02 backfill + 1 GSD-01 backfill).
```

New:
```
**Total requirements**: 12 v1 requirements (+ 15 DX/UX/WF/BUG-07 backfills + 1 ADR-02 backfill + 1 GSD-01 backfill + 1 SSR-01 backfill).
```

**New section — insert between line 74 (end of GSD block) and line 75 (the
`---` separator above "Future Requirements"):**

```markdown

### Session Start/Resume (SSR)

- [ ] **SSR-01**: SessionStart hook is the single source of vault context
  loading for configured projects. The `session-manager` skill does not
  auto-activate on greetings; its `/resume` path checks a
  `.claude/.session-loaded` marker (atomic, ISO 8601 UTC) and skips the
  redundant `cat` when marker is < 60 min old. CLAUDE.md template
  instructs Claude not to re-read `context.md` on the first message.
  Install wizard adds the marker path to project `.gitignore` idempotently.

  Success Criteria:
  1. CLAUDE.md template "Knowledge Base" section instructs Claude NOT to re-read `context.md` / session logs on the first message.
  2. `session-manager` skill description omits greeting triggers ("привет", "hi", "начинаем") and first-message auto-activation.
  3. SessionStart hook writes `.claude/.session-loaded` marker atomically (ISO 8601 UTC timestamp) on every successful run.
  4. Install wizard adds `.claude/.session-loaded` to each configured project's `.gitignore` idempotently.
  5. `session-manager` `/resume` path checks marker mtime — if < 60 min, uses pre-loaded context; otherwise falls through to explicit `cat`.
  6. `.planning/REQUIREMENTS.md` contains this section and the `| SSR-01 | 28 | — | pending |` traceability row.
```

**Traceability row — append after the GSD-01 line at the bottom of the
table (current last line, line 122):**

```
| SSR-01 | 28 | — | pending |
```
</interfaces>
</context>

<verification>
- `grep -n "SSR-01" .planning/REQUIREMENTS.md` → prints at least two lines:
  one in the new section header/bullet, one in the traceability row.
- `grep -n "### Session Start/Resume (SSR)" .planning/REQUIREMENTS.md` →
  prints exactly one match.
- `git diff .planning/REQUIREMENTS.md` shows only additions (no deletions
  apart from the header count line's single replaced line).
- No test file required — this is a content-only documentation change.
</verification>

<do_not>
- Do NOT renumber or reorder any existing requirement IDs.
- Do NOT edit other sections (BUG, LIMIT, DX, UX, ADR, GSD bullets).
- Do NOT reformat the Traceability table (keep the same column widths and
  `—` em-dash for the Plan column).
- Do NOT update STATE.md / ROADMAP.md — those are handled by the GSD
  wrapper, not by this plan.
</do_not>
