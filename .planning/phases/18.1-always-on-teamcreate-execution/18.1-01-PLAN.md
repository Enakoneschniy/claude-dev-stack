---
phase: 18.1-always-on-teamcreate-execution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - $HOME/.claude/get-shit-done/workflows/transition.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "When 2+ pending phases exist, transition.md Route A spawns a TeamCreate with one member per phase — no user consent prompt"
    - "Task dependencies from ROADMAP depends_on are translated to TaskCreate blockedBy — phases wait automatically"
    - "If TeamCreate is unavailable at runtime, the code falls back to sequential single-phase logic unchanged"
    - "If only 1 pending phase remains, the sequential logic runs as before — no team spawned"
    - "Users still invoke /gsd-execute-phase N — the change is transparent"
  artifacts:
    - path: "$HOME/.claude/get-shit-done/workflows/transition.md"
      provides: "Always-on TeamCreate spawning in Route A"
      contains: "TeamCreate"
  key_links:
    - from: "transition.md Route A"
      to: "roadmap analyze"
      via: "gsd-tools.cjs roadmap analyze JSON parsing"
      pattern: "roadmap analyze"
    - from: "transition.md Route A"
      to: "TeamCreate"
      via: "spawn one member per pending phase"
      pattern: "TeamCreate"
    - from: "transition.md Route A"
      to: "TaskCreate blockedBy"
      via: "depends_on mapping"
      pattern: "blockedBy"
---

<objective>
Replace the current "detect parallel → offer TeamCreate" model in transition.md Route A with an "always TeamCreate" model. When executing a milestone with multiple remaining phases, ALL phase members are spawned upfront with task dependencies matching ROADMAP `depends_on`. No separate parallel/sequential code paths. Members auto-start when their blockers complete, idle when blocked.

Purpose: Eliminate the independence test and user consent prompt. Every multi-phase milestone automatically gets team-based execution with dependency-driven scheduling.
Output: Modified `$HOME/.claude/get-shit-done/workflows/transition.md` with new Route A parallel block (lines ~490-559 replaced).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18.1-always-on-teamcreate-execution/18.1-CONTEXT.md

<interfaces>
<!-- Target file: transition.md Route A, lines 486-559 -->
<!-- The executor must REPLACE lines 490-559 (from "**Parallel phase detection**" through -->
<!-- the "CRITICAL per D-12" paragraph) with the new always-team block. -->
<!-- Line 486 "**Route A: More phases remain in milestone**" stays. -->
<!-- Line 488 "Read ROADMAP.md to get the next phase's name and goal." stays. -->
<!-- Lines 560+ (sequential Route A: "Check if next phase has CONTEXT.md" etc.) MUST remain intact. -->

Current structure (lines 486-560):
```
486: **Route A: More phases remain in milestone**
488: Read ROADMAP.md to get the next phase's name and goal.
490: **Parallel phase detection (before presenting next phase):**
491: Check if multiple independent phases can run concurrently.
...  [independence test, consent prompt, TeamCreate offer]
559: **CRITICAL per D-12:** ... existing flow runs exactly as before.
560: (blank line)
561: **Check if next phase has CONTEXT.md:**
```

After edit:
```
486: **Route A: More phases remain in milestone**  (unchanged)
488: Read ROADMAP.md to get the next phase's name and goal.  (unchanged)
490: **Always-on team execution (multi-phase):**  (NEW — replacement block)
...  [new always-team logic]
~NNN: (blank line)
~NNN+1: **Check if next phase has CONTEXT.md:**  (unchanged, was line 561)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace parallel detection block with always-team logic in transition.md</name>
  <files>$HOME/.claude/get-shit-done/workflows/transition.md</files>
  <read_first>
    - $HOME/.claude/get-shit-done/workflows/transition.md (full file, focus on lines 486-570)
  </read_first>
  <action>
Edit `$HOME/.claude/get-shit-done/workflows/transition.md`. Replace the content between line 489 (after "Read ROADMAP.md to get the next phase's name and goal.") and line 560 (before the blank line + "**Check if next phase has CONTEXT.md:**") with the new always-team block below.

**IMPORTANT:** Do NOT touch line 486 ("**Route A: More phases remain in milestone**"), line 488 ("Read ROADMAP.md to get the next phase's name and goal."), or anything from line 561 onward. Only replace lines 490-559.

The replacement content (insert after line 489, before what was line 561):

```markdown

**Always-on team execution (multi-phase):**

Detect all remaining pending phases and spawn a team to execute them with dependency-aware scheduling.

\```bash
ROADMAP_DATA=$(node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" roadmap analyze)
\```

Parse the JSON result. Collect all phases with status "pending" (not started, not complete) into a list called `PENDING_PHASES`. For each phase, extract:
- `number` — phase number (e.g., 15)
- `name` — phase name/slug
- `depends_on` — list of phase numbers this phase depends on
- `plan_count` — number of plans

**If 2+ pending phases found — spawn team:**

Use TeamCreate to create a milestone execution team. One member per pending phase.

**Step 1 — Create tasks with dependency mapping:**

For each pending phase, create a task via TaskCreate:
- `subject`: "Execute Phase {number}: {name}"
- `description`: "Run /gsd-execute-phase {number}. Plans: {plan_count}. Depends on: {depends_on or 'nothing'}."

After creating all tasks, wire dependencies via TaskUpdate:
- For each phase whose `depends_on` includes another pending phase number, add `blockedBy` pointing to that phase's task ID.
- Phases that depend on already-completed phases have NO blockers (the dependency is already satisfied).
- Phases with no dependencies and no pending blockers start immediately.

**Step 2 — Spawn team members:**

Use TeamCreate with:
- `team_name`: "milestone-execution"
- `description`: "Execute all remaining milestone phases with dependency-aware scheduling"

For each pending phase, spawn a member via Agent tool:
- `name`: "phase-{number}-{slug}" (e.g., "phase-16-git-conventions")
- `isolation`: "worktree"
- `team_name`: "milestone-execution"
- `prompt`: "You are executing Phase {number}: {name}. Check your task status — if blocked, wait for dependencies to complete. When unblocked, run: /gsd-execute-phase {number}. Report completion via SendMessage to the team lead when done."

Members with unblocked tasks start immediately. Members with blocked tasks idle until their blockers are marked complete.

**Step 3 — Team lead coordination:**

The team lead (this agent) monitors task completion:
- When a member completes its phase, mark its task as `completed` via TaskUpdate
- This automatically unblocks dependent members who were waiting
- Send completion notification to waiting members via SendMessage

**After team completion:**
- Collect results from all members
- Report successes: "Phase {X} completed successfully"
- Report failures: "Phase {Y} failed: {error}. Options: retry, skip, investigate"
- Partial success is valid (per D-08) — do not re-run successful phases
- User can retry failed phases individually (per D-09)

**If only 1 pending phase found — sequential execution:**

Skip team spawning. Fall through to the existing sequential Route A logic below (presenting next phase, yolo auto-continue, interactive prompt). This is the same behavior as before.

**If TeamCreate is not available in the runtime:**

Catch the error and fall back: `warn('Team execution not available in this runtime — running sequentially')`. Then proceed with existing sequential Route A logic below. This preserves backward compatibility with older Claude Code versions or restricted environments (per D-11).
```

**What to verify after editing:**
1. Line 486 still reads "**Route A: More phases remain in milestone**"
2. The new block starts with "**Always-on team execution (multi-phase):**"
3. The old independence test, consent prompt, and "CRITICAL per D-12" paragraph are gone
4. The line "**Check if next phase has CONTEXT.md:**" still exists after the new block
5. No mention of "independence test" or "consent prompt" or "Run in parallel" option remains
  </action>
  <acceptance_criteria>
    - grep "Always-on team execution" $HOME/.claude/get-shit-done/workflows/transition.md returns a match
    - grep "TeamCreate" $HOME/.claude/get-shit-done/workflows/transition.md returns matches (team spawning)
    - grep "blockedBy" $HOME/.claude/get-shit-done/workflows/transition.md returns matches (dependency wiring)
    - grep "Independence test" $HOME/.claude/get-shit-done/workflows/transition.md returns NO match (old logic removed)
    - grep "Parallel Execution Available" $HOME/.claude/get-shit-done/workflows/transition.md returns NO match (consent prompt removed)
    - grep "Run in parallel" $HOME/.claude/get-shit-done/workflows/transition.md returns NO match (old option removed)
    - grep "Check if next phase has CONTEXT.md" $HOME/.claude/get-shit-done/workflows/transition.md returns a match (sequential fallback preserved)
    - grep "Team execution not available" $HOME/.claude/get-shit-done/workflows/transition.md returns a match (D-11 fallback)
    - grep "Route A: More phases remain" $HOME/.claude/get-shit-done/workflows/transition.md returns a match (header preserved)
  </acceptance_criteria>
  <verify>
    <automated>grep -c "Always-on team execution" "$HOME/.claude/get-shit-done/workflows/transition.md" && grep -c "blockedBy" "$HOME/.claude/get-shit-done/workflows/transition.md" && grep -c "Independence test" "$HOME/.claude/get-shit-done/workflows/transition.md" | grep "^0$" && grep -c "Check if next phase has CONTEXT.md" "$HOME/.claude/get-shit-done/workflows/transition.md"</automated>
  </verify>
  <done>
    - transition.md Route A parallel detection block (lines 490-559) is replaced with always-team logic
    - Old independence test, consent prompt, and "Run in parallel" option are removed
    - New block spawns TeamCreate with one member per pending phase
    - Dependencies mapped from ROADMAP depends_on to TaskCreate blockedBy
    - Sequential fallback preserved for 1-phase case and TeamCreate-unavailable case
    - All content after the replaced block (line 561+) is untouched
  </done>
</task>

<task type="auto">
  <name>Task 2: Validate transition.md structural integrity after edit</name>
  <files>$HOME/.claude/get-shit-done/workflows/transition.md</files>
  <read_first>
    - $HOME/.claude/get-shit-done/workflows/transition.md (lines 480-620, verify structure)
  </read_first>
  <action>
Read the edited transition.md and verify structural integrity:

1. **Route A header preserved:** Line containing "**Route A: More phases remain in milestone**" exists
2. **New block present:** "**Always-on team execution (multi-phase):**" exists after Route A header
3. **Key new elements present:**
   - `roadmap analyze` command in a bash code block
   - "TeamCreate" mentioned for team spawning
   - "blockedBy" mentioned for dependency wiring
   - "milestone-execution" as team name
   - "isolation.*worktree" for member isolation
   - Sequential fallback: "If only 1 pending phase found"
   - Runtime fallback: "TeamCreate is not available"
4. **Old elements removed:**
   - No "Independence test" text
   - No "Parallel Execution Available" heading
   - No "Run in parallel" option
   - No "consent prompt" language
   - No "CRITICAL per D-12" paragraph (replaced by simpler fallback logic)
5. **Sequential Route A preserved:** "**Check if next phase has CONTEXT.md:**" exists after the new block
6. **Route B/B1 intact:** "**Route B1:**" and "**Route B:**" sections exist later in file
7. **No broken markdown:** No orphaned code fences, no duplicate headers

If any check fails, fix the issue by editing the file. If all checks pass, the task is done.
  </action>
  <acceptance_criteria>
    - grep "Route A: More phases remain" $HOME/.claude/get-shit-done/workflows/transition.md returns match
    - grep "Always-on team execution" $HOME/.claude/get-shit-done/workflows/transition.md returns match
    - grep "milestone-execution" $HOME/.claude/get-shit-done/workflows/transition.md returns match
    - grep "isolation.*worktree\|worktree.*isolation" $HOME/.claude/get-shit-done/workflows/transition.md returns match (note: may be on separate lines, so also check grep "isolation" and grep "worktree" independently)
    - grep "Check if next phase has CONTEXT.md" $HOME/.claude/get-shit-done/workflows/transition.md returns match
    - grep "Route B1" $HOME/.claude/get-shit-done/workflows/transition.md returns match
    - grep -c "Independence test" $HOME/.claude/get-shit-done/workflows/transition.md returns 0
  </acceptance_criteria>
  <verify>
    <automated>grep -q "Always-on team execution" "$HOME/.claude/get-shit-done/workflows/transition.md" && grep -q "milestone-execution" "$HOME/.claude/get-shit-done/workflows/transition.md" && grep -q "Check if next phase has CONTEXT.md" "$HOME/.claude/get-shit-done/workflows/transition.md" && grep -q "Route B1" "$HOME/.claude/get-shit-done/workflows/transition.md" && ! grep -q "Independence test" "$HOME/.claude/get-shit-done/workflows/transition.md" && echo "ALL CHECKS PASS"</automated>
  </verify>
  <done>
    - All structural integrity checks pass
    - Route A header, new always-team block, sequential fallback, and Route B/B1 are all intact
    - No old parallel detection artifacts remain
    - File is valid markdown with no broken fences or orphaned sections
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| transition.md → Claude runtime | Workflow prompt executed by Claude — no untrusted user input crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18.1-01 | T (Tampering) | transition.md | accept | File is in user's home directory, same trust level as all GSD workflows. No elevation of privilege. |
| T-18.1-02 | D (Denial of Service) | TeamCreate spawning | mitigate | Fallback to sequential if TeamCreate unavailable (D-11). Bounded by number of pending phases (typically 2-5). |
| T-18.1-03 | I (Information Disclosure) | roadmap analyze output | accept | ROADMAP.md is local project data, not sensitive. Output stays within Claude session. |
</threat_model>

<verification>
After both tasks complete:
1. `grep "Always-on team execution" "$HOME/.claude/get-shit-done/workflows/transition.md"` — returns match
2. `grep "blockedBy" "$HOME/.claude/get-shit-done/workflows/transition.md"` — returns match
3. `grep -c "Independence test" "$HOME/.claude/get-shit-done/workflows/transition.md"` — returns 0
4. `grep "Check if next phase has CONTEXT.md" "$HOME/.claude/get-shit-done/workflows/transition.md"` — returns match
5. Route A header, Route B, Route B1 all intact
</verification>

<success_criteria>
- transition.md Route A uses always-on TeamCreate model instead of opt-in parallel detection
- Dependencies from ROADMAP.md depends_on are mapped to TaskCreate blockedBy
- Sequential fallback preserved for single-phase and TeamCreate-unavailable scenarios
- All Route A content after the replaced block is untouched
- Route B and Route B1 are untouched
- Users still invoke /gsd-execute-phase N — change is transparent (per D-14)
</success_criteria>

<output>
After completion, create `.planning/phases/18.1-always-on-teamcreate-execution/18.1-01-SUMMARY.md`
</output>
