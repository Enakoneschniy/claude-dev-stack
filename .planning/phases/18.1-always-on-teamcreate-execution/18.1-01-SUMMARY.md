---
phase: 18.1-always-on-teamcreate-execution
plan: "01"
subsystem: gsd-workflow
tags: [teamcreate, transition, parallel-execution, dependency-scheduling]
dependency_graph:
  requires: []
  provides: [always-on-teamcreate-in-route-a]
  affects: [transition.md Route A]
tech_stack:
  added: []
  patterns: [TeamCreate, TaskCreate blockedBy, dependency-aware scheduling]
key_files:
  created: []
  modified:
    - $HOME/.claude/get-shit-done/workflows/transition.md
key_decisions:
  - "Always spawn TeamCreate for 2+ pending phases — no independence test, no consent prompt"
  - "Use TaskCreate blockedBy to map ROADMAP depends_on — dependency scheduling is automatic"
  - "Sequential fallback: 1 pending phase skips team spawning, falls through to existing Route A"
  - "TeamCreate unavailable: warn and fall through to existing sequential Route A"
metrics:
  duration: "~5 minutes"
  completed: "2026-04-13T12:37:56Z"
  tasks_completed: 2
  tasks_total: 2
  files_modified: 1
---

# Phase 18.1 Plan 01: Always-on TeamCreate Execution Summary

**One-liner:** Replace opt-in parallel detection with always-on TeamCreate for multi-phase milestone execution via dependency-aware TaskCreate blockedBy wiring.

## What Was Done

Modified `$HOME/.claude/get-shit-done/workflows/transition.md` Route A parallel block (lines 490–559). Replaced the "independence test + consent prompt + opt-in TeamCreate" model with an "always-on TeamCreate" model that spawns one team member per pending phase, wiring ROADMAP `depends_on` to TaskCreate `blockedBy` for automatic dependency scheduling.

## Tasks

| Task | Name | Status | Notes |
|------|------|--------|-------|
| 1 | Replace parallel detection block with always-team logic | Complete | transition.md lines 490-559 replaced |
| 2 | Validate transition.md structural integrity after edit | Complete | All checks passed |

Note: `transition.md` is in `$HOME/.claude/get-shit-done/` which is not a git repository. No per-task commits were possible — changes are captured in this SUMMARY commit.

## Verification Results

All acceptance criteria passed:

| Check | Result |
|-------|--------|
| `grep "Always-on team execution"` | 1 match |
| `grep "TeamCreate"` | 3 matches |
| `grep "blockedBy"` | 1 match |
| `grep "Independence test"` | 0 matches (removed) |
| `grep "Parallel Execution Available"` | 0 matches (removed) |
| `grep "Run in parallel"` | 0 matches (removed) |
| `grep "CRITICAL per D-12"` | 0 matches (removed) |
| `grep "Check if next phase has CONTEXT.md"` | 1 match (preserved) |
| `grep "Team execution not available"` | 1 match |
| `grep "Route A: More phases remain"` | 1 match (preserved) |
| `grep "milestone-execution"` | 2 matches |
| `grep "Route B1"` | 2 matches (preserved) |

## New Route A Logic (Always-on Team)

The replacement block:

1. Runs `roadmap analyze` to get all pending phases
2. **If 2+ pending phases:** spawns TeamCreate `milestone-execution` team with one member per phase; wires `depends_on` → `blockedBy` via TaskUpdate; members with no blockers start immediately
3. **If 1 pending phase:** skips team spawning, falls through to sequential Route A
4. **If TeamCreate unavailable:** warns and falls through to sequential Route A

## Deviations from Plan

None — plan executed exactly as written.

The only deviation from the commit protocol: `transition.md` is outside the git repository so per-task commits were not possible. The file modification is captured here.

## Self-Check

- [x] `$HOME/.claude/get-shit-done/workflows/transition.md` exists and contains new block
- [x] All grep acceptance criteria pass (verified above)
- [x] Route A header preserved at line 486
- [x] Sequential fallback `**Check if next phase has CONTEXT.md:**` preserved at line 555
- [x] Route B and Route B1 intact

## Self-Check: PASSED
