---
phase: 37-mcp-adapter
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/cds-cli/package.json
  - packages/cds-cli/src/mcp-server.ts
  - packages/cds-cli/src/mcp-tools/shared.ts
  - packages/cds-cli/src/index.ts
  - packages/cds-cli/src/mcp-server.test.ts
  - packages/cds-cli/src/mcp-server.integration.test.ts
  - packages/cds-cli/vitest.config.ts
  - NOTICES.md
autonomous: true
requirements:
  - MCP-01
user_setup: []
must_haves:
  truths:
    - "Running `node packages/cds-cli/dist/mcp-server.js` over stdio and sending a ListTools request returns 5 tool definitions"
    - "Each of the 5 tool definitions has name, description, and inputSchema conforming to MCP spec"
    - "Invalid tool name on CallTool throws McpError with code MethodNotFound"
    - "Process exits cleanly when stdin closes"
  artifacts:
    - path: "packages/cds-cli/src/mcp-server.ts"
      provides: "MCP server entry point with setRequestHandler for ListTools and CallTool over stdio"
      contains: "StdioServerTransport"
      min_lines: 80
    - path: "packages/cds-cli/src/mcp-tools/shared.ts"
      provides: "CdsMcpError base + 6 domain subclasses (NotAGsdProjectError, SessionNotFoundError, InvalidFilterError, RipgrepMissingError, VaultNotFoundError, FTS5UnavailableError)"
      contains: "class CdsMcpError extends McpError"
      min_lines: 40
    - path: "packages/cds-cli/package.json"
      provides: "Dependency on @modelcontextprotocol/sdk@^1.29.0 plus @cds/core workspace dep"
      contains: "@modelcontextprotocol/sdk"
    - path: "NOTICES.md"
      provides: "License attribution for @modelcontextprotocol/sdk (MIT)"
      contains: "@modelcontextprotocol/sdk"
    - path: "packages/cds-cli/src/mcp-server.integration.test.ts"
      provides: "End-to-end test spawning the server and asserting 5 tools via SDK Client"
      contains: "ListToolsRequestSchema"
  key_links:
    - from: "packages/cds-cli/src/mcp-server.ts"
      to: "packages/cds-cli/src/mcp-tools/shared.ts"
      via: "import { CdsMcpError, ErrorCode } from './mcp-tools/shared.js'"
      pattern: "from './mcp-tools/shared"
    - from: "packages/cds-cli/src/index.ts"
      to: "packages/cds-cli/src/mcp-server.ts"
      via: "export { main as mcpServerMain } from './mcp-server.js'"
      pattern: "export.*mcpServerMain.*from.*mcp-server"
---

<objective>
Create the MCP server scaffold: dependency wiring, stdio transport, tool-handler skeleton with 5 placeholder tool definitions, error class hierarchy, and a smoke integration test. No tool implementations yet — plans 02 and 03 fill those in.

Purpose: Establishes the runtime surface for MCP-01. Subsequent plans can plug tool handlers into the existing `switch` branch without re-doing SDK integration.
Output: A runnable `mcp-server.ts` that responds to ListTools and returns `MethodNotFound` on CallTool (until plans 02/03 land).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/37-mcp-adapter/37-CONTEXT.md
@.planning/phases/37-mcp-adapter/37-RESEARCH.md
@.planning/phases/37-mcp-adapter/37-PATTERNS.md
@.planning/phases/37-mcp-adapter/37-VALIDATION.md
@bin/cli.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 37-01-01: Install @modelcontextprotocol/sdk dependency + NOTICES entry</name>
  <files>packages/cds-cli/package.json, NOTICES.md</files>
  <read_first>packages/cds-cli/package.json, NOTICES.md, .planning/phases/37-mcp-adapter/37-RESEARCH.md, .planning/phases/37-mcp-adapter/37-CONTEXT.md</read_first>
  <action>
    Run `pnpm --filter @cds/cli add @modelcontextprotocol/sdk@^1.29.0` from repo root.
    Also ensure `packages/cds-cli/package.json` `dependencies` block contains `"@cds/core": "workspace:*"` (add if missing — Phase 35/34 dep).
    Do NOT touch root `package.json` `dependencies` or `bin` (Phase 33 D-03 constraint).
    Append to `NOTICES.md` the entry:
      `| @modelcontextprotocol/sdk | ^1.29.0 | MIT | https://github.com/modelcontextprotocol/typescript-sdk |`
    If `NOTICES.md` does not exist, create it with a header matching Phase 34 SDK-01 conventions: top-level `# Third-Party Notices` heading, then a table with columns `| Package | Version | License | Source |`, then the row above.
    If NOTICES.md exists, locate the existing table and append the row as the last row before any trailing blank lines. Do NOT duplicate if the row already exists (string-match on `@modelcontextprotocol/sdk`).
  </action>
  <verify>
    `grep -q '"@modelcontextprotocol/sdk"' packages/cds-cli/package.json`
    `grep -q '"@cds/core": "workspace:\*"' packages/cds-cli/package.json`
    `grep -q '@modelcontextprotocol/sdk' NOTICES.md`
    `pnpm install --frozen-lockfile=false` succeeds from repo root
  </verify>
  <acceptance_criteria>
    - `packages/cds-cli/package.json` dependencies contain `"@modelcontextprotocol/sdk"` key with value matching `^1.29.0` (or `^1.29.x`)
    - `packages/cds-cli/package.json` dependencies contain `"@cds/core": "workspace:*"`
    - Root `package.json` dependencies block is UNCHANGED (diff returns zero modifications to that file's `dependencies` object)
    - `NOTICES.md` contains a line matching regex `^\|\s*@modelcontextprotocol/sdk\s*\|` exactly once
    - `pnpm install` exits 0
  </acceptance_criteria>
  <done>SDK resolved in node_modules and pnpm-lock.yaml updated.</done>
</task>

<task type="auto">
  <name>Task 37-01-02: Create shared error hierarchy module</name>
  <files>packages/cds-cli/src/mcp-tools/shared.ts</files>
  <read_first>.planning/phases/37-mcp-adapter/37-RESEARCH.md, .planning/phases/37-mcp-adapter/37-CONTEXT.md, packages/cds-cli/tsconfig.json</read_first>
  <action>
    Create `packages/cds-cli/src/mcp-tools/shared.ts` that re-exports `ErrorCode` + `McpError` from `@modelcontextprotocol/sdk/types.js` and defines 7 classes:

    1. `class CdsMcpError extends McpError` — constructor signature `(code: ErrorCode, kind: string, message: string)`. Sets `this.data = { kind }` so clients can programmatic-match.
    2. `class NotAGsdProjectError extends CdsMcpError` — `super(ErrorCode.InvalidParams, 'NotAGsdProjectError', message)`
    3. `class SessionNotFoundError extends CdsMcpError` — `super(ErrorCode.InvalidParams, 'SessionNotFoundError', message)`
    4. `class InvalidFilterError extends CdsMcpError` — `super(ErrorCode.InvalidParams, 'InvalidFilterError', message)`
    5. `class RipgrepMissingError extends CdsMcpError` — `super(ErrorCode.InternalError, 'RipgrepMissingError', message)` — informational; tools that use this should handle it internally and fall through to grep.
    6. `class VaultNotFoundError extends CdsMcpError` — `super(ErrorCode.InvalidParams, 'VaultNotFoundError', message)`
    7. `class FTS5UnavailableError extends CdsMcpError` — `super(ErrorCode.InternalError, 'FTS5UnavailableError', message)`

    Also export a helper:
      `export function assertValidScopeBasename(name: string): void` — throws `InvalidFilterError` if `name` contains `/`, `\\`, `..`, starts with `~`, starts with `/`, or is empty/whitespace. Used by `docs.search` and `planning.status` in Plan 03.

    Use named exports only. No default export. ESM `.js` extensions in imports per NodeNext resolution (`@modelcontextprotocol/sdk/types.js`).
  </action>
  <verify>
    `pnpm --filter @cds/cli exec tsc --noEmit`
    `grep -c 'extends CdsMcpError' packages/cds-cli/src/mcp-tools/shared.ts` returns 6
  </verify>
  <acceptance_criteria>
    - File exists at `packages/cds-cli/src/mcp-tools/shared.ts`
    - File contains `class CdsMcpError extends McpError`
    - File contains `class NotAGsdProjectError`, `class SessionNotFoundError`, `class InvalidFilterError`, `class RipgrepMissingError`, `class VaultNotFoundError`, `class FTS5UnavailableError`
    - File contains `export function assertValidScopeBasename`
    - File imports `McpError, ErrorCode` from `@modelcontextprotocol/sdk/types.js`
    - `tsc --noEmit` on the cds-cli package exits 0
  </acceptance_criteria>
  <done>Error hierarchy compiles and is importable from sibling modules.</done>
</task>

<task type="auto">
  <name>Task 37-01-03: Scaffold mcp-server.ts with 5-tool ListTools registration + CallTool router skeleton</name>
  <files>packages/cds-cli/src/mcp-server.ts</files>
  <read_first>.planning/phases/37-mcp-adapter/37-RESEARCH.md, .planning/phases/37-mcp-adapter/37-CONTEXT.md, .planning/phases/37-mcp-adapter/37-PATTERNS.md, packages/cds-cli/src/mcp-tools/shared.ts, bin/cli.mjs</read_first>
  <action>
    Create `packages/cds-cli/src/mcp-server.ts` with this structure:

    1. Top-level ESM imports:
       - `Server` from `@modelcontextprotocol/sdk/server/index.js`
       - `StdioServerTransport` from `@modelcontextprotocol/sdk/server/stdio.js`
       - `ListToolsRequestSchema`, `CallToolRequestSchema`, `ErrorCode` from `@modelcontextprotocol/sdk/types.js`
       - `CdsMcpError`, error subclasses from `./mcp-tools/shared.js`
       - `McpError` from `@modelcontextprotocol/sdk/types.js`

    2. Export `async function main(args: string[]): Promise<void>`. Body:
       - Construct `const server = new Server({ name: 'cds', version: '1.0.0' }, { capabilities: { tools: {} } });`
       - Register ListTools handler that returns an object `{ tools: [...] }` with the 5 tool definitions below (each with exact `name`, human-readable `description`, and full `inputSchema` per CONTEXT D-77..D-81):

         a) `sessions.search` — inputSchema with `query: string (required)` + optional `filters` object containing `date_from`, `date_to`, `project`, `type: string[]`, `session_id`, `limit: number (default 20, max 100)`.
         b) `sessions.timeline` — `anchor_observation_id: number (required)`, `window_before: number (default 5, max 20)`, `window_after: number (default 5, max 20)`.
         c) `sessions.get_observations` — `ids: number[] (required, max 50 items)`, `format: 'raw' | 'summary' (default 'raw')`.
         d) `docs.search` — `query: string (required)`, `scope: string (default 'current')`, `limit: number (default 20, max 100)`.
         e) `planning.status` — `project?: string`.

         Each inputSchema is a JSON Schema draft-7 `{ type: 'object', properties: {...}, required: [...], additionalProperties: false }`.

       - Register CallTool handler that switches on `request.params.name`:
         - For each of the 5 tool names, call a stub function (`sessionsSearchStub`, `sessionsTimelineStub`, etc.) that throws `new McpError(ErrorCode.InternalError, 'Tool not yet implemented')`. These stubs exist only in this plan so the scaffold compiles and registers. Plan 02 replaces `sessions.*` stubs with real implementations; Plan 03 replaces `docs.search` and `planning.status`.
         - `default:` → `throw new McpError(ErrorCode.MethodNotFound, \`Unknown tool: \${name}\`)`.
         - Wrap in try/catch: if caught error is instance of `McpError` → re-throw; else `throw new McpError(ErrorCode.InternalError, err instanceof Error ? err.message : String(err))`.

       - Return the tool result as `{ content: [{ type: 'text', text: JSON.stringify(result) }] }`.

       - Create `const transport = new StdioServerTransport();` and `await server.connect(transport);`

    3. If invoked directly (top-of-file `if (import.meta.url === \`file://\${process.argv[1]}\`) { await main(process.argv.slice(2)); }`), run main and log unhandled errors to stderr + exit(1). Skip direct-invoke if any hosting harness imports the module — the guard protects against double-entry.

    Keep the file under 200 lines (scaffold only, tools externalized to plan 02/03 modules).
  </action>
  <verify>
    `pnpm --filter @cds/cli exec tsc --noEmit`
    `grep -c 'setRequestHandler' packages/cds-cli/src/mcp-server.ts` returns 2
    `grep -c "name: 'sessions.search'" packages/cds-cli/src/mcp-server.ts` returns 1
    `grep -c "name: 'planning.status'" packages/cds-cli/src/mcp-server.ts` returns 1
  </verify>
  <acceptance_criteria>
    - `packages/cds-cli/src/mcp-server.ts` exists
    - Contains `import { Server } from '@modelcontextprotocol/sdk/server/index.js'`
    - Contains `import { StdioServerTransport } from '@modelcontextprotocol/sdk/server/stdio.js'`
    - Contains the 5 tool names as string literals: `sessions.search`, `sessions.timeline`, `sessions.get_observations`, `docs.search`, `planning.status`
    - Contains `server.setRequestHandler(ListToolsRequestSchema, ...)` AND `server.setRequestHandler(CallToolRequestSchema, ...)`
    - Contains `new StdioServerTransport()` AND `server.connect(transport)`
    - Exports `async function main` (grep `export async function main` returns ≥ 1)
    - `tsc --noEmit` on the package exits 0
  </acceptance_criteria>
  <done>Server scaffold compiles and is the target for plan 02/03 handler fill-in.</done>
</task>

<task type="auto">
  <name>Task 37-01-04: Add named export from package index + vitest config</name>
  <files>packages/cds-cli/src/index.ts, packages/cds-cli/vitest.config.ts</files>
  <read_first>packages/cds-cli/src/index.ts, packages/cds-cli/package.json, packages/cds-cli/src/mcp-server.ts</read_first>
  <action>
    1. Append to `packages/cds-cli/src/index.ts`:
       `export { main as mcpServerMain } from './mcp-server.js';`
       If the file has existing exports, add this as a new line. If the file doesn't exist yet (unlikely post-Phase 33 but guard defensively), create it with just this export.

    2. Create `packages/cds-cli/vitest.config.ts` (if not present from Phase 33 MONO-03):
       ```ts
       import { defineConfig } from 'vitest/config';

       export default defineConfig({
         test: {
           globals: false,
           environment: 'node',
           include: ['src/**/*.test.ts'],
           testTimeout: 10000,
         },
       });
       ```
       If Phase 33 already created this file with different settings, extend rather than overwrite: just confirm `include` pattern matches `src/**/*.test.ts`. Do NOT introduce watch-mode defaults.
  </action>
  <verify>
    `grep -q 'mcpServerMain' packages/cds-cli/src/index.ts`
    `test -f packages/cds-cli/vitest.config.ts`
    `pnpm --filter @cds/cli exec vitest list` (should list test files without running — confirms config valid)
  </verify>
  <acceptance_criteria>
    - `packages/cds-cli/src/index.ts` contains `export { main as mcpServerMain } from './mcp-server.js'` (exact string)
    - `packages/cds-cli/vitest.config.ts` exists
    - `packages/cds-cli/vitest.config.ts` contains `include:` with `src/**/*.test.ts`
    - `packages/cds-cli/vitest.config.ts` does NOT contain `watch: true`
  </acceptance_criteria>
  <done>Package exposes mcpServerMain; vitest config ready for tests in plans 02/03/04.</done>
</task>

<task type="auto">
  <name>Task 37-01-05: Unit test — ListTools returns 5 tools, CallTool on unknown throws MethodNotFound</name>
  <files>packages/cds-cli/src/mcp-server.test.ts</files>
  <read_first>packages/cds-cli/src/mcp-server.ts, .planning/phases/37-mcp-adapter/37-RESEARCH.md, .planning/phases/37-mcp-adapter/37-VALIDATION.md</read_first>
  <action>
    Create `packages/cds-cli/src/mcp-server.test.ts`.

    This is a UNIT test, not integration — it exercises the ListTools/CallTool handlers by directly constructing a `Server` via the same factory logic as `main()`. To do that, refactor `main()` in `mcp-server.ts` to extract a pure function `createServer(): Server` that returns the configured Server WITHOUT connecting transport. Then `main()` becomes `const server = createServer(); const transport = new StdioServerTransport(); await server.connect(transport);`. This refactor is part of THIS task if not already in Task 37-01-03.

    Test cases (use `describe`/`it` from `vitest`):

    1. `it('ListTools returns exactly 5 tools')` — call the ListTools handler directly (via `server.request({ method: 'tools/list' }, ListToolsResultSchema)` OR by grabbing the registered handler). Assert `result.tools.length === 5`.
    2. `it('each tool has name, description, and inputSchema')` — iterate the 5 tools, assert presence of those 3 top-level fields.
    3. `it('tool names match the canonical set')` — assert the name set equals `new Set(['sessions.search', 'sessions.timeline', 'sessions.get_observations', 'docs.search', 'planning.status'])`.
    4. `it('CallTool with unknown name throws McpError MethodNotFound')` — call CallTool handler with `name: 'nonexistent'`, expect `McpError` with `code === ErrorCode.MethodNotFound`.
    5. `it('CallTool with known name throws InternalError (stub phase)')` — call with `name: 'sessions.search'`, expect `McpError` with message containing `not yet implemented`. (This test is REPLACED in plans 02/03 when real handlers land — note the expected-removal in a comment.)

    Use `vitest` globals-off pattern: `import { describe, it, expect } from 'vitest'`.
  </action>
  <verify>
    `pnpm --filter @cds/cli test src/mcp-server.test.ts`
    Test file exits 0 with 5 passing tests.
  </verify>
  <acceptance_criteria>
    - File `packages/cds-cli/src/mcp-server.test.ts` exists
    - Contains 5 `it(` blocks
    - `pnpm --filter @cds/cli test src/mcp-server.test.ts` exits 0
    - Test output includes the string `5 passed` (vitest summary)
    - `mcp-server.ts` exports both `main` AND `createServer` (needed by test)
  </acceptance_criteria>
  <done>Server scaffold is unit-tested; 5 tests green.</done>
</task>

<task type="auto">
  <name>Task 37-01-06: Integration test — spawn server over stdio, assert ListTools shape</name>
  <files>packages/cds-cli/src/mcp-server.integration.test.ts</files>
  <read_first>packages/cds-cli/src/mcp-server.ts, .planning/phases/37-mcp-adapter/37-RESEARCH.md</read_first>
  <action>
    Create `packages/cds-cli/src/mcp-server.integration.test.ts`.

    Uses the SDK's `Client` class (`@modelcontextprotocol/sdk/client/index.js`) + `StdioClientTransport` (`@modelcontextprotocol/sdk/client/stdio.js`) to spawn the server as a child process and exchange JSON-RPC.

    Steps:
    1. Build the package first as a prerequisite (Wave 0): run `pnpm --filter @cds/cli build` OR rely on vitest running from `src/` via tsx/vite-node loader. Choose the path that does not require a build step — use vitest's native TypeScript support.
    2. Alternative to building: run the server via `tsx` — in the test, spawn `npx tsx packages/cds-cli/src/mcp-server.ts` as the child. If `tsx` not available, fall back to spawning `node` against a freshly-built `dist/mcp-server.js` (add `beforeAll(() => pnpm --filter @cds/cli build)`).
    3. Connect SDK `Client` to child process via `StdioClientTransport({ command: 'npx', args: ['tsx', 'packages/cds-cli/src/mcp-server.ts'] })`. Set cwd to repo root.
    4. Send `ListToolsRequest` via `client.request(...)`. Assert `result.tools.length === 5`.
    5. Assert each tool has an `inputSchema` with `type === 'object'`.
    6. Close client cleanly.

    Test tolerance: if `tsx` is unavailable in the sandbox, the test SKIPS gracefully via `it.skipIf(!hasCommand('npx'))` pattern. Document skip condition inline.

    Add timeout: `it('spawns server and lists tools', { timeout: 20000 }, async () => { ... })`.
  </action>
  <verify>
    `pnpm --filter @cds/cli test src/mcp-server.integration.test.ts`
    Exits 0 with 1 passing (or 1 skipped with clear condition).
  </verify>
  <acceptance_criteria>
    - File `packages/cds-cli/src/mcp-server.integration.test.ts` exists
    - Contains `StdioClientTransport` or `StdioServerTransport` reference (uses SDK client channel)
    - Contains `result.tools.length` assertion
    - `pnpm --filter @cds/cli test src/mcp-server.integration.test.ts` exits 0
    - Either passes with 1 test or skips with clear reason (never fails)
  </acceptance_criteria>
  <done>Plans 02/03 have a spawn-based smoke test they can re-run to confirm regressions don't break the stdio wiring.</done>
</task>

</tasks>

<verification>
- [ ] `pnpm install` succeeds from repo root with new `@modelcontextprotocol/sdk` dep
- [ ] `pnpm --filter @cds/cli exec tsc --noEmit` passes
- [ ] `pnpm --filter @cds/cli test src/mcp-server.test.ts` — 5 passing
- [ ] `pnpm --filter @cds/cli test src/mcp-server.integration.test.ts` — 1 passing or skipped
- [ ] `NOTICES.md` lists `@modelcontextprotocol/sdk` MIT row
- [ ] No changes to root `package.json` `bin` or `dependencies` blocks
</verification>

<success_criteria>
- All 6 tasks completed
- `mcp-server.ts` scaffold compiles and registers 5 tool definitions
- Error hierarchy in `shared.ts` exports CdsMcpError + 6 subclasses + `assertValidScopeBasename`
- Unit tests (5 cases) all pass
- Integration test passes or skips gracefully
- `NOTICES.md` updated with SDK attribution
</success_criteria>

<output>
After completion, create `.planning/phases/37-mcp-adapter/37-01-SUMMARY.md` documenting:
- SDK version installed + pnpm-lock.yaml diff summary
- Final shape of `shared.ts` error hierarchy
- ListTools schema per tool (5 entries)
- Test counts (unit + integration)
- Any deviations from plan (e.g., if tsx not available and build step required)
</output>
