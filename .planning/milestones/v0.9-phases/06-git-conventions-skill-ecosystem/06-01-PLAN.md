---
phase: 06-git-conventions-skill-ecosystem
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/shared.mjs
  - tests/shared.test.mjs
  - tests/helpers/fixtures.mjs
  - tests/helpers/fixtures.test.mjs
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02

must_haves:
  truths:
    - "atomicWriteJson writes JSON atomically via temp+rename pattern"
    - "makeTempVault creates a valid vault structure in /tmp/cds-*"
    - "makeTempGitRepo creates a git repo with initial commit that works in CI"
    - "makeTempMonorepo(stackType) creates sentinel files for all 8 supported stack types"
    - "withStubBinary installs a bash stub on PATH, runs callback, restores PATH on exit"
    - "All /tmp/cds-* dirs are cleaned up after test completion"
  artifacts:
    - path: "lib/shared.mjs"
      provides: "atomicWriteJson export"
      exports: ["atomicWriteJson"]
    - path: "tests/helpers/fixtures.mjs"
      provides: "Shared test helpers for all v0.9 phases"
      exports: ["makeTempVault", "makeTempGitRepo", "makeTempMonorepo", "withStubBinary"]
    - path: "tests/helpers/fixtures.test.mjs"
      provides: "Self-tests proving fixtures work correctly"
      min_lines: 80
  key_links:
    - from: "lib/shared.mjs"
      to: "node:fs"
      via: "writeFileSync + renameSync for atomic pattern"
      pattern: "renameSync"
    - from: "tests/helpers/fixtures.mjs"
      to: "node:child_process"
      via: "execSync for git init in makeTempGitRepo"
      pattern: "execSync.*git init"
---

<objective>
Ship cross-cutting infrastructure that all Phase 6 plans and later v0.9 phases depend on: `atomicWriteJson()` in `lib/shared.mjs` and the `tests/helpers/fixtures.mjs` test helper module.

Purpose: Eliminate duplicated test boilerplate across 350+ tests and provide a safe JSON write primitive for all config files (git-scopes.json, notion_pages.json, notebooklm-manifest.json).
Output: Two new exports in shared.mjs, one new test helper module with 4 exports, self-tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-git-conventions-skill-ecosystem/06-RESEARCH.md

@lib/shared.mjs
@tests/shared.test.mjs
@tests/notebooklm.test.mjs (reference for withStubBinary pattern)
@tests/fixtures/notebooklm-stub.sh (reference for env-var-driven stub convention)
@lib/notebooklm-manifest.mjs (lines 245-252: existing atomic write pattern to extract)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add atomicWriteJson to lib/shared.mjs + tests</name>
  <files>lib/shared.mjs, tests/shared.test.mjs</files>
  <read_first>
    - lib/shared.mjs (full file -- see current exports, import list, code style)
    - tests/shared.test.mjs (full file -- see existing test patterns, describe/it structure)
    - lib/notebooklm-manifest.mjs (lines 240-253 -- existing atomic write to extract from)
  </read_first>
  <behavior>
    - Test: atomicWriteJson(filePath, obj) writes valid JSON with 2-space indent + trailing newline
    - Test: atomicWriteJson creates parent directories if they do not exist
    - Test: atomicWriteJson leaves no .tmp file after successful write
    - Test: atomicWriteJson result can be parsed back with JSON.parse and equals input object
    - Test: atomicWriteJson with nested object preserves structure
  </behavior>
  <action>
1. Add `renameSync` to the existing `import { existsSync, mkdirSync, readdirSync, writeFileSync } from 'fs';` line in lib/shared.mjs.

2. Add this export function at the end of lib/shared.mjs (before any closing comments), after the existing `mkdirp` export:

```javascript
export function atomicWriteJson(filePath, obj) {
  const dir = dirname(filePath);
  if (!existsSync(dir)) mkdirSync(dir, { recursive: true });
  const tmp = filePath + '.tmp';
  writeFileSync(tmp, JSON.stringify(obj, null, 2) + '\n', 'utf8');
  renameSync(tmp, filePath);
}
```

Note: `dirname` is already imported from 'path' in shared.mjs. `renameSync` is the only new import needed.

3. In tests/shared.test.mjs, add a new `describe('atomicWriteJson', ...)` block with tests for the 5 behaviors listed above. Use `mkdtempSync(join(tmpdir(), 'cds-atomic-'))` for temp dirs, clean up in `after()` with `rmSync`. Import `atomicWriteJson` from `../lib/shared.mjs`.
  </action>
  <verify>
    <automated>node --test tests/shared.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - lib/shared.mjs contains `export function atomicWriteJson(filePath, obj)`
    - lib/shared.mjs import line contains `renameSync`
    - tests/shared.test.mjs contains `describe('atomicWriteJson'`
    - tests/shared.test.mjs contains at least 4 `it(` calls inside the atomicWriteJson describe block
    - `node --test tests/shared.test.mjs` exits 0
    - No `.tmp` files remain after test run in the temp directory
  </acceptance_criteria>
  <done>atomicWriteJson exported from lib/shared.mjs, 5 tests passing, no .tmp residue</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create tests/helpers/fixtures.mjs + self-tests</name>
  <files>tests/helpers/fixtures.mjs, tests/helpers/fixtures.test.mjs</files>
  <read_first>
    - tests/notebooklm.test.mjs (lines 0-55 -- before/after pattern for PATH stub, exact stub setup)
    - tests/fixtures/notebooklm-stub.sh (env-var-driven stub convention: STUB_STDOUT, STUB_EXIT, STUB_STDERR)
    - lib/shared.mjs (mkdirp export for reference)
  </read_first>
  <behavior>
    - Test: makeTempVault() creates dir with meta/ and projects/ subdirectories
    - Test: makeTempVault().cleanup() removes the directory entirely
    - Test: makeTempGitRepo() creates dir with .git/ and at least one commit
    - Test: makeTempGitRepo() works without global git config (CI-safe via GIT_AUTHOR_NAME env)
    - Test: makeTempGitRepo().cleanup() removes the directory
    - Test: makeTempMonorepo('pnpm-workspace') creates pnpm-workspace.yaml + apps/ dirs
    - Test: makeTempMonorepo('npm-workspaces') creates package.json with workspaces field + dirs
    - Test: makeTempMonorepo('cargo-workspace') creates Cargo.toml with [workspace] + crates/ dir
    - Test: makeTempMonorepo('single-package') creates dir with no sentinel files
    - Test: withStubBinary('mycmd', 'echo hello', fn) makes `which mycmd` resolve inside fn
    - Test: withStubBinary restores original PATH after fn completes
    - Test: withStubBinary restores PATH even if fn throws
    - Test: withStubBinary cleans up stub directory in /tmp
  </behavior>
  <action>
Create `tests/helpers/` directory, then create `tests/helpers/fixtures.mjs` with these 4 exports:

**makeTempVault():**
- Uses `mkdtempSync(join(tmpdir(), 'cds-vault-'))` to create temp dir
- Creates `meta/` and `projects/` subdirs inside it
- Returns `{ dir, cleanup }` where cleanup does `rmSync(dir, { recursive: true, force: true })`

**makeTempGitRepo():**
- Uses `mkdtempSync(join(tmpdir(), 'cds-git-'))` to create temp dir
- Runs `git init` via `execSync('git init', { cwd: dir, stdio: 'pipe' })`
- Runs `git commit --allow-empty -m "init"` with explicit env vars: `GIT_AUTHOR_NAME: 'test'`, `GIT_AUTHOR_EMAIL: 'test@test.com'`, `GIT_COMMITTER_NAME: 'test'`, `GIT_COMMITTER_EMAIL: 'test@test.com'` (CI-safe, no global git config required)
- Returns `{ dir, cleanup }`

**makeTempMonorepo(stackType):**
- Uses `mkdtempSync(join(tmpdir(), 'cds-mono-${stackType}-'))` (template literal with stackType)
- Switch on stackType to create sentinel files:
  - `'pnpm-workspace'`: write `pnpm-workspace.yaml` with content `packages:\n  - apps/*\n  - packages/*\n`, create dirs `apps/web/`, `apps/api/`, `packages/ui/`
  - `'npm-workspaces'`: write `package.json` with `{"workspaces":["apps/*","packages/*"]}`, create dirs `apps/web/`, `packages/core/`
  - `'lerna'`: write `lerna.json` with `{"packages":["packages/*"]}`, create dir `packages/core/`
  - `'nx'`: write `nx.json` with `{}`, create dirs `apps/web/`, `packages/core/`
  - `'turborepo'`: write `turbo.json` with `{}`, write `package.json` with `{"workspaces":["apps/*","packages/*"]}`, create dirs `apps/web/`, `packages/ui/`
  - `'cargo-workspace'`: write `Cargo.toml` with `[workspace]\nmembers = ["crates/core", "crates/api"]`, create dirs `crates/core/`, `crates/api/`
  - `'go-multi-module'`: create dirs `cmd/server/`, `pkg/core/`, write `cmd/server/go.mod` and `pkg/core/go.mod` each with `module example.com/x`
  - `'python-uv'`: write `pyproject.toml` with `[tool.uv.workspace]\nmembers = ["packages/*"]`, create dir `packages/core/`
  - `'single-package'`: no sentinel files, just the empty dir
  - Default: throw `Error('Unknown stack type: ${stackType}')`
- Returns `{ dir, cleanup }`

**withStubBinary(name, scriptContent, fn):**
- Creates temp dir via `mkdtempSync(join(tmpdir(), 'cds-stub-'))`
- Writes `#!/bin/sh\n${scriptContent}` to `join(dir, name)`, chmod 0o755
- Saves `process.env.PATH`, prepends stub dir: `process.env.PATH = dir + delimiter + originalPath`
- Calls `fn(dir)` inside try block
- In finally: restores `process.env.PATH = originalPath`, calls `rmSync(dir, { recursive: true, force: true })`
- Returns result of `fn(dir)`

Imports needed: `mkdtempSync, mkdirSync, writeFileSync, chmodSync, rmSync, existsSync` from `'fs'`; `join, delimiter` from `'path'`; `tmpdir` from `'os'`; `execSync` from `'child_process'`.

Then create `tests/helpers/fixtures.test.mjs` with `describe` blocks for each of the 4 exports, testing the 13 behaviors listed above. Use `node:test` + `node:assert/strict`. Each test should call cleanup() and verify the dir no longer exists.
  </action>
  <verify>
    <automated>node --test tests/helpers/fixtures.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - tests/helpers/fixtures.mjs contains `export function makeTempVault(`
    - tests/helpers/fixtures.mjs contains `export function makeTempGitRepo(`
    - tests/helpers/fixtures.mjs contains `export function makeTempMonorepo(`
    - tests/helpers/fixtures.mjs contains `export function withStubBinary(`
    - tests/helpers/fixtures.test.mjs contains `describe('makeTempVault'`
    - tests/helpers/fixtures.test.mjs contains `describe('withStubBinary'`
    - tests/helpers/fixtures.test.mjs contains at least 10 `it(` calls
    - `node --test tests/helpers/fixtures.test.mjs` exits 0
    - No /tmp/cds-* directories remain after test completion
  </acceptance_criteria>
  <done>All 4 fixture helpers exported, 13+ self-tests passing, cleanup verified, CI-safe git identity</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test temp dirs | Tests create dirs in /tmp; cleanup must be reliable |
| PATH mutation in withStubBinary | Temporary PATH change could leak if cleanup fails |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | T (Tampering) | atomicWriteJson | mitigate | Write to .tmp first, then renameSync (POSIX atomic on same filesystem); readers never see partial JSON |
| T-06-02 | D (DoS) | makeTempVault/Repo | accept | /tmp dirs cleaned in finally blocks; CI ephemeral runners discard /tmp anyway |
| T-06-03 | T (Tampering) | withStubBinary | mitigate | PATH restored in finally block even on exception; stub dir removed with rmSync force |
</threat_model>

<verification>
- `node --test tests/shared.test.mjs` passes (atomicWriteJson tests)
- `node --test tests/helpers/fixtures.test.mjs` passes (fixture self-tests)
- `npm test` passes (full suite including new tests, no regressions)
- No /tmp/cds-* dirs accumulate after test run
</verification>

<success_criteria>
- atomicWriteJson exported from lib/shared.mjs and used by later plans
- 4 fixture helpers exported from tests/helpers/fixtures.mjs
- 18+ new tests passing (5 atomic + 13 fixtures)
- Full test suite green (264 + new tests)
</success_criteria>

<output>
After completion, create `.planning/phases/06-git-conventions-skill-ecosystem/06-01-SUMMARY.md`
</output>
