---
phase: 05-cli-integration-trigger-wizard
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - hooks/notebooklm-sync-trigger.mjs
  - hooks/notebooklm-sync-runner.mjs
  - hooks/session-end-check.sh
  - tests/hooks.test.mjs
  - tests/fixtures/notebooklm-sync-stub.sh
autonomous: true
requirements:
  - NBLM-21
  - NBLM-22
  - NBLM-23
tags:
  - notebooklm
  - hooks
  - session-lifecycle
  - background-sync
user_setup: []

must_haves:
  truths:
    - "Session-end hook (hooks/session-end-check.sh) invokes the NotebookLM trigger AFTER context update and BEFORE vault git push (D-07)."
    - "Trigger (hooks/notebooklm-sync-trigger.mjs) exits in under 1 second even when the runner would take 30+ seconds — parent process is never blocked on network I/O (NBLM-22)."
    - "Trigger short-circuits with exit 0 when `notebooklm` binary is not in PATH (NBLM-21/23 'feature not configured')."
    - "Runner (hooks/notebooklm-sync-runner.mjs) runs `notebooklm auth check` via Phase 2 spawn; if exit is non-zero it writes '[info] sync skipped reason=auth-check-failed' and exits 0 without calling syncVault."
    - "Runner calls syncVault directly (imports from lib/notebooklm-sync.mjs — ADR-0011 avoids PATH dependency on global CLI install)."
    - "Runner writes exactly one result line to `~/vault/.notebooklm-sync.log` per invocation: `[info] sync done uploaded=N skipped=M failed=K duration=Tms` OR `[error] sync failed reason=...`."
    - "Runner NEVER propagates errors: process.on('uncaughtException') writes `[error] crash: ...` to log and calls process.exit(0) — no session-end UI noise (NBLM-23)."
    - "Runner exit code is always 0 regardless of sync outcome."
    - "Bash hook invocation wraps the trigger in `2>/dev/null || true` — double safety against any trigger crash leaking to session-end stderr."
    - "Runner imports Phase 2 error classes (NotebooklmCliError, NotebooklmRateLimitError, NotebooklmNotInstalledError) and logs them at appropriate levels (info for Not-Installed/Rate-Limit, error for unexpected CliError)."
    - "Log entries follow D-14 format: `{ISO timestamp} [level] message key=val` (plain text, single-line, append-only)."
  artifacts:
    - path: "hooks/notebooklm-sync-trigger.mjs"
      provides: "Fire-and-forget launcher — spawn detached runner, exit 0 immediately"
      min_lines: 40
    - path: "hooks/notebooklm-sync-runner.mjs"
      provides: "Detached subprocess — auth check + syncVault + log append"
      min_lines: 80
    - path: "hooks/session-end-check.sh"
      provides: "Extended hook — invokes trigger between context update and vault push"
      contains: "notebooklm-sync-trigger.mjs"
    - path: "tests/hooks.test.mjs"
      provides: "Extended integration tests for trigger non-blocking behavior + runner best-effort"
      contains: "notebooklm-sync-trigger"
    - path: "tests/fixtures/notebooklm-sync-stub.sh"
      provides: "Extended argv-aware stub with `auth check` mode controlled by NOTEBOOKLM_SYNC_STUB_AUTH_EXIT"
      contains: "auth"
  key_links:
    - from: "hooks/session-end-check.sh"
      to: "hooks/notebooklm-sync-trigger.mjs"
      via: "VAULT_PATH=... node trigger 2>/dev/null || true (after context-update line ~39, before vault push line ~42)"
      pattern: "notebooklm-sync-trigger\\.mjs"
    - from: "hooks/notebooklm-sync-trigger.mjs"
      to: "hooks/notebooklm-sync-runner.mjs"
      via: "spawn(process.execPath, [runner], { detached: true, stdio: ['ignore', logFd, logFd] }).unref()"
      pattern: "detached:\\s*true"
    - from: "hooks/notebooklm-sync-runner.mjs"
      to: "lib/notebooklm-sync.mjs::syncVault"
      via: "direct import + await syncVault(opts)"
      pattern: "from.*notebooklm-sync\\.mjs"
    - from: "hooks/notebooklm-sync-runner.mjs"
      to: "~/vault/.notebooklm-sync.log"
      via: "fs.appendFileSync on every log line"
      pattern: "appendFileSync.*notebooklm-sync\\.log"
---

<objective>
Wire the session-end auto-sync trigger. Creates two new hook files (trigger + runner) following ADR-0011 split pattern and Phase 1's `hooks/update-context.mjs` architectural template. Extends `hooks/session-end-check.sh` with 3-5 lines to invoke the trigger. Extends tests/hooks.test.mjs with integration tests that use REAL `spawn` + `.unref()` (mocking would defeat NBLM-22 verification — research §Test Strategy §Anti-sampling note).

Purpose: After every session end, the vault auto-syncs to NotebookLM in the background, invisible to the user on failure. Users without notebooklm-py installed see zero impact.
Output: 2 new hook files (~140 LoC combined), 3-5 line extension to session-end-check.sh, ~10-12 new tests in tests/hooks.test.mjs, extended stub fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-cli-integration-trigger-wizard/05-CONTEXT.md
@.planning/phases/05-cli-integration-trigger-wizard/05-RESEARCH.md
@.planning/phases/05-cli-integration-trigger-wizard/05-VALIDATION.md
@.planning/phases/01-fix-session-manager-context-auto-update/01-02-SUMMARY.md
@.planning/phases/02-notebooklm-api-client/02-02-SUMMARY.md
@.planning/phases/04-vault-notebooklm-sync-pipeline/04-02-SUMMARY.md
@hooks/session-end-check.sh
@hooks/update-context.mjs
@lib/notebooklm.mjs
@lib/notebooklm-sync.mjs
@lib/shared.mjs
@tests/hooks.test.mjs
@tests/fixtures/notebooklm-sync-stub.sh

<interfaces>
<!-- Contracts the executor must consume without re-exploring. -->

From lib/notebooklm-sync.mjs (Phase 4, SHIPPED):
  export async function syncVault(opts = {}) { /* returns stats or throws */ }
  // opts: { vaultRoot?: string, notebookName?: string, dryRun?: boolean }
  // Runner will call: syncVault({ vaultRoot, notebookName }) — no dryRun
  // Returns D-16 shape (uploaded, skipped, failed, errors, durationMs, notebookId, rateLimited)
  // Throws: Error('Vault not found'), NotebooklmRateLimitError (on ensureNotebook rate limit abort),
  //         any unexpected CliError propagated from syncOneFile.
  // NOTE: individual per-file CliErrors are collected in stats.errors[] — not thrown.

From lib/notebooklm.mjs (Phase 2, SHIPPED):
  export class NotebooklmNotInstalledError extends Error { }
  export class NotebooklmCliError extends Error { command; exitCode; stderr; }
  export class NotebooklmRateLimitError extends NotebooklmCliError { }
  // For runner: catch these separately, log info/error accordingly.

From hooks/update-context.mjs (Phase 1 template — pattern to mirror):
- `#!/usr/bin/env node` shebang
- Reads VAULT_PATH + CDS_PROJECT_NAME from env
- Has a main() function that exits 0 on non-fatal errors, 2 on programmer errors
- Uses process.stderr.write for diagnostic messages kept silent via 2>/dev/null in hook

From hooks/session-end-check.sh (Phase 1 SHIPPED — lines 29-52, structure):
- Outer: `if ls "$SESSION_DIR/$TODAY"*.md 1>/dev/null 2>&1; then`
- Inside: lines 31-40 are the update-context invocation block
  - Re-derives SCRIPT_DIR via cd/dirname at line 35
  - Calls `VAULT_PATH=... CDS_PROJECT_NAME=... node "$SCRIPT_DIR/update-context.mjs" $LOG 2>/dev/null || true`
- Lines 42-50 are the vault auto-push block (git add/commit/push wrapped in `.git` existence guard)
- Phase 5 inserts the trigger invocation BETWEEN these two blocks (D-07 ordering)

Detached spawn pattern (runtime-verified in research §R5):
  import { spawn } from 'node:child_process';
  import { openSync } from 'node:fs';

  const logFd = openSync(logPath, 'a');
  const child = spawn(process.execPath, [runnerPath], {
    detached: true,
    stdio: ['ignore', logFd, logFd],
    env: { ...process.env }
  });
  child.unref();
  process.exit(0);
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create hooks/notebooklm-sync-trigger.mjs (fire-and-forget launcher) + extend stub fixture</name>
  <files>hooks/notebooklm-sync-trigger.mjs, tests/fixtures/notebooklm-sync-stub.sh</files>
  <read_first>
    - hooks/update-context.mjs (full file — architectural template)
    - .planning/phases/05-cli-integration-trigger-wizard/05-RESEARCH.md §R5 detached spawn verified (lines 177-201)
    - .planning/phases/05-cli-integration-trigger-wizard/05-CONTEXT.md §D-04, §D-06
    - lib/shared.mjs::hasCommand (lines 48-50)
    - tests/fixtures/notebooklm-sync-stub.sh (understand argv-aware dispatch before extending)
  </read_first>
  <behavior>
    - Test: trigger with `notebooklm` NOT in PATH → exits 0 in <1 second, no child spawned.
    - Test: trigger with `notebooklm` in PATH (fixture stub) → exits 0 in <1 second, child is spawned (verified via log file sentinel).
    - Test: trigger non-blocking — wall clock time from spawn to exit < 1000ms.
    - Test: stub fixture supports `auth check` subcommand via NOTEBOOKLM_SYNC_STUB_AUTH_EXIT env var.
  </behavior>
  <action>
    Create **hooks/notebooklm-sync-trigger.mjs** (~50-60 LoC) using the research-verified detached spawn pattern. Required contract:

    1. `#!/usr/bin/env node` shebang at line 1 (matches Phase 1 `hooks/update-context.mjs`).
    2. Inline `hasCommandInline(name)` helper that uses `execSync` with a hardcoded `which <name>` (no user input concatenated) to avoid importing `lib/shared.mjs` — keeps trigger startup fast and dependency-free.
    3. `main()` body sequence:
       a. If `hasCommandInline('notebooklm')` is false → `process.exit(0)` silently.
       b. Resolve `vaultRoot = process.env.VAULT_PATH || join(homedir(), 'vault')`; if `!existsSync(vaultRoot)` → `process.exit(0)`.
       c. Compute `logPath = join(vaultRoot, '.notebooklm-sync.log')` and open it with `openSync(logPath, 'a')` to get an FD (research §R5 requires integer fd, not string).
       d. Resolve sibling runner path via `new URL('./notebooklm-sync-runner.mjs', import.meta.url).pathname`. If runner file missing → `process.exit(0)`.
       e. Spawn detached with exact options: `{ detached: true, stdio: ['ignore', logFd, logFd], env: { ...process.env, VAULT_PATH: vaultRoot } }`.
       f. Call `child.unref()` then `process.exit(0)`.
    4. Wrap entire `main()` in try/catch — any unhandled error still exits 0 (NBLM-23 no terminal noise).
    5. NO `lib/*.mjs` imports (trigger must be fast + self-contained).

    **Extend tests/fixtures/notebooklm-sync-stub.sh** with auth check handling. Read the current stub structure first — it is argv-aware per Phase 4. Add a new dispatch branch at the TOP of the dispatch logic (before existing branches so `auth` never falls through):

    - When `$1 == "auth"` and `$2 == "check"`:
      - Read `EXIT=${NOTEBOOKLM_SYNC_STUB_AUTH_EXIT:-0}`
      - If `$EXIT == "0"` → `echo '{"status":"ok","checks":{}}'` and `exit 0`
      - Else → `echo "Authentication failed" >&2` and `exit "$EXIT"`

    Do NOT break existing Phase 4 stub behavior. After the edit, run `node --test tests/notebooklm-sync.test.mjs` to confirm all 32 Phase 4 tests still pass.

    **Add tests to tests/hooks.test.mjs** — new describe block `notebooklm-sync-trigger` with ~4 tests:
    1. Trigger with empty PATH → exits 0 quickly
    2. Trigger with stub in PATH → exits 0 AND wall-clock elapsed < 1000ms
    3. Trigger spawns runner (sentinel detection: wait up to 500ms post-exit and check log file appears)
    4. Trigger with missing VAULT_PATH → exits 0 (graceful skip)

    Use `spawnSync(process.execPath, [triggerPath], { env: {...}, encoding: 'utf8' })` to invoke. Capture `Date.now()` before/after for wall-clock assertion.
  </action>
  <verify>
    <automated>node --test tests/hooks.test.mjs tests/notebooklm-sync.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `test -f hooks/notebooklm-sync-trigger.mjs` — file exists
    - `head -1 hooks/notebooklm-sync-trigger.mjs` — begins with `#!/usr/bin/env node`
    - `grep -c "detached: true" hooks/notebooklm-sync-trigger.mjs` — exactly 1
    - `grep -c "\.unref()" hooks/notebooklm-sync-trigger.mjs` — exactly 1
    - `grep -c "stdio: \['ignore'" hooks/notebooklm-sync-trigger.mjs` — exactly 1
    - `grep -c "process.exit(0)" hooks/notebooklm-sync-trigger.mjs` — at least 3 (binary-absent, vault-absent, runner-absent guards)
    - `grep -Ec "process.exit\\([1-9]" hooks/notebooklm-sync-trigger.mjs` — 0 (never non-zero)
    - `grep -c "from '\\.\\./lib/" hooks/notebooklm-sync-trigger.mjs` — 0 (no lib imports — self-contained)
    - `grep -c "auth" tests/fixtures/notebooklm-sync-stub.sh` — at least 1 (auth mode added)
    - `grep -c "NOTEBOOKLM_SYNC_STUB_AUTH_EXIT" tests/fixtures/notebooklm-sync-stub.sh` — at least 1
    - `node --test tests/notebooklm-sync.test.mjs` — Phase 4 tests all pass (stub extension additive)
    - `node --test tests/hooks.test.mjs` — new trigger tests pass
    - Trigger wall-clock test passes (<1000ms even when stub would take 5+s)
  </acceptance_criteria>
  <done>
    Trigger file ships. Binary-absent path is silent. Binary-present path spawns detached. Fixture stub supports auth check. Phase 4 stub-dependent tests still pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create hooks/notebooklm-sync-runner.mjs (detached subprocess — auth check + syncVault + log)</name>
  <files>hooks/notebooklm-sync-runner.mjs, tests/hooks.test.mjs</files>
  <read_first>
    - hooks/notebooklm-sync-trigger.mjs (from Task 1)
    - .planning/phases/05-cli-integration-trigger-wizard/05-RESEARCH.md §D-05 runner behavior, §R1 auth check exit semantics, §Claude's Discretion Resolutions (runner crash handling)
    - .planning/phases/05-cli-integration-trigger-wizard/05-CONTEXT.md §D-05, §D-14 log format
    - lib/notebooklm-sync.mjs (syncVault signature + stats shape lines 380-466)
    - lib/notebooklm.mjs (error classes)
  </read_first>
  <behavior>
    - Test: runner with auth stub exit 0 AND syncVault-compatible stub → log has `[info] sync start` + `[info] sync done uploaded=N skipped=M failed=K duration=Tms`; exits 0.
    - Test: runner with auth stub exit 1 → log has `[info] sync skipped reason=auth-check-failed`; no syncVault invocation; exits 0.
    - Test: runner with syncVault throwing unexpected Error → log has `[error] sync failed reason=...`; exits 0.
    - Test: runner with `process.on('uncaughtException')` path (simulated) → log has `[error] crash: ...`; exits 0.
    - Test: log format is plain text, ISO timestamp + level + message.
    - Test: multiple runner invocations append (no truncation).
  </behavior>
  <action>
    Create **hooks/notebooklm-sync-runner.mjs** (~100-120 LoC). Required contract:

    1. `#!/usr/bin/env node` shebang at line 1.
    2. Imports: `spawnSync` from `node:child_process`; `appendFileSync`, `existsSync` from `node:fs`; `homedir` from `node:os`; `join`, `basename` from `node:path`; `syncVault` from `../lib/notebooklm-sync.mjs`; `NotebooklmNotInstalledError`, `NotebooklmRateLimitError`, `NotebooklmCliError` from `../lib/notebooklm.mjs`.
    3. Module-scope constants resolved early so uncaughtException handler can access them:
       - `VAULT_ROOT = process.env.VAULT_PATH || join(homedir(), 'vault')`
       - `LOG_PATH = join(VAULT_ROOT, '.notebooklm-sync.log')`
    4. `appendLogLine(level, message, kv = {})` helper — composes `{ISO} [level] message key=val ...` per D-14. Must be wrapped in try/catch so log-write failures drop silently (best-effort).
    5. `truncate(s, max = 200)` helper — caps `reason` strings at 200 chars so subprocess stderr (possibly containing vault content) is clipped before logging. T-05-10 mitigation.
    6. **Install handlers BEFORE main() runs:**
       - `process.on('uncaughtException', (err) => { appendLogLine('error', 'crash:', { reason: truncate(err.message ?? String(err)) }); process.exit(0); });`
       - `process.on('unhandledRejection', (reason) => { appendLogLine('error', 'crash:', { reason: truncate(String(reason)) }); process.exit(0); });`
    7. `async function main()` body:
       a. `appendLogLine('info', 'sync start', { project: basename(VAULT_ROOT) })`
       b. If `!existsSync(VAULT_ROOT)` → log `sync skipped reason=vault-not-found` → `process.exit(0)`.
       c. Auth check: `const authResult = spawnSync('notebooklm', ['auth', 'check'], { stdio: ['ignore', 'pipe', 'pipe'], encoding: 'utf8' })`. Use `{ stdio: ['ignore', 'pipe', 'pipe'] }` exactly — never `'inherit'` (detached context).
       d. If `authResult.error || authResult.status !== 0` → log `sync skipped reason=auth-check-failed` → `process.exit(0)`.
       e. Call `stats = await syncVault({ vaultRoot: VAULT_ROOT })` inside try/catch.
       f. On success: if `stats.rateLimited` → log `sync rate-limited`; else log `sync done uploaded=N skipped=M failed=K duration=Tms`. If `stats.errors.length > 0` → additional `[warn] sync partial errorCount=N` entry.
       g. On catch: branch by instanceof:
          - `NotebooklmNotInstalledError` → `[info] sync skipped reason=binary-missing` (defense-in-depth — trigger filtered but be safe)
          - `NotebooklmRateLimitError` → `[info] sync rate-limited reason=...`
          - `NotebooklmCliError` → `[error] sync failed reason=... exitCode=...`
          - Any other → `[error] sync failed reason=...`
       h. `process.exit(0)` at the end of main().
    8. `main().catch(() => process.exit(0))` — last line of defense.

    **CRITICAL — no thrown errors escape.** `grep "throw " hooks/notebooklm-sync-runner.mjs` must return 0.

    **Add tests to tests/hooks.test.mjs** — new describe block `notebooklm-sync-runner` with ~6-8 tests:

    1. Successful sync path: auth stub exit 0 + sync stub returning minimal success → log contains `sync start` and `sync done`
    2. Auth failure path: `NOTEBOOKLM_SYNC_STUB_AUTH_EXIT=1` → log contains `sync skipped reason=auth-check-failed`, no `sync done` line, runner exit 0
    3. syncVault throws unexpected → log contains `[error] sync failed` OR `[error] crash:`, runner exit 0
    4. Vault not found → log contains `sync skipped reason=vault-not-found`, runner exit 0
    5. Multiple invocations append: run runner twice → log has ≥2 `sync start` lines
    6. Log format: every line matches `/^\\d{4}-\\d{2}-\\d{2}T.*\\Z \\[(info|warn|error)\\]/`
    7. Runner exit code is 0 on every tested branch (already implied by tests above, but add explicit `assert.equal(result.status, 0)` in each)

    Use `spawnSync(process.execPath, [runnerPath], { env: {...}, encoding: 'utf8' })` with PATH prefixed by stub dir. For tests 3 (syncVault throw), drive the stub via env vars that make the list/upload subcommands fail (existing Phase 4 stub behavior).
  </action>
  <verify>
    <automated>node --test tests/hooks.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `test -f hooks/notebooklm-sync-runner.mjs` — file exists
    - `head -1 hooks/notebooklm-sync-runner.mjs` — begins with `#!/usr/bin/env node`
    - `grep -c "from '\\.\\./lib/notebooklm-sync\\.mjs'" hooks/notebooklm-sync-runner.mjs` — exactly 1 (direct import per Claude's Discretion)
    - `grep -c "auth" hooks/notebooklm-sync-runner.mjs` — at least 1 (auth check call)
    - `grep -c "uncaughtException" hooks/notebooklm-sync-runner.mjs` — exactly 1
    - `grep -c "unhandledRejection" hooks/notebooklm-sync-runner.mjs` — exactly 1
    - `grep -c "process.exit(0)" hooks/notebooklm-sync-runner.mjs` — at least 4 (every exit path is 0)
    - `grep -Ec "process.exit\\([1-9]" hooks/notebooklm-sync-runner.mjs` — 0
    - `grep -c "^throw \\|^  throw \\| throw " hooks/notebooklm-sync-runner.mjs` — 0 (no throws that escape)
    - `grep -c "NotebooklmRateLimitError\\|NotebooklmCliError\\|NotebooklmNotInstalledError" hooks/notebooklm-sync-runner.mjs` — at least 3 (instanceof branches)
    - `grep -c "appendFileSync" hooks/notebooklm-sync-runner.mjs` — at least 1 (log append)
    - `node --test tests/hooks.test.mjs` — all new runner tests pass
  </acceptance_criteria>
  <done>
    Runner ships. Auth check gates syncVault. All error paths → log entry + exit 0. uncaughtException path exercised. Tests green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire trigger into hooks/session-end-check.sh + end-to-end hook integration tests</name>
  <files>hooks/session-end-check.sh, tests/hooks.test.mjs</files>
  <read_first>
    - hooks/session-end-check.sh (full file — line 29-52 is the `ls *.md` branch)
    - .planning/phases/05-cli-integration-trigger-wizard/05-CONTEXT.md §D-07 ordering
    - .planning/phases/05-cli-integration-trigger-wizard/05-RESEARCH.md §Session-End-Check.sh Integration (lines 423-445)
    - tests/hooks.test.mjs (understand existing test patterns before extending)
  </read_first>
  <behavior>
    - Test: source-level ordering — `update-context.mjs` appears BEFORE `notebooklm-sync-trigger.mjs` appears BEFORE `git -C "$VAULT" push`.
    - Test: trigger invocation line contains `2>/dev/null || true` suffix.
    - Test: `bash -n hooks/session-end-check.sh` passes (syntax valid).
    - Test: running the hook end-to-end with stub binary in PATH and valid vault/session-log → exit 0 AND log file gains an entry.
    - Test: running the hook when trigger file is absent (rename it temporarily) → exit 0 (graceful `if [ -f ]` guard).
  </behavior>
  <action>
    **Modify hooks/session-end-check.sh.** Read lines 29-52 carefully. Insert the trigger invocation AFTER the closing `fi` of the update-context.mjs block (currently ~line 40) and BEFORE the vault auto-push `if [ -d "$VAULT/.git" ]` block (currently ~line 43). Both `SCRIPT_DIR` resolution approaches are acceptable:

    **Option A (minimal diff, prefer this):** Lift the `SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"` assignment from inside the `if [ -n "$SESSION_LOG" ]` block (currently ~line 35) to the top of the outer `if ls ...` branch (just after the `if` keyword). Then reference it from both the update-context call and the new trigger call.

    **Option B (larger diff):** Re-derive `SCRIPT_DIR` inside the trigger invocation block.

    Planner recommends Option A for cleanliness. Executor picks based on diff hygiene.

    The exact trigger invocation snippet to insert (copy verbatim):

        # Trigger NotebookLM background sync (D-04 fire-and-forget).
        # NBLM-21/22/23: silent skip if binary absent or auth fails; detached spawn;
        # never blocks session-end UI; bash 2>/dev/null || true as double-safety.
        TRIGGER="$SCRIPT_DIR/notebooklm-sync-trigger.mjs"
        if [ -f "$TRIGGER" ]; then
          VAULT_PATH="$VAULT" node "$TRIGGER" 2>/dev/null || true
        fi

    Indentation must match the surrounding block (2 spaces if that's what Phase 1 used).

    **Run `bash -n hooks/session-end-check.sh`** after the edit to catch syntax errors before committing.

    **Add tests to tests/hooks.test.mjs** — new describe block `session-end-check.sh — notebooklm trigger wiring (NBLM-21)` with ~4-5 tests:

    1. **Source-level ordering assertion (no subprocess)** — read `hooks/session-end-check.sh` as string, compute `updateCtxIdx = src.indexOf('update-context.mjs')`, `triggerIdx = src.indexOf('notebooklm-sync-trigger.mjs')`, `vaultPushIdx = src.indexOf('git -C "$VAULT" push')`. Assert all three > 0 AND `updateCtxIdx < triggerIdx < vaultPushIdx`. This is the cheapest + most reliable D-07 verification.

    2. **Suffix assertion** — locate the trigger invocation line and assert it matches `/2>\\/dev\\/null \\|\\| true/`.

    3. **End-to-end hook integration** — create a temp vault with `projects/test-proj/sessions/{today}-test.md` + `project-map.json` resolving cwd to `test-proj`. Copy stub fixture to a temp PATH dir. Run `spawnSync('bash', [hookPath], { env: { ..., VAULT_PATH: tmpVault, PATH: `${stubBinDir}:${origPath}` } })`. Assert `result.status === 0`. Give detached runner up to 500ms to write log (use `setTimeout` + read), then assert log file exists with at least one entry.

    4. **Trigger-missing graceful skip** — temporarily rename `hooks/notebooklm-sync-trigger.mjs` (or run hook from a dir where it's absent). Assert hook still exits 0.

    5. **Phase 1 regression** — run hook and verify context.md still gets updated (existing Phase 1 SKILL-01 behavior unchanged).
  </action>
  <verify>
    <automated>node --test tests/hooks.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "notebooklm-sync-trigger.mjs" hooks/session-end-check.sh` — exactly 1 (the invocation)
    - `grep -c "2>/dev/null || true" hooks/session-end-check.sh` — at least 2 (update-context + trigger)
    - `bash -n hooks/session-end-check.sh` — exit 0 (syntax valid)
    - Ordering source-level test passes: `update-context.mjs` index < `notebooklm-sync-trigger.mjs` index < `git -C "$VAULT" push` index
    - `node --test tests/hooks.test.mjs` — full hook test suite green, including new NBLM-21 describe block
    - Existing Phase 1 tests in tests/hooks.test.mjs still pass (regression check)
    - `npm test` (full suite) green — baseline 183 + Plan 05-01 (~15) + Plan 05-02 (~10-14) tests
  </acceptance_criteria>
  <done>
    session-end-check.sh invokes trigger in correct order. Ordering verified by source-level test. Full suite green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Shell hook → Node trigger | Bash hook passes VAULT_PATH env var to trigger; no user-supplied args |
| Trigger → detached runner | Trigger passes env vars to runner; stdio is redirected to log file fd |
| Runner → notebooklm subprocess | Runner calls `notebooklm auth check` and syncVault (which spawns notebooklm); subprocess stderr captured |
| Runner → log file append | Append-only write to ~/vault/.notebooklm-sync.log |
| OS process lifecycle | Detached child survives parent exit; must not leak file descriptors on orphan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | D (DoS / best-effort) | Detached runner post-session-end | accept | Runner may be killed by OS if user closes laptop mid-sync → orphan `sync start` line in log. Doctor uses `manifest.generated_at` (Plan 05-03), NOT log tail, so orphan start lines are cosmetic. NBLM-23 contracts best-effort. |
| T-05-04 | T (TOCTOU) | Trigger `hasCommandInline('notebooklm')` | accept | User uninstalls binary between check and spawn → runner gets ENOENT on subprocess launch → caught → `[info] sync skipped` → exit 0. NBLM-23 covers this. |
| T-05-06 | T (Race on log append) | Log file concurrent writes | accept | POSIX O_APPEND (Node appendFileSync with 'a' flag) is atomic for small writes. Two sessions ending within 100ms can both write to the log; POSIX guarantees line-level atomicity for buffers < PIPE_BUF. Entries are ≤200 chars — no interleaving risk. |
| T-05-10 | I (Info disclosure) | Runner log error lines | mitigate | `truncate(reason, 200)` clips subprocess error messages. Vault content embedded in stderr is clipped. Log is gitignored (Plan 05-03 D-15) so never committed. |
| T-05-11 | R (Repudiation) | Runner crash path | mitigate | uncaughtException and unhandledRejection handlers write `[error] crash:` with `err.message` + exit 0. Every runner invocation produces at minimum one log entry (start, done, skipped, or crash). |
| T-05-12 | D (Hook unresponsive) | Trigger wall-clock | mitigate | Test-verified trigger exits <1000ms even with long-running stub runner. `.unref()` releases parent event loop. Bash hook wraps in `2>/dev/null || true` — even a pathological trigger crash cannot delay session-end UI. |
</threat_model>

<verification>
- Trigger exits in <1000ms (wall-clock test)
- Runner exit code is always 0 across every tested branch (grep finds no `process.exit([1-9])`)
- Runner's uncaughtException handler is in place before main() runs
- hooks/session-end-check.sh passes `bash -n` syntax check
- Ordering invariant D-07 verified by source-level string-index assertion
- Phase 1 hook tests still pass (regression)
- Phase 4 notebooklm-sync tests still pass after stub fixture extension
- `npm test` full suite green
- No new runtime npm dependencies (`grep -c '"prompts"' package.json` still = 1)
</verification>

<success_criteria>
- NBLM-21: Session-end triggers sync only if binary + auth check pass; silent skip otherwise
- NBLM-22: Trigger is non-blocking (detached + unref + <1s wall-clock)
- NBLM-23: Runner never surfaces errors to terminal; every outcome → log entry + exit 0
- hooks/session-end-check.sh preserves Phase 1 behavior (context update still runs, vault push still runs)
- Trigger and runner follow Phase 1 `hooks/update-context.mjs` architectural template
</success_criteria>

<output>
After completion, create `.planning/phases/05-cli-integration-trigger-wizard/05-02-SUMMARY.md` documenting:
- Files created/modified
- Test counts + delta
- Wall-clock measurement for trigger exit
- Any deviations from plan
- Notes on log format stability for downstream parsers (Plan 05-03 doctor may inspect log)
</output>
