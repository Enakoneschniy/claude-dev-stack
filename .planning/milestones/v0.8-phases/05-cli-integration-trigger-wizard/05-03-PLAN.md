---
phase: 05-cli-integration-trigger-wizard
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - bin/install.mjs
  - lib/doctor.mjs
  - lib/notebooklm-manifest.mjs
  - tests/notebooklm-manifest.test.mjs
  - tests/doctor.test.mjs
  - tests/install.test.mjs
autonomous: false
requirements:
  - NBLM-26
  - NBLM-27
tags:
  - notebooklm
  - install-wizard
  - doctor
  - manifest
user_setup:
  - service: notebooklm-py
    why: "Interactive browser OAuth login during wizard step (NBLM-26 d)"
    env_vars: []
    dashboard_config:
      - task: "User completes Google OAuth in browser during `notebooklm login` subprocess invocation"
        location: "Browser window opened by notebooklm-py during wizard step"

must_haves:
  truths:
    - "`bin/install.mjs::installNotebookLM` at line 816 is REPLACED with the real D-08..D-11 wizard step (not just the summary placeholder at lines 1103-1114) — research finding #4 collision resolved."
    - "Wizard detects `notebooklm` binary via hasCommand first; if present, skips to login verification (idempotent re-runs safe)."
    - "Wizard offers `pipx install notebooklm-py[browser]` as primary install; falls back to `python3 -m pip install --user notebooklm-py[browser]` if pipx absent."
    - "Wizard runs `notebooklm login` via `spawnSync('notebooklm', ['login'], { stdio: 'inherit' })` — exact invocation, blocking, browser OAuth."
    - "Wizard handles SIGINT during login (result.signal === 'SIGINT') → prints info message, skips first-sync step."
    - "Wizard verifies setup with `notebooklm auth check` and reports ok/warn."
    - "Wizard offers 'Run first sync now?' AFTER successful auth check; inline blocking syncVault call; prints stats."
    - "package.json dependencies block remains exactly `{\"prompts\": \"^2.4.2\"}` — single-dep constraint intact after wizard rewrite."
    - "Wizard NEVER prompts for, reads, or stores `NOTEBOOKLM_API_KEY` or any credential — ADR-0001 compliance."
    - "lib/doctor.mjs has 3 NotebookLM lines inserted after the Prerequisites section (~line 63): (1) binary presence with `info` severity on absence, (2) auth check status, (3) last sync from manifest.generated_at."
    - "Doctor's `info` severity for missing binary does NOT increment `issues` or `warnings` counter (ADR-0012 severity discipline)."
    - "Doctor Line 3 (last sync) uses `manifest.generated_at` (via readManifest), NOT log tail parsing — research §R6 recommendation."
    - "`lib/notebooklm-manifest.mjs::ensureManifestGitignored` managed block includes 4 entries: `.notebooklm-sync.json`, `.notebooklm-sync.json.tmp`, `.notebooklm-sync.corrupt-*`, AND new `.notebooklm-sync.log` (D-15)."
    - "The idempotency sentinel bug (research finding #5) is FIXED: existing Phase 3 vaults with the 3-entry block get `.notebooklm-sync.log` appended on next run. Migration is verified by a new test that pre-populates a 3-line block and asserts the 4th line is added."
    - "Existing Phase 3 test T3-07 at tests/notebooklm-manifest.test.mjs:309 is updated from 'all three entries' to 'all four entries' with `.notebooklm-sync.log` assertion."
    - "No other Phase 3 tests are modified — idempotency guarantees (T3-04, T3-05, T3-06) still hold."
  artifacts:
    - path: "bin/install.mjs"
      provides: "Replaced installNotebookLM function body (lines 816-832) with D-08..D-11 wizard flow; updated showInstructions placeholder (lines 1103-1114) with accurate summary"
      contains: "pipx install"
    - path: "lib/doctor.mjs"
      provides: "NotebookLM 3-line section after Prerequisites"
      contains: "notebooklm"
    - path: "lib/notebooklm-manifest.mjs"
      provides: "ensureManifestGitignored extended to 4-entry managed block + migration-safe sentinel"
      contains: ".notebooklm-sync.log"
    - path: "tests/notebooklm-manifest.test.mjs"
      provides: "T3-07 updated to 4 entries + new migration test for 3→4 line upgrade"
      contains: ".notebooklm-sync.log"
    - path: "tests/doctor.test.mjs"
      provides: "Doctor NotebookLM section tests (info severity on absence, ok on present+authed)"
      min_lines: 80
    - path: "tests/install.test.mjs"
      provides: "Install wizard NotebookLM step tests with mocked prompts + fake binary"
      min_lines: 80
  key_links:
    - from: "bin/install.mjs::installNotebookLM (new body)"
      to: "lib/notebooklm-sync.mjs::syncVault"
      via: "Inline await syncVault({vaultRoot}) for first-sync prompt (D-11)"
      pattern: "syncVault\\("
    - from: "bin/install.mjs::installNotebookLM"
      to: "notebooklm login subprocess"
      via: "spawnSync('notebooklm', ['login'], { stdio: 'inherit' })"
      pattern: "spawnSync.*notebooklm.*login.*stdio.*inherit"
    - from: "lib/doctor.mjs NotebookLM section"
      to: "lib/notebooklm-manifest.mjs::readManifest"
      via: "readManifest(vaultPath).generated_at for last-sync line"
      pattern: "readManifest"
    - from: "lib/notebooklm-manifest.mjs::ensureManifestGitignored"
      to: "Migration-safe sentinel"
      via: "Two-phase check: (1) block presence, (2) log entry presence; append missing log line if block exists but log absent"
      pattern: "\\.notebooklm-sync\\.log"
---

<objective>
Deliver the user-facing onboarding and observability surfaces for NotebookLM. Three independent concerns wired into existing files:

1. **Install wizard rewrite** (`bin/install.mjs`) — REPLACE the existing `installNotebookLM` function at line 816 (research finding #4 — this is the function that actually runs during the wizard, NOT the placeholder at 1103-1114 alone) with a proper D-08..D-11 flow: detect → install via pipx/pip → `notebooklm login` interactive → auth check → first-sync prompt. ALSO update the summary placeholder at lines 1103-1114 to reflect the actual outcome.

2. **Doctor NotebookLM section** (`lib/doctor.mjs`) — add 3 lines (binary, auth, last sync) after the Prerequisites section using ADR-0012 severity discipline (`info` for missing optional binary, NOT `fail`).

3. **Phase 3 gitignore extension** (`lib/notebooklm-manifest.mjs`) — extend the managed block from 3 to 4 entries to include `.notebooklm-sync.log`, AND fix the idempotency sentinel bug (research finding #5 — current sentinel only checks `.notebooklm-sync.json` presence, so existing Phase 3 vaults with 3-line blocks never get upgraded). Migration-safe: new test verifies 3→4 upgrade path.

**NOT autonomous** — Task 1 (wizard) includes a `checkpoint:human-verify` step because the real `notebooklm login` subprocess requires user interaction with a browser and cannot be fully automated in CI. The wizard CODE is autonomous, but verification that the interactive subprocess handoff works end-to-end requires the user to run it once manually.

Purpose: Users can discover, install, authenticate, and first-sync NotebookLM through a single `claude-dev-stack install` run. Doctor gives honest health status. Existing Phase 3 vaults get the log gitignored on next sync.
Output: Replaced ~80-120 LoC in bin/install.mjs, ~40 LoC added to lib/doctor.mjs, ~5 LoC change to lib/notebooklm-manifest.mjs (block + sentinel), ~3 test-expectation updates in tests/notebooklm-manifest.test.mjs + 1 new migration test, ~6-10 tests in new tests/doctor.test.mjs + tests/install.test.mjs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-cli-integration-trigger-wizard/05-CONTEXT.md
@.planning/phases/05-cli-integration-trigger-wizard/05-RESEARCH.md
@.planning/phases/05-cli-integration-trigger-wizard/05-VALIDATION.md
@.planning/phases/03-sync-manifest-change-detection/03-01-SUMMARY.md
@.planning/phases/04-vault-notebooklm-sync-pipeline/04-02-SUMMARY.md
@bin/install.mjs
@lib/doctor.mjs
@lib/notebooklm-manifest.mjs
@lib/notebooklm-sync.mjs
@lib/notebooklm.mjs
@lib/shared.mjs
@tests/notebooklm-manifest.test.mjs

<interfaces>
<!-- Extracted from SHIPPED code — executor consumes these contracts directly. -->

From lib/notebooklm-manifest.mjs (Phase 3, lines 280-318, SHIPPED):
  export function ensureManifestGitignored(vaultRoot) {
    // blockWithoutLeadingBlank is currently 3 entries (lines 285-289):
    //   '.notebooklm-sync.json'
    //   '.notebooklm-sync.json.tmp'
    //   '.notebooklm-sync.corrupt-*'
    // Idempotency sentinel at line 299:
    //   const alreadyPresent = lines.some(line => line.trim() === '.notebooklm-sync.json');
    //   if (alreadyPresent) return;  ← BUG: returns early on existing vaults, so .notebooklm-sync.log never gets added
  }

  export function readManifest(vaultRoot): {
    version: 1,
    generated_at: string | undefined,
    files: { [path]: { hash, notebook_source_id, uploaded_at } }
  }
  // Fresh vault → empty-shape return, never throws.

From lib/notebooklm-sync.mjs (Phase 4, SHIPPED):
  export async function syncVault(opts): Promise<stats>
  // See Plan 05-01 interfaces block for full shape.

From lib/notebooklm.mjs (Phase 2/4, SHIPPED):
  export class NotebooklmNotInstalledError extends Error { }
  export class NotebooklmCliError extends Error { command; exitCode; stderr; }
  export class NotebooklmRateLimitError extends NotebooklmCliError { }

From lib/shared.mjs (SHIPPED):
  export function hasCommand(name): boolean
  export function runCmd(cmd, opts?): string | null   // uses execSync; returns null on failure
  export const c = { ... };
  export function ok/fail/warn/info(msg): void
  export { spawnSync, existsSync, homedir };

From bin/install.mjs (SHIPPED, READ BEFORE EDIT):
- Line 816 `function installNotebookLM(pipCmd, stepNum, totalSteps)`:
    step(stepNum, totalSteps, '📚 Installing NotebookLM');
    info('Installing notebooklm-py (this may take a minute)...');
    const result = runCmd(`${pipCmd} install "notebooklm-py[browser]" --break-system-packages 2>/dev/null || ${pipCmd} install "notebooklm-py[browser]"`);
    if (result !== null) {
      runCmd('playwright install chromium 2>/dev/null');
      runCmd('notebooklm skill install 2>/dev/null');
      ok('NotebookLM installed');
      warn('Run "notebooklm login" to authenticate with Google');
      return true;
    }
    return false;
  — This is the function called at line 1215 during the wizard. MUST be replaced.

- Lines 1103-1114 `showInstructions` NotebookLM block:
    if (components.notebooklm) {
      console.log('');
      const stepN = ...;
      console.log(`  ${c.bold}${stepN}. Set up NotebookLM${c.reset}`);
      console.log(`     ${c.dim}NotebookLM needs a one-time Google authentication:${c.reset}`);
      console.log(`     ${c.white}notebooklm login${c.reset}`);
      console.log(`     ${c.dim}  → Opens browser for Google sign-in${c.reset}`);
    }
  — This is the SUMMARY placeholder shown AFTER wizard. Replace with accurate post-wizard text.

- Line 1215 caller:
    if (components.notebooklm) {
      installNotebookLM(pipCmd, stepNum++, totalSteps)
        ? installed.push('NotebookLM') : failed.push('NotebookLM');
    }
  — Keep the caller signature. Function still returns boolean.

From lib/doctor.mjs (SHIPPED):
- Lines 28-63: prerequisites section (existing pattern `ok/fail/warn/info`).
- Line 63-127: Knowledge Vault section — PHASE 5 INSERTION GOES BETWEEN these two (after prerequisites, before vault).
- `let issues = 0; let warnings = 0;` counters — NotebookLM binary absence must NOT increment either (ADR-0012).

From tests/notebooklm-manifest.test.mjs (SHIPPED):
- Line 309: `it('managed block contains all three entries (T3-07 — D-22)', () => { ... })` — MUST update to 'all four entries' + add `.notebooklm-sync.log` assertion.
- Lines 279-295: idempotency tests T3-04/T3-05 — must still pass (they only check `.notebooklm-sync.json` count).

Research §R1 (auth check) — `notebooklm auth check --json` returns `{status: "ok", checks: {...}}` and exits 0 on success. Plain `notebooklm auth check` (no --json) also exits 0 on success. Phase 5 wizard + doctor both use plain form for simplicity.

Research §R2 (login) — `notebooklm login` opens browser + waits for ENTER. No `--headless` flag. Ctrl+C yields `result.signal === 'SIGINT'` in spawnSync.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend Phase 3 gitignore managed block + fix idempotency sentinel (migration-safe)</name>
  <files>lib/notebooklm-manifest.mjs, tests/notebooklm-manifest.test.mjs</files>
  <read_first>
    - lib/notebooklm-manifest.mjs (lines 280-318 — current ensureManifestGitignored implementation)
    - tests/notebooklm-manifest.test.mjs (lines 270-330 — existing gitignore tests T3-01..T3-08)
    - .planning/phases/05-cli-integration-trigger-wizard/05-CONTEXT.md §D-15
    - .planning/phases/05-cli-integration-trigger-wizard/05-RESEARCH.md §R7 Phase 3 Gitignore Block Extension Analysis (lines 218-243) + §Pitfall 3 (lines 540-546)
    - .planning/phases/03-sync-manifest-change-detection/03-CONTEXT.md §D-18, §D-19, §D-22 (block shape)
  </read_first>
  <behavior>
    - Test: fresh vault with no .gitignore → block is written with all 4 entries including `.notebooklm-sync.log`.
    - Test: fresh vault with unrelated .gitignore content → block is appended with all 4 entries.
    - Test: vault that already has the FULL 4-entry block → idempotent no-op (line count unchanged).
    - Test (MIGRATION — new test for research finding #5): vault with the OLD 3-entry block already present (simulating existing Phase 3 user) → calling ensureManifestGitignored appends ONLY `.notebooklm-sync.log` as a 4th entry; the 3 existing entries are NOT duplicated.
    - Test: T3-04 (idempotency N calls) still holds with the new 4-entry block.
    - Test: T3-05 (exactly one occurrence of `.notebooklm-sync.json`) still holds.
    - Test: T3-06 (CRLF handling) still holds.
    - Test (UPDATED T3-07): assert all 4 entries are present — `.notebooklm-sync.json`, `.notebooklm-sync.json.tmp`, `.notebooklm-sync.corrupt-*`, `.notebooklm-sync.log`.
  </behavior>
  <action>
    **Modify lib/notebooklm-manifest.mjs::ensureManifestGitignored** (lines 280-318). Two changes:

    **Change 1 — extend block string (line 285-289):**

        const blockWithoutLeadingBlank =
          '# Claude Dev Stack \u2014 NotebookLM sync state (do not commit)\n' +
          '.notebooklm-sync.json\n' +
          '.notebooklm-sync.json.tmp\n' +
          '.notebooklm-sync.corrupt-*\n' +
          '.notebooklm-sync.log';

    Note: the 4th line has NO trailing `\n` to match the existing pattern (the final `\n` is appended by the write logic at lines 306-315).

    **Change 2 — fix the migration-unsafe sentinel (line 299). Replace:**

        const alreadyPresent = lines.some(line => line.trim() === '.notebooklm-sync.json');
        if (alreadyPresent) {
          return; // Idempotent no-op (D-18 step 2)
        }

    **With a two-phase check:**

        const hasJsonEntry = lines.some(line => line.trim() === '.notebooklm-sync.json');
        const hasLogEntry = lines.some(line => line.trim() === '.notebooklm-sync.log');

        if (hasJsonEntry && hasLogEntry) {
          return; // Fully-present block (4 entries already there) — idempotent no-op.
        }

        if (hasJsonEntry && !hasLogEntry) {
          // Migration path: existing Phase 3 vault has the 3-entry block.
          // Append only the new .notebooklm-sync.log entry, preserving existing content.
          const needsLeadingNewline = !existing.endsWith('\n');
          const migration = (needsLeadingNewline ? '\n' : '') + '.notebooklm-sync.log\n';
          writeFileSync(gitignorePath, existing + migration, 'utf8');
          return;
        }

        // Otherwise: no block yet. Fall through to existing block-write logic.

    Then keep the existing block-write logic (lines 304-317) unchanged — it already handles fresh/empty/unnewlined cases correctly, and now writes the new 4-entry block.

    **Update tests/notebooklm-manifest.test.mjs:**

    1. **Update T3-07 at line 309.** Rename to `'managed block contains all four entries (T3-07 — D-22, extended for .notebooklm-sync.log)'`. Add assertion:

        assert.ok(content.includes('.notebooklm-sync.log'));

    2. **Add a new migration test** (new `it(...)` block immediately after T3-07):

        it('existing 3-entry block gets .notebooklm-sync.log appended on re-run (migration)', () => {
          const gitignorePath = join(vaultRoot, '.gitignore');
          // Pre-populate the vault .gitignore with the OLD 3-entry block (Phase 3 shipped state).
          const oldBlock =
            '# Claude Dev Stack — NotebookLM sync state (do not commit)\n' +
            '.notebooklm-sync.json\n' +
            '.notebooklm-sync.json.tmp\n' +
            '.notebooklm-sync.corrupt-*\n';
          writeFileSync(gitignorePath, 'node_modules\n' + oldBlock, 'utf8');

          // Run ensureManifestGitignored — should append the missing 4th entry.
          ensureManifestGitignored(vaultRoot);

          const after = readFileSync(gitignorePath, 'utf8');
          assert.ok(after.includes('.notebooklm-sync.log'));
          // All 3 original entries still present exactly once.
          const lines = after.split(/\r?\n/);
          assert.equal(lines.filter(l => l.trim() === '.notebooklm-sync.json').length, 1);
          assert.equal(lines.filter(l => l.trim() === '.notebooklm-sync.json.tmp').length, 1);
          assert.equal(lines.filter(l => l.trim() === '.notebooklm-sync.corrupt-*').length, 1);
          assert.equal(lines.filter(l => l.trim() === '.notebooklm-sync.log').length, 1);
        });

        it('fully populated 4-entry block on second call is idempotent no-op', () => {
          ensureManifestGitignored(vaultRoot);
          const afterFirst = readFileSync(join(vaultRoot, '.gitignore'), 'utf8');
          ensureManifestGitignored(vaultRoot);
          const afterSecond = readFileSync(join(vaultRoot, '.gitignore'), 'utf8');
          assert.equal(afterFirst, afterSecond);
        });

    3. Do NOT modify T3-01, T3-02, T3-03, T3-04, T3-05, T3-06, T3-08 — they must still pass unchanged. Run the full Phase 3 test file after the edit as regression check.
  </action>
  <verify>
    <automated>node --test tests/notebooklm-manifest.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\\.notebooklm-sync\\.log" lib/notebooklm-manifest.mjs` — at least 1
    - `grep -c "hasJsonEntry" lib/notebooklm-manifest.mjs` — exactly 1 (new sentinel variable name)
    - `grep -c "hasLogEntry" lib/notebooklm-manifest.mjs` — exactly 1
    - `grep -c "migration" lib/notebooklm-manifest.mjs` — at least 1 (comment or var)
    - `grep -c "all four entries\\|four entries" tests/notebooklm-manifest.test.mjs` — at least 1 (updated T3-07 label)
    - `grep -c "migration" tests/notebooklm-manifest.test.mjs` — at least 1 (new migration test)
    - `node --test tests/notebooklm-manifest.test.mjs` — all tests pass (updated + new + all existing T3-01..T3-08)
    - Baseline Phase 3 test count increases by 1-2 (new migration tests) with ZERO pre-existing test failures
  </acceptance_criteria>
  <done>
    4-entry block ships. Migration sentinel resolves research finding #5. T3-07 updated. New migration test passes. No existing Phase 3 test regressed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add NotebookLM section to lib/doctor.mjs with ADR-0012 severity discipline</name>
  <files>lib/doctor.mjs, tests/doctor.test.mjs</files>
  <read_first>
    - lib/doctor.mjs (full file — understand existing section structure, counter pattern, `ok/fail/warn/info` usage)
    - .planning/phases/05-cli-integration-trigger-wizard/05-CONTEXT.md §D-13, §C §Anti-pattern R
    - .planning/phases/05-cli-integration-trigger-wizard/05-RESEARCH.md §R6 log parse strategy (use manifest.generated_at), §Pitfall 5 info severity for missing binary
    - lib/notebooklm-manifest.mjs::readManifest (signature + shape)
    - lib/shared.mjs (ok/fail/warn/info + hasCommand + runCmd)
  </read_first>
  <behavior>
    - Test: missing `notebooklm` binary → doctor prints `info` line "notebooklm — not installed" (blue ℹ icon) AND does NOT increment issues or warnings counters.
    - Test: binary present, auth check exits 0 → doctor prints `ok('notebooklm — installed (v0.3.4)')` AND `ok('notebooklm auth — ok')` AND third line for last sync.
    - Test: binary present, auth check exits non-zero → doctor prints `warn('notebooklm auth — login required, run: notebooklm login')` AND increments warnings counter.
    - Test: manifest.generated_at is undefined (fresh vault) → doctor prints `info('last sync: never')`.
    - Test: manifest.generated_at is 1 day ago → doctor prints `ok('last sync: 1 day(s) ago, N files tracked')`.
    - Test: manifest.generated_at is 5 days ago → doctor prints `warn('last sync: 5 day(s) ago (consider running manually)')` AND increments warnings counter.
    - Test: `notebooklm --version` subprocess output is parsed and displayed if present.
  </behavior>
  <action>
    **Modify lib/doctor.mjs.** Insert a new NotebookLM section AFTER the Prerequisites section (after the `for (const py of ['python3', 'python'])` block at line 58-63) and BEFORE the `// ── 2. Vault ──` comment at line 66.

    Required implementation outline:

        // ── 1b. NotebookLM (optional feature) ──
        console.log('');
        console.log(`  ${c.bold}NotebookLM Sync${c.reset}`);
        console.log('');

        const hasNotebooklm = hasCommand('notebooklm');
        if (hasNotebooklm) {
          // Line 1: binary version
          const ver = runCmd('notebooklm --version 2>/dev/null');
          ok(`notebooklm${ver ? ` (${ver.replace(/^NotebookLM CLI, version /, '')})` : ''}`);

          // Line 2: auth check (plain form — research §R1 exit 0 = authenticated)
          const authOk = runCmd('notebooklm auth check 2>/dev/null') !== null;
          if (authOk) {
            ok('notebooklm auth — ok');
          } else {
            warn('notebooklm auth — login required, run: notebooklm login');
            warnings++;
          }

          // Line 3: last sync from manifest (research §R6 — avoid log parsing)
          if (vaultPath) {
            try {
              const { readManifest } = await import('./notebooklm-manifest.mjs');
              const manifest = readManifest(vaultPath);
              const lastSync = manifest.generated_at;
              const fileCount = Object.keys(manifest.files ?? {}).length;
              if (!lastSync || fileCount === 0) {
                info('last sync: never');
              } else {
                const ageMs = Date.now() - new Date(lastSync).getTime();
                const ageDays = Math.floor(ageMs / (1000 * 60 * 60 * 24));
                const label = ageDays === 0 ? 'today' : `${ageDays} day${ageDays === 1 ? '' : 's'} ago`;
                if (ageDays > 3) {
                  warn(`last sync: ${label}, ${fileCount} files tracked (consider running manually)`);
                  warnings++;
                } else {
                  ok(`last sync: ${label}, ${fileCount} files tracked`);
                }
              }
            } catch {
              info('last sync: unknown');
            }
          } else {
            info('last sync: unknown (no vault)');
          }
        } else {
          // CRITICAL (ADR-0012 + Pitfall 5): `info` level, NOT `fail` or `warn`.
          // NotebookLM is an optional feature — missing binary is "not configured",
          // not a health problem. DO NOT increment issues or warnings counters.
          info('notebooklm — not installed (optional, run claude-dev-stack to set up)');
        }

    **IMPORTANT:** `vaultPath` is computed LATER in the existing doctor flow (line 70). The NotebookLM section is inserted BEFORE that. Either:
    - **Option A:** Move the `const vaultPath = findVault();` line UP to just before the NotebookLM section, then reuse in both NotebookLM section AND existing vault section.
    - **Option B:** Compute a local `const nbVaultPath = findVault();` just for the NotebookLM section.

    Option A is cleaner — hoist the `findVault()` call once. Verify nothing in Prerequisites section depends on its absence.

    **Import adjustment:** `readManifest` is dynamic-imported inside the try block to avoid breaking doctor if the module somehow fails to load. Top-of-file static import is also acceptable if preferred — executor picks.

    **Create tests/doctor.test.mjs** (new file, ~100 LoC, 6-8 tests). Use PATH prefix + stub fixture + temp vault:

        import { describe, it, before, after, beforeEach, afterEach } from 'node:test';
        import assert from 'node:assert/strict';
        import { execFileSync, spawnSync } from 'node:child_process';
        import { mkdtempSync, mkdirSync, rmSync, writeFileSync, copyFileSync, chmodSync } from 'node:fs';
        import { tmpdir } from 'node:os';
        import { join, dirname } from 'node:path';
        import { fileURLToPath } from 'node:url';

        const __dirname = dirname(fileURLToPath(import.meta.url));
        const projectRoot = join(__dirname, '..');
        const stubPath = join(projectRoot, 'tests', 'fixtures', 'notebooklm-sync-stub.sh');

        describe('doctor — NotebookLM section (NBLM-27)', () => {
          // ... before/after lifecycle with tmpVault + stubBinDir ...

          it('missing binary → info line, no issues/warnings increment', () => {
            // Run doctor with empty PATH (no notebooklm)
            // Assert stdout contains "notebooklm — not installed"
            // Assert stdout does NOT contain red ✘ or yellow ⚠ for notebooklm line
            // Assert final summary shows 0 issues, 0 warnings (or baseline only)
          });

          it('binary present + auth check ok → 3 ok lines for notebooklm section', () => {
            // Stub with auth_exit=0, minimal vault manifest
            // Assert ok('notebooklm'), ok('notebooklm auth — ok'), and a last-sync line
          });

          it('binary present + auth check fails → warn line + warnings increment', () => {
            // Stub with NOTEBOOKLM_SYNC_STUB_AUTH_EXIT=1
            // Assert stdout contains "login required"
          });

          it('last sync recent (manifest.generated_at = today) → ok last sync', () => { /* ... */ });
          it('last sync stale (manifest.generated_at = 5 days ago) → warn last sync', () => { /* ... */ });
          it('fresh vault no manifest → "last sync: never"', () => { /* ... */ });
        });

    Invocation pattern: `execFileSync(node, [cliPath, 'doctor'], { env: {...}, encoding: 'utf8' })` captures stdout. Doctor already exits 0 regardless; parse stdout for expected lines.
  </action>
  <verify>
    <automated>node --test tests/doctor.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "NotebookLM Sync" lib/doctor.mjs` — exactly 1 (new section header)
    - `grep -c "notebooklm" lib/doctor.mjs` — at least 4 (binary, auth, last sync, info-on-missing paths)
    - `grep -c "fail(.*notebooklm" lib/doctor.mjs` — 0 (ADR-0012: NEVER fail() on missing binary)
    - `grep -c "readManifest" lib/doctor.mjs` — exactly 1
    - `grep -c "not installed (optional" lib/doctor.mjs` — exactly 1 (matches Pitfall 5 phrasing)
    - `grep -c "login required" lib/doctor.mjs` — exactly 1
    - `node --test tests/doctor.test.mjs` — all tests pass
    - `node bin/cli.mjs doctor` (manual inspection) — NotebookLM section renders cleanly on dev machine
    - Doctor exit code unchanged — still exits 0 regardless
    - `grep -c '"prompts"' package.json` — exactly 1 (single-dep preserved)
  </acceptance_criteria>
  <done>
    Doctor has 3-line NotebookLM section. Missing binary is `info` severity. Auth check and last sync reflect real state. Tests green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Replace bin/install.mjs installNotebookLM with D-08..D-11 wizard + update showInstructions placeholder</name>
  <files>bin/install.mjs, tests/install.test.mjs</files>
  <read_first>
    - bin/install.mjs (lines 1-50 for header/imports, 400-500 for selectComponents, 810-840 for current installNotebookLM, 1090-1135 for showInstructions placeholder, 1160-1220 for main() wiring)
    - .planning/phases/05-cli-integration-trigger-wizard/05-CONTEXT.md §D-08, §D-09, §D-10, §D-11
    - .planning/phases/05-cli-integration-trigger-wizard/05-RESEARCH.md §Install Wizard Insertion Points (lines 321-375), §R2 login behavior, §R3 pipx detection, §Pitfall 1 installNotebookLM collision
    - lib/shared.mjs (spawnSync, hasCommand, runCmd, prompt, c helpers)
    - lib/notebooklm-sync.mjs (syncVault signature)
  </read_first>
  <behavior>
    - Test: binary already present → wizard skips install step, goes directly to login verification.
    - Test: binary absent + pipx present → wizard offers pipx install command (visible in prompt), runs it, re-checks hasCommand.
    - Test: binary absent + pipx absent + python3 present → wizard offers pip --user fallback, runs it, re-checks hasCommand.
    - Test: binary absent + no Python → wizard prints manual-install instructions and skips remaining steps cleanly (returns false).
    - Test: login subprocess mocked — wizard calls spawnSync with exact args `['notebooklm', ['login'], { stdio: 'inherit' }]`. Captured via mock (planner picks: either monkey-patch spawnSync or inject a test mode flag).
    - Test: login returns `result.signal === 'SIGINT'` → wizard prints info, skips first-sync step, returns true-with-partial or false (planner picks semantics).
    - Test: auth check fails after login → wizard warns, skips first-sync, returns true (installed but not authenticated).
    - Test: auth check passes → wizard prompts "Run first sync now?". If user confirms, calls syncVault inline.
    - Test: no NOTEBOOKLM_API_KEY or credential handling anywhere in the wizard function — ADR-0001 grep sweep.
  </behavior>
  <action>
    **CRITICAL — research finding #4 collision.** The existing `installNotebookLM` function at `bin/install.mjs:816` is the one actually invoked from the wizard at line 1215, NOT the placeholder at lines 1103-1114. D-08 said "replace placeholder 1103-1114" but that's the summary display. Phase 5 MUST:

    1. **Replace `installNotebookLM` function body at lines 816-832** with the real D-08..D-11 flow.
    2. **Update the showInstructions placeholder at lines 1103-1114** with post-wizard-accurate text (not install instructions anymore — those already ran).

    Both changes go in the same commit.

    **Replace installNotebookLM function (lines 816-832)** with ~80-120 LoC. Signature stays `function installNotebookLM(pipCmd, stepNum, totalSteps)` returning boolean. Keep it SYNCHRONOUS to match the existing call site pattern at line 1215 (or make it async and await — the main() function is already async). Planner recommends making it `async function` and changing line 1215 to `await installNotebookLM(...)` — cleaner for the syncVault await.

    Required flow:

        async function installNotebookLM(pipCmd, stepNum, totalSteps) {
          step(stepNum, totalSteps, '📚 Setting up NotebookLM');
          info('NotebookLM lets Claude ground answers in your vault history.');
          info('Requires notebooklm-py (Python package) + one-time Google OAuth login.');

          // Step 1: Detect if notebooklm binary is already available (idempotent re-run).
          if (hasCommand('notebooklm')) {
            ok('notebooklm binary already in PATH — skipping install');
          } else {
            // Step 2: Choose install method — pipx first (D-09), pip --user fallback.
            let installCmd = null;
            if (hasCommand('pipx')) {
              installCmd = 'pipx install "notebooklm-py[browser]"';
            } else if (runCmd('python3 -m pip --version') !== null) {
              installCmd = 'python3 -m pip install --user "notebooklm-py[browser]"';
            }

            if (!installCmd) {
              warn('Neither pipx nor python3 detected.');
              info('To install manually later: pipx install "notebooklm-py[browser]"');
              return false;
            }

            // Show the exact command before running (user consent).
            console.log(`  ${c.dim}Will run: ${installCmd}${c.reset}`);
            const { proceed } = await prompt({
              type: 'confirm',
              name: 'proceed',
              message: 'Install notebooklm-py now?',
              initial: true,
            });
            if (!proceed) {
              info('Skipped — you can install later with: ' + installCmd);
              return false;
            }

            info('Installing notebooklm-py (this may take a minute)...');
            const installResult = runCmd(`${installCmd} 2>&1`);
            if (installResult === null) {
              fail('Install failed.');
              info(`Try manually: ${installCmd}`);
              return false;
            }

            // Re-check after install (T-05-13: install may silently fail on PATH edge cases).
            if (!hasCommand('notebooklm')) {
              fail('notebooklm binary still not in PATH after install.');
              info('You may need to restart your shell or adjust PATH.');
              return false;
            }
            ok('notebooklm-py installed');
          }

          // Step 3: Run `notebooklm login` interactively (D-10).
          info('Launching notebooklm login — browser will open for Google OAuth.');
          info('After signing in, return to this terminal and press ENTER.');
          const loginResult = spawnSync('notebooklm', ['login'], { stdio: 'inherit' });

          if (loginResult.signal === 'SIGINT') {
            info('Login cancelled. Run "notebooklm login" manually to complete setup.');
            return false;
          }
          if (loginResult.error) {
            fail(`Login subprocess failed: ${loginResult.error.message}`);
            return false;
          }
          if (loginResult.status !== 0) {
            warn('Login may not have completed. You can re-run "notebooklm login" later.');
            // Continue anyway — auth check will confirm actual state.
          }

          // Step 4: Verify with auth check (D-10 post-login).
          const authOk = runCmd('notebooklm auth check 2>/dev/null') !== null;
          if (!authOk) {
            warn('notebooklm auth check — not authenticated.');
            info('You can retry with: notebooklm login');
            return false;
          }
          ok('notebooklm authenticated');

          // Step 5: Offer first sync (D-11).
          const { runFirstSync } = await prompt({
            type: 'confirm',
            name: 'runFirstSync',
            message: 'Run first NotebookLM sync now? (recommended)',
            initial: true,
          });
          if (runFirstSync) {
            info('Running first sync — this may take 30-60 seconds on a populated vault...');
            try {
              const { syncVault } = await import('../lib/notebooklm-sync.mjs');
              const stats = await syncVault({});
              ok(`First sync complete: ${stats.uploaded} uploaded, ${stats.skipped} skipped, ${stats.failed} failed (${stats.durationMs}ms)`);
              if (stats.errors && stats.errors.length > 0) {
                warn(`${stats.errors.length} file(s) had errors — run 'claude-dev-stack notebooklm sync' again to retry.`);
              }
            } catch (err) {
              warn(`First sync failed: ${err.message}`);
              info("You can run 'claude-dev-stack notebooklm sync' later.");
            }
          } else {
            info("You can run sync later with: claude-dev-stack notebooklm sync");
          }

          return true;
        }

    **Update the call site at line 1215.** If you made `installNotebookLM` async (recommended), change:

        if (components.notebooklm) {
          installNotebookLM(pipCmd, stepNum++, totalSteps)
            ? installed.push('NotebookLM') : failed.push('NotebookLM');
        }

    to:

        if (components.notebooklm) {
          (await installNotebookLM(pipCmd, stepNum++, totalSteps))
            ? installed.push('NotebookLM') : failed.push('NotebookLM');
        }

    **Update the showInstructions placeholder at lines 1103-1114.** Replace with post-wizard summary:

        // Step N: NotebookLM post-install summary (wizard already ran — this is just confirmation)
        if (components.notebooklm) {
          console.log('');
          const stepN = 2 + (components.vault || components.obsidianSkills ? 1 : 0) + (components.gsd ? 1 : 0) + (components.deepResearch ? 1 : 0);
          console.log(`  ${c.bold}${stepN}. NotebookLM Sync${c.reset}`);
          console.log(`     ${c.dim}Vault automatically syncs to NotebookLM after every session end.${c.reset}`);
          console.log(`     ${c.white}claude-dev-stack notebooklm sync${c.reset}     ${c.dim}Manual sync${c.reset}`);
          console.log(`     ${c.white}claude-dev-stack notebooklm status${c.reset}   ${c.dim}Show last sync + file count${c.reset}`);
          console.log(`     ${c.white}claude-dev-stack doctor${c.reset}              ${c.dim}Health check${c.reset}`);
        }

    **Create tests/install.test.mjs** (~80-120 LoC, 5-8 tests). Testing strategy: Since the wizard is interactive + relies on external binaries, tests will monkey-patch or mock key subprocess calls. Planner recommends:

    - Import `installNotebookLM` directly via dynamic import (if exported). If not exported, add an export in this task: `export { installNotebookLM };` at bottom of bin/install.mjs.
    - Mock `hasCommand`, `runCmd`, `spawnSync`, and `prompt` by module-level stubbing (assign `process.env.CLAUDE_DEV_STACK_TEST_MODE = '1'` and have installNotebookLM check it — OR use a simpler approach: test by running the CLI with a PATH prefixed with fake binaries).

    Simpler approach: Make `tests/install.test.mjs` tests use **PATH-based fake binaries + env-var-driven prompt bypass**. Add a minimal `INSTALL_TEST_ANSWERS` env var (e.g., a comma-separated list of "yes/no") that the `prompt` call checks in test mode. OR keep it simple: limit test coverage to grep-based structural assertions (function shape, no API key handling, ADR-0001 compliance), plus a single integration test that runs the function against a fake-pipx + fake-notebooklm PATH.

    Minimum test coverage:

    1. **Structural test (grep):** `grep "NOTEBOOKLM_API_KEY" bin/install.mjs` returns 0 matches (ADR-0001).
    2. **Structural test (grep):** `grep "spawnSync.*notebooklm.*login.*inherit" bin/install.mjs` returns ≥1 match (D-10 exact invocation).
    3. **Structural test (grep):** function body at line 816 onward contains `pipx install` AND `pip install --user` AND `auth check` AND `syncVault` — all five markers present.
    4. **Functional test (mocked path):** Run function with pre-existing notebooklm in PATH → skips install, proceeds to login verification phase. Mock login to return `{status: 0}`. Mock auth check via stub. Mock `runFirstSync` prompt to return false. Assert function returns true.
    5. **Functional test (mocked path):** Run function with empty PATH and neither pipx nor python3 → returns false without crashing.
    6. **ADR-0001 sweep:** after function replacement, `grep -c "NOTEBOOKLM_API_KEY\\|storage_state\\.json\\|\\.notebooklm.*credentials" bin/install.mjs` — 0.

    If monkey-patching proves too invasive, replace tests 4 and 5 with CLI-level smoke tests:

    - Invoke `bin/install.mjs` with PATH prefixed by a fake binary dir, non-interactive (auto-answer prompts via environment or stdin).

    **Task 3 is NOT fully autonomous** — the `notebooklm login` subprocess requires real browser interaction that cannot be automated in tests. Include a `checkpoint:human-verify` task AFTER Task 3 (see Task 4 below).
  </action>
  <verify>
    <automated>node --test tests/install.test.mjs tests/notebooklm-manifest.test.mjs tests/doctor.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "NOTEBOOKLM_API_KEY" bin/install.mjs` — 0 (ADR-0001 compliance)
    - `grep -c "storage_state" bin/install.mjs` — 0 (no credential file access)
    - `grep -c "spawnSync.*notebooklm.*login" bin/install.mjs` — at least 1 (D-10 invocation)
    - `grep -c "stdio: 'inherit'" bin/install.mjs` — at least 1 (matches D-10 exact option)
    - `grep -c "pipx install" bin/install.mjs` — at least 1 (D-09)
    - `grep -c "pip install --user\\|python3 -m pip install --user" bin/install.mjs` — at least 1 (D-09 fallback)
    - `grep -c "auth check" bin/install.mjs` — at least 1 (D-10 verify)
    - `grep -c "syncVault" bin/install.mjs` — at least 1 (D-11 first sync)
    - `grep -c "notebooklm-py\\[browser\\]" bin/install.mjs` — at least 2 (both pipx + pip commands; research §Install wizard note on `[browser]` extra)
    - `grep "pip install \"notebooklm-py\\[browser\\]\" --break-system-packages" bin/install.mjs` — 0 matches (old installNotebookLM removed per 5-TBD collision row in 05-VALIDATION.md)
    - `grep -c "playwright install chromium" bin/install.mjs` — 0 (old pattern replaced; playwright is bundled with `[browser]` extra anyway)
    - `node --test tests/install.test.mjs` — tests pass
    - `grep -c '"prompts"' package.json` — exactly 1 (single-dep preserved)
    - `node bin/cli.mjs doctor` exit 0 (regression — wizard edit didn't break doctor)
  </acceptance_criteria>
  <done>
    installNotebookLM body replaced. showInstructions placeholder updated. ADR-0001 grep sweep clean. Tests green. Single-dep preserved.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verification — real notebooklm login + first sync wizard run</name>
  <files>bin/install.mjs</files>
  <what-built>
    Plan 05-03 replaces bin/install.mjs::installNotebookLM with an interactive D-08..D-11 wizard that detects the notebooklm binary, optionally installs via pipx/pip, runs `notebooklm login` via `spawnSync({stdio: 'inherit'})`, verifies with auth check, and offers a first sync. The interactive subprocess handoff CANNOT be fully automated in CI — human verification is required ONCE to confirm the end-to-end flow works on the dev machine.
  </what-built>
  <how-to-verify>
    Run the wizard against a disposable sandbox vault on the dev machine (which already has notebooklm-py v0.3.4 installed and authenticated per research §Environment Availability). Steps:

    1. **Back up first:** `cp ~/vault/.notebooklm-sync.json ~/vault/.notebooklm-sync.json.backup-before-phase5-check` (if the manifest exists). This lets you restore if something goes sideways.

    2. **Run the wizard in a sandbox vault:**
       - Create a disposable test vault: `mkdir -p /tmp/phase5-vault/projects/test-proj/sessions /tmp/phase5-vault/meta`
       - Run `VAULT_PATH=/tmp/phase5-vault node bin/install.mjs`
       - Navigate through wizard steps until you reach "Setting up NotebookLM".

    3. **Verify the detection path:** Wizard should say `notebooklm binary already in PATH — skipping install` (because dev machine has it). If it tries to install, something is wrong.

    4. **Verify login invocation:** Wizard should launch `notebooklm login`. Since dev machine is already authenticated, the subprocess should exit 0 quickly (or you press ENTER when prompted). Verify no stack trace leaks.

    5. **Verify auth check:** Wizard should print `ok('notebooklm authenticated')`.

    6. **Verify first sync prompt:** Wizard should ask "Run first NotebookLM sync now?". Answer **NO** (to avoid polluting the real notebook with test vault data). Wizard should exit cleanly.

    7. **Grep validation after:**
       - `grep "NOTEBOOKLM_API_KEY" bin/install.mjs` → 0 matches
       - `grep -n "spawnSync.*notebooklm.*login" bin/install.mjs` → 1 match with `stdio: 'inherit'` on the same line

    8. **Clean up:** `rm -rf /tmp/phase5-vault`

    **Optional end-to-end smoke (risky — not required for approval):**
       - Back up `~/.notebooklm/storage_state.json` first
       - Run `claude-dev-stack install` against the REAL vault with Yes to first sync
       - Verify ~/vault/.notebooklm-sync.json was created/updated
       - If anything looks off, restore from backup

    Report the findings in the approval response.
  </how-to-verify>
  <action>
    Human operator executes the verification steps in <how-to-verify> against a disposable /tmp/phase5-vault sandbox and reports observed behavior. No code changes in this task — it's a verification gate on the output of Task 3.
  </action>
  <verify>
    <automated>MANUAL — human verification checkpoint; no automated command. Acceptance is the human operator typing "approved" after completing the steps in <how-to-verify>.</automated>
  </verify>
  <done>
    Human operator has run the wizard end-to-end on the dev machine against a disposable sandbox vault, confirmed detection path + login invocation + auth check + first-sync prompt all behave as specified, reported findings, and typed "approved". If issues are found, Plan 05-03 Task 3 is revisited.
  </done>
  <resume-signal>Type "approved" if wizard works end-to-end on the dev machine, or describe issues to revise the wizard flow.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User prompts → install commands | Wizard reads user confirmation and runs `pipx install` or `pip install --user` as user's shell command |
| Wizard → notebooklm login subprocess | stdio inherited; browser opens; ENTER captured |
| Wizard → notebooklm auth check subprocess | stdout/stderr captured to determine status |
| Wizard → syncVault inline | first sync runs in wizard process; blocks until complete |
| Doctor → manifest.generated_at | Read-only manifest inspection; no writes |
| Doctor → notebooklm --version subprocess | runCmd captures version string; no user input in command |
| lib/notebooklm-manifest.mjs → .gitignore file | Append-only write for migration path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | T (Tampering) | Wizard pipx/pip install subprocess | accept | `installCmd` is hardcoded format string with no user input interpolation — the only variables are `pipCmd` (from prereq check) and package name (literal `notebooklm-py[browser]`). No injection surface. User sees the exact command before confirming (consent gate). |
| T-05-05 | T (Trusted install) | `pipx install notebooklm-py[browser]` network call | accept | Package is explicit user opt-in via `components.notebooklm` flag. Package name is literal. PyPI supply-chain risk is inherent to any pip install; user's responsibility. Research §Environment Availability documents this. |
| T-05-06 | I (SIGINT handling) | spawnSync login subprocess | mitigate | Wizard checks `loginResult.signal === 'SIGINT'` — prints info message + returns false (partial install) instead of crashing or continuing as if login succeeded. |
| T-05-13 | T (TOCTOU) | Post-install hasCommand re-check | mitigate | Wizard re-runs `hasCommand('notebooklm')` AFTER install to catch silent install failures (e.g., PATH not updated until shell restart). Returns false with explicit instruction to restart shell. |
| T-05-14 | I (Credentials leak) | Wizard secret handling | mitigate | Zero credential surface. `grep "NOTEBOOKLM_API_KEY\\|storage_state" bin/install.mjs` returns 0. Login delegated entirely to notebooklm-py (ADR-0001). Verified by acceptance criterion. |
| T-05-15 | D (Wizard freeze on login) | `spawnSync(..., { stdio: 'inherit' })` | accept | Expected behavior — wizard blocks while user completes browser OAuth. No reasonable timeout. User can Ctrl+C (handled via SIGINT branch T-05-06). Research §R4 documents this. |
| T-05-16 | I (Info disclosure in syncVault first-sync error) | Wizard first-sync catch | mitigate | `warn(`First sync failed: ${err.message}`)` could leak subprocess stderr content. Mitigation: truncate err.message to 200 chars before printing (or accept — wizard is interactive, user already consented to running sync). Accept for now; revisit if user reports leakage. |
| T-05-17 | I (Doctor reveals vault path) | Doctor NotebookLM section | accept | Doctor already prints vault path in prerequisites section — no new disclosure. Manifest file count is not sensitive. |
| T-05-18 | T (Gitignore migration race) | ensureManifestGitignored migration path | accept | Two syncs racing on .gitignore append are unlikely in practice (manifest write is atomic, gitignore is one-shot per sync). POSIX append semantics prevent corruption. Worst case: duplicate log entry line — harmless, idempotency check catches on next run. |
</threat_model>

<verification>
- All 4 tasks complete (Task 4 is human-verify checkpoint — wizard ran interactively on dev machine at least once without errors)
- Phase 3 tests (tests/notebooklm-manifest.test.mjs) still pass — no regression (only T3-07 updated + new migration test added)
- `npm test` full suite green after all three plans — baseline 183 + Plan 05-01 (~15) + Plan 05-02 (~10-14) + Plan 05-03 (~10-16) tests = ~218-228 total
- package.json dependencies block remains `{"prompts": "^2.4.2"}` (grep assertion)
- `grep -r NOTEBOOKLM_API_KEY bin/ lib/ hooks/` returns 0 matches (milestone-wide ADR-0001 sweep)
- `git diff lib/notebooklm.mjs lib/notebooklm-sync.mjs` empty (Phase 2/4 sacred — only Phase 3 manifest gets the gitignore extension)
</verification>

<success_criteria>
- NBLM-26: Install wizard detects binary, offers install via pipx/pip, runs `notebooklm login` interactively, verifies with `auth check`, offers first sync — end-to-end flow works per human-verify checkpoint
- NBLM-27: Doctor reports 3 NotebookLM lines (binary / auth / last-sync) with ADR-0012 severity discipline (info for missing optional binary)
- Phase 3 gitignore extension ships (4 entries) with migration path for existing 3-entry vaults (research finding #5 fix verified by new test)
- No Phase 2/3/4 shipped library code modified beyond the surgical `ensureManifestGitignored` edit
- Single-dep constraint preserved
- ADR-0001 zero-credential-handling preserved
</success_criteria>

<output>
After completion, create `.planning/phases/05-cli-integration-trigger-wizard/05-03-SUMMARY.md` documenting:
- Files created/modified + LoC counts
- Test counts + delta
- Phase 3 migration test result (3→4 upgrade verified)
- Human-verify checkpoint outcome (what was tested, what was observed)
- Any deviations from plan
- Phase 5 milestone completion status (all 5 phases of v0.8 shipped?)
</output>
