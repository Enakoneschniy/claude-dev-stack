---
phase: 260415-ps8-backfill-v0-12-bookkeeping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/19-project-level-hooks-wizard-bug-fixes/19-VERIFICATION.md
  - .planning/phases/22-post-reset-handoff/22-VERIFICATION.md
  - .planning/phases/23-smart-re-install-pre-fill/23-VERIFICATION.md
  - .planning/phases/24-wizard-ux-polish/24-VERIFICATION.md
  - .planning/phases/27-gsd-workflow-customization-via-patches/27-VERIFICATION.md
  - .planning/phases/30-claude-md-idempotent-merge/30-VERIFICATION.md
autonomous: true
requirements:
  - BUG-01
  - BUG-02
  - BUG-03
  - BUG-04
  - BUG-05
  - BUG-06
  - BUG-07
  - LIMIT-01
  - LIMIT-02
  - LIMIT-03
  - LIMIT-04
  - LIMIT-05
  - DX-07
  - DX-08
  - DX-09
  - DX-10
  - DX-11
  - DX-12
  - DX-13
  - UX-01
  - UX-02
  - UX-03
  - UX-04
  - UX-05
  - UX-06
  - UX-07
  - GSD-01
  - WF-01

must_haves:
  truths:
    - "ROADMAP.md Phases list shows [x] for phases 21, 22, 25, 29 (flipped from [ ])"
    - "Each flipped ROADMAP phase entry ends with ' (completed 2026-04-15)' appended after the existing req-code suffix"
    - "REQUIREMENTS.md Traceability table shows status=complete for BUG-01..07, LIMIT-01..05, GSD-01, WF-01, DX-07..13, UX-01..07 (≥25 rows)"
    - "REQUIREMENTS.md Traceability table still shows status=pending for ADR-02 and SSR-01 (UAT deferred)"
    - "REQUIREMENTS.md Traceability table still shows status=complete for SKL-01..04 (unchanged, no duplication)"
    - "6 new VERIFICATION.md files exist at the specified phase paths (19, 22, 23, 24, 27, 30)"
    - "Each new VERIFICATION.md has YAML frontmatter with status=passed, verified=2026-04-15, verification_type=shipped-release-backfill"
    - "Each new VERIFICATION.md body lists Requirements Covered section with phase's REQ-IDs"
    - "ROADMAP.md entries for phases 26, 28, 31 are UNTOUCHED"
    - "No files outside .planning/ are modified (no code, no tests, no production artifacts)"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Updated phase checkboxes (21, 22, 25, 29 flipped to [x] with date note)"
      contains: "Phase 29: GSD Workflow Enforcer Hook"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Traceability table with ≥25 complete rows"
      contains: "Traceability"
    - path: ".planning/phases/19-project-level-hooks-wizard-bug-fixes/19-VERIFICATION.md"
      provides: "Backfill verification stub for Phase 19 (BUG-01..06)"
      contains: "shipped-release-backfill"
    - path: ".planning/phases/22-post-reset-handoff/22-VERIFICATION.md"
      provides: "Backfill verification stub for Phase 22 (LIMIT-04)"
      contains: "shipped-release-backfill"
    - path: ".planning/phases/23-smart-re-install-pre-fill/23-VERIFICATION.md"
      provides: "Backfill verification stub for Phase 23 (DX-07..13)"
      contains: "shipped-release-backfill"
    - path: ".planning/phases/24-wizard-ux-polish/24-VERIFICATION.md"
      provides: "Backfill verification stub for Phase 24 (UX-01..07)"
      contains: "shipped-release-backfill"
    - path: ".planning/phases/27-gsd-workflow-customization-via-patches/27-VERIFICATION.md"
      provides: "Backfill verification stub for Phase 27 (GSD-01)"
      contains: "shipped-release-backfill"
    - path: ".planning/phases/30-claude-md-idempotent-merge/30-VERIFICATION.md"
      provides: "Backfill verification stub for Phase 30 (BUG-07)"
      contains: "shipped-release-backfill"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: ".planning/REQUIREMENTS.md"
      via: "Requirement IDs (BUG/LIMIT/UX/DX/GSD/WF codes)"
      pattern: "Phase \\d+"
    - from: ".planning/REQUIREMENTS.md Traceability"
      to: ".planning/phases/*/VERIFICATION.md"
      via: "status=complete backed by evidence stub"
      pattern: "\\| complete \\|"
---

<objective>
Close the GSD bookkeeping gap for v0.12 non-UAT phases so the milestone is audit-clean and ready for `/gsd-complete-milestone`.

v0.12 was tagged (v0.12.0 on npm, PR #37 merged at b12d89e, 912 tests green), but GSD artifacts lag reality:
- ROADMAP.md has 4 shipped phases still marked `[ ]` (21, 22, 25, 29)
- REQUIREMENTS.md traceability table has ~25 requirements marked `pending` despite being shipped
- 6 non-UAT phases lack VERIFICATION.md stubs

Purpose: synchronize GSD state with release reality without touching code, tests, or production files. Phases 26, 28, 31 are UAT-pending and explicitly out of scope.

Output: Updated ROADMAP.md, updated REQUIREMENTS.md, 6 new VERIFICATION.md stubs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-gsd-workflow-enforcer-hook/29-VERIFICATION.md

<!-- 29-VERIFICATION.md is the style reference for the stubs, but the stubs are SHORTER (release-backfill, not full inline verification). Use the frontmatter shape + section headers only. -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ROADMAP.md phase checkboxes (21, 22, 25, 29)</name>
  <files>.planning/ROADMAP.md</files>
  <action>
Use the Edit tool (NOT sed/heredoc) to flip exactly 4 checkbox entries in the `## Phases` list section of `.planning/ROADMAP.md`. For each of the 4 target phases, replace the single matching line:

**Phase 21** — find:
`- [ ] **Phase 21: Continuation Prompt & loop.md** — 4-option continuation prompt when budget low + loop.md template for scheduled tasks (LIMIT-02, LIMIT-03)`
replace with:
`- [x] **Phase 21: Continuation Prompt & loop.md** — 4-option continuation prompt when budget low + loop.md template for scheduled tasks (LIMIT-02, LIMIT-03) (completed 2026-04-15)`

**Phase 22** — find:
`- [ ] **Phase 22: Post-Reset Handoff** — Load STATE.md on scheduled task fire and continue from stopped_at (LIMIT-04)`
replace with:
`- [x] **Phase 22: Post-Reset Handoff** — Load STATE.md on scheduled task fire and continue from stopped_at (LIMIT-04) (completed 2026-04-15)`

**Phase 25** — find:
`- [ ] **Phase 25: Budget-Aware Execution Gate** — Pre-check plan usage before GSD operations, statusline integration, schedule-for-later via CronCreate (LIMIT-05)`
replace with:
`- [x] **Phase 25: Budget-Aware Execution Gate** — Pre-check plan usage before GSD operations, statusline integration, schedule-for-later via CronCreate (LIMIT-05) (completed 2026-04-15)`

**Phase 29** — find:
`- [ ] **Phase 29: GSD Workflow Enforcer Hook** — PostToolUse hook enforces discuss+plan+manager batching; prevents per-phase execute suggestion when multiple phases pending (WF-01)`
replace with:
`- [x] **Phase 29: GSD Workflow Enforcer Hook** — PostToolUse hook enforces discuss+plan+manager batching; prevents per-phase execute suggestion when multiple phases pending (WF-01) (completed 2026-04-15)`

Constraints:
- DO NOT touch lines for Phase 26, Phase 28, or Phase 31 (UAT deferred — must stay `[ ]`).
- DO NOT touch Phase 19, 20, 23, 24, 27, 30 (already `[x]`). Do not re-append date notes if already present.
- DO NOT modify the `## Phase Details` section, `## Coverage Table`, `## Dependency Graph`, or `## Progress` section in this task — those are either informational or already current.
- Use Edit with exact full-line old_string to guarantee a unique match.
  </action>
  <verify>
    <automated>grep -c "^- \[x\] \*\*Phase \(21\|22\|25\|29\):" .planning/ROADMAP.md | grep -q "^4$" && grep -c "(completed 2026-04-15)" .planning/ROADMAP.md | awk '$1 >= 4 { exit 0 } { exit 1 }' && ! grep -E "^- \[x\].*Phase (26|28|31):" .planning/ROADMAP.md</automated>
  </verify>
  <done>
- 4 phase entries (21, 22, 25, 29) show `[x]` in the `## Phases` list.
- Each of those 4 entries ends with ` (completed 2026-04-15)` after the existing req-code suffix.
- Phases 26, 28, 31 remain `[ ]` in the Phases list.
- No other sections of ROADMAP.md modified.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md Traceability table (flip 27 rows to complete)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
The Traceability table is at the bottom of `.planning/REQUIREMENTS.md`. The current state has BUG-01..BUG-06, LIMIT-01..LIMIT-04, DX-07..DX-13, UX-01..UX-07, ADR-02, GSD-01, SSR-01 all marked `pending`, plus SKL-01..SKL-04 already `complete`.

There is NO existing row for BUG-07, LIMIT-05, or WF-01 in the table as currently shown in the required_reading — you must ADD those three rows before SKL-01 if missing (insert in logical position: BUG-07 after BUG-06, LIMIT-05 after LIMIT-04, WF-01 after GSD-01). Check the current file state first with Read before editing.

Use the Edit tool per-row (one Edit call per changed line) to flip status from `pending` to `complete` for these 24 existing rows:

```
| BUG-01 | 19 | — | pending |  →  | BUG-01 | 19 | — | complete |
| BUG-02 | 19 | — | pending |  →  | BUG-02 | 19 | — | complete |
| BUG-03 | 19 | — | pending |  →  | BUG-03 | 19 | — | complete |
| BUG-04 | 19 | — | pending |  →  | BUG-04 | 19 | — | complete |
| BUG-05 | 19 | — | pending |  →  | BUG-05 | 19 | — | complete |
| BUG-06 | 19 | — | pending |  →  | BUG-06 | 19 | — | complete |
| LIMIT-01 | 20 | — | pending |  →  | LIMIT-01 | 20 | — | complete |
| LIMIT-02 | 21 | — | pending |  →  | LIMIT-02 | 21 | — | complete |
| LIMIT-03 | 21 | — | pending |  →  | LIMIT-03 | 21 | — | complete |
| LIMIT-04 | 22 | — | pending |  →  | LIMIT-04 | 22 | — | complete |
| DX-07 | 23 | — | pending |  →  | DX-07 | 23 | — | complete |
| DX-08 | 23 | — | pending |  →  | DX-08 | 23 | — | complete |
| DX-09 | 23 | — | pending |  →  | DX-09 | 23 | — | complete |
| DX-10 | 23 | — | pending |  →  | DX-10 | 23 | — | complete |
| DX-11 | 23 | — | pending |  →  | DX-11 | 23 | — | complete |
| DX-12 | 23 | — | pending |  →  | DX-12 | 23 | — | complete |
| DX-13 | 23 | — | pending |  →  | DX-13 | 23 | — | complete |
| UX-01 | 24 | — | pending |  →  | UX-01 | 24 | — | complete |
| UX-02 | 24 | — | pending |  →  | UX-02 | 24 | — | complete |
| UX-03 | 24 | — | pending |  →  | UX-03 | 24 | — | complete |
| UX-04 | 24 | — | pending |  →  | UX-04 | 24 | — | complete |
| UX-05 | 24 | — | pending |  →  | UX-05 | 24 | — | complete |
| UX-06 | 24 | — | pending |  →  | UX-06 | 24 | — | complete |
| UX-07 | 24 | — | pending |  →  | UX-07 | 24 | — | complete |
| GSD-01 | 27 | 27-01..04 | pending |  →  | GSD-01 | 27 | 27-01..04 | complete |
```

Then insert three NEW rows (only if not already present — Read the file first to check):

- After `| BUG-06 | 19 | — | complete |`, insert: `| BUG-07 | 30 | — | complete |`
- After `| LIMIT-04 | 22 | — | complete |`, insert: `| LIMIT-05 | 25 | — | complete |`
- After `| GSD-01 | 27 | 27-01..04 | complete |`, insert: `| WF-01 | 29 | 29-01, 29-02 | complete |`

Rows that MUST remain `pending` (do not touch):
- `| ADR-02 | 26 | — | pending |` (UAT deferred)
- `| SSR-01 | 28 | 28-01..03 | pending |` (UAT deferred)

Rows already `complete` (verify present, do not duplicate or re-write):
- SKL-01, SKL-02, SKL-03, SKL-04

Note on the `## v1 Requirements` checklist section at the top of the file (the `- [ ] **BUG-01**: ...` list): this task does NOT modify that section — it stays `[ ]` as a requirements backlog. Only the Traceability table is updated here. If the executor has context budget to also flip those checkboxes, it's acceptable but not required (focus on the table first).
  </action>
  <verify>
    <automated>awk '/^## Traceability/,/^## /' .planning/REQUIREMENTS.md | grep -c "| complete |" | awk '$1 >= 25 { exit 0 } { exit 1 }' && awk '/^## Traceability/,0' .planning/REQUIREMENTS.md | grep -q "| ADR-02 | 26 | — | pending |" && awk '/^## Traceability/,0' .planning/REQUIREMENTS.md | grep -q "| SSR-01 | 28 | 28-01..03 | pending |" && awk '/^## Traceability/,0' .planning/REQUIREMENTS.md | grep -q "| BUG-07 | 30 | — | complete |" && awk '/^## Traceability/,0' .planning/REQUIREMENTS.md | grep -q "| LIMIT-05 | 25 | — | complete |" && awk '/^## Traceability/,0' .planning/REQUIREMENTS.md | grep -q "| WF-01 | 29 | 29-01, 29-02 | complete |"</automated>
  </verify>
  <done>
- Traceability table contains ≥25 rows with status `complete`.
- ADR-02 row still `pending`, SSR-01 row still `pending`.
- New rows BUG-07 (Phase 30), LIMIT-05 (Phase 25), WF-01 (Phase 29) present and `complete`.
- SKL-01..04 rows still `complete`, not duplicated.
- No other files modified.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create 6 VERIFICATION.md backfill stubs + commit everything</name>
  <files>
.planning/phases/19-project-level-hooks-wizard-bug-fixes/19-VERIFICATION.md,
.planning/phases/22-post-reset-handoff/22-VERIFICATION.md,
.planning/phases/23-smart-re-install-pre-fill/23-VERIFICATION.md,
.planning/phases/24-wizard-ux-polish/24-VERIFICATION.md,
.planning/phases/27-gsd-workflow-customization-via-patches/27-VERIFICATION.md,
.planning/phases/30-claude-md-idempotent-merge/30-VERIFICATION.md
  </files>
  <action>
Create 6 VERIFICATION.md files using the Write tool. Use the template below, substituting `{N}`, `{SLUG}`, `{REQ_IDS}`, and `{BRIEF}` per phase.

**Template:**

```markdown
---
status: passed
phase: {N}-{SLUG}
verified: 2026-04-15
verification_type: shipped-release-backfill
evidence: "PR #37 merged (b12d89e), v0.12.0 on npm, 912 tests green on Node 18/20/22"
requirements_verified:
{REQ_IDS_YAML_LIST}
---

# Phase {N} — Verification Report (Backfill)

## Status: PASSED

Phase shipped in v0.12.0 release. Code complete and merged via PR #37 (commit `b12d89e`).
Full test suite green (912 tests on Node 18/20/22). Full UAT evidence in PR #37 body
(see GitHub). Phase SUMMARY.md has implementation details.

This VERIFICATION.md is a **release-backfill stub** created 2026-04-15 to close the GSD
bookkeeping gap identified during v0.12 milestone audit. It is NOT a full inline
verification report — see `{N}-*-SUMMARY.md` for per-plan completion evidence and the
PR #37 description for end-to-end UAT evidence.

## Requirements Covered

{REQ_IDS_MD_LIST}

All listed requirements are marked `complete` in `.planning/REQUIREMENTS.md` Traceability
table as of 2026-04-15.

## Out of Scope for This Stub

- Full per-SC evidence rows — see phase SUMMARY.md files and PR #37 description.
- Manual smoke test transcripts — captured inline in the PR review.
- Test delta audit — consolidated at the milestone level (558 → 912).
```

**Per-phase substitutions:**

1. `.planning/phases/19-project-level-hooks-wizard-bug-fixes/19-VERIFICATION.md`
   - `{N}` = 19, `{SLUG}` = project-level-hooks-wizard-bug-fixes
   - `{REQ_IDS_YAML_LIST}`:
     ```
       - BUG-01
       - BUG-02
       - BUG-03
       - BUG-04
       - BUG-05
       - BUG-06
     ```
   - `{REQ_IDS_MD_LIST}`:
     ```
     - **BUG-01**: Wizard writes session hooks to project-level `.claude/settings.json`.
     - **BUG-02**: Wizard writes `allowedTools` to project-level `.claude/settings.json`.
     - **BUG-03**: Re-install wizard pre-selects projects already in `project-map.json`.
     - **BUG-04**: Re-install wizard pre-selects installed components with "(installed)" label.
     - **BUG-05**: `installGitConventions()` skips or prompts when `git-scopes.json` already exists.
     - **BUG-06**: GSD transition.md TeamCreate patch auto-reapplied after `/gsd-update`.
     ```

2. `.planning/phases/22-post-reset-handoff/22-VERIFICATION.md`
   - `{N}` = 22, `{SLUG}` = post-reset-handoff
   - `{REQ_IDS_YAML_LIST}`:
     ```
       - LIMIT-04
     ```
   - `{REQ_IDS_MD_LIST}`:
     ```
     - **LIMIT-04**: Post-reset handoff reads STATE.md `stopped_at` and resumes execution from last position; works with fresh git clone.
     ```

3. `.planning/phases/23-smart-re-install-pre-fill/23-VERIFICATION.md`
   - `{N}` = 23, `{SLUG}` = smart-re-install-pre-fill
   - `{REQ_IDS_YAML_LIST}`:
     ```
       - DX-07
       - DX-08
       - DX-09
       - DX-10
       - DX-11
       - DX-12
       - DX-13
     ```
   - `{REQ_IDS_MD_LIST}`:
     ```
     - **DX-07**: Re-install wizard pre-fills communication/code language from vault profile.
     - **DX-08**: Re-install wizard pre-fills projects directory from existing `project-map.json`.
     - **DX-09**: Already-registered projects skip the "project name" prompt.
     - **DX-10**: Use case selection pre-filled from previous install.
     - **DX-11**: GSD install checks version; skips re-install if already latest.
     - **DX-12**: NotebookLM login skipped if `storage_state.json` exists.
     - **DX-13**: Bulk prompts ("Install for all N projects? Y/n") replace per-project confirms.
     ```

4. `.planning/phases/24-wizard-ux-polish/24-VERIFICATION.md`
   - `{N}` = 24, `{SLUG}` = wizard-ux-polish
   - `{REQ_IDS_YAML_LIST}`:
     ```
       - UX-01
       - UX-02
       - UX-03
       - UX-04
       - UX-05
       - UX-06
       - UX-07
     ```
   - `{REQ_IDS_MD_LIST}`:
     ```
     - **UX-01**: Git sync step detects existing configured remote.
     - **UX-02**: loop.md installation uses bulk prompt.
     - **UX-03**: git-conventions installation uses bulk prompt.
     - **UX-04**: Git sync step checks for existing remote before offering init/push.
     - **UX-05**: Wizard step counter is accurate (no "Step 15 of 14").
     - **UX-06**: Detect banner project count matches vault step project count.
     - **UX-07**: All wizard confirmation prompts use consistent select-style.
     ```

5. `.planning/phases/27-gsd-workflow-customization-via-patches/27-VERIFICATION.md`
   - `{N}` = 27, `{SLUG}` = gsd-workflow-customization-via-patches
   - `{REQ_IDS_YAML_LIST}`:
     ```
       - GSD-01
     ```
   - `{REQ_IDS_MD_LIST}`:
     ```
     - **GSD-01** (formalization cut): Shipped `patches/*.md` + wizard copy to `~/.claude/gsd-local-patches/` + `gsd-auto-reapply-patches.sh` SessionStart hook with SHA-diff re-apply, idempotent behavior, and BUG-06 D-07 precedence. Extended criteria (per-project `.planning/gsd-overrides/`, `gsd customize` CLI, diff patches, workflow.auto_push/auto_pr/merge_strategy) deferred to backlog per 27-CONTEXT D-1.
     ```

6. `.planning/phases/30-claude-md-idempotent-merge/30-VERIFICATION.md`
   - `{N}` = 30, `{SLUG}` = claude-md-idempotent-merge
   - `{REQ_IDS_YAML_LIST}`:
     ```
       - BUG-07
     ```
   - `{REQ_IDS_MD_LIST}`:
     ```
     - **BUG-07**: `lib/install/claude-md.mjs` uses idempotent `updateProjectClaudeMd()` with `<!-- @claude-dev-stack:start/end -->` markers; preserves user content on re-install (created/updated/appended status line).
     ```

**Phases explicitly skipped in this task:**

- Phase 21 (Continuation Prompt & loop.md) — no SUMMARY.md exists; marked as bookkeeping gap in the commit message.
- Phase 25 (Budget-Aware Execution Gate) — no SUMMARY.md exists; marked as bookkeeping gap in the commit message.

Do NOT create VERIFICATION.md files for 21 or 25.

**Commit all changes:**

After writing all 6 stubs, stage and commit all edits from Tasks 1, 2, and 3 in a single commit:

```bash
git add .planning/ROADMAP.md \
        .planning/REQUIREMENTS.md \
        .planning/phases/19-project-level-hooks-wizard-bug-fixes/19-VERIFICATION.md \
        .planning/phases/22-post-reset-handoff/22-VERIFICATION.md \
        .planning/phases/23-smart-re-install-pre-fill/23-VERIFICATION.md \
        .planning/phases/24-wizard-ux-polish/24-VERIFICATION.md \
        .planning/phases/27-gsd-workflow-customization-via-patches/27-VERIFICATION.md \
        .planning/phases/30-claude-md-idempotent-merge/30-VERIFICATION.md
```

Commit message (use HEREDOC, NO Co-Authored-By per project memory):

```
docs(planning): backfill v0.12 GSD bookkeeping for non-UAT phases

v0.12.0 shipped (PR #37, b12d89e, 912 tests green), but GSD artifacts lagged:
- ROADMAP.md: flip phases 21, 22, 25, 29 to [x] with 2026-04-15 completion date
- REQUIREMENTS.md: flip 24 rows pending→complete + add 3 missing rows (BUG-07,
  LIMIT-05, WF-01) so traceability reflects shipped state (≥25 complete)
- VERIFICATION.md stubs created for phases 19, 22, 23, 24, 27, 30
  (shipped-release-backfill type, PR #37 as evidence of record)

Out of scope (UAT deferred, intentionally left pending):
- Phase 26 (ADR-02 auto-capture) — requires live Claude Code session
- Phase 28 (SSR-01 silent session start) — requires live Claude Code session
- Phase 31 (SKL-01..04 already complete — body UAT pending)

Known bookkeeping gaps to revisit (no SUMMARY.md on disk — inline/no-plan-files
phases; need retroactive SUMMARY.md + VERIFICATION.md or explicit "shipped inline"
documentation before /gsd-complete-milestone can close v0.12):
- Phase 21 (Continuation Prompt & loop.md) — LIMIT-02, LIMIT-03
- Phase 25 (Budget-Aware Execution Gate) — LIMIT-05

No code, no tests, no production files modified.
```
  </action>
  <verify>
    <automated>test "$(find .planning/phases -name "*-VERIFICATION.md" | wc -l | tr -d ' ')" = "7" && test -f .planning/phases/19-project-level-hooks-wizard-bug-fixes/19-VERIFICATION.md && test -f .planning/phases/22-post-reset-handoff/22-VERIFICATION.md && test -f .planning/phases/23-smart-re-install-pre-fill/23-VERIFICATION.md && test -f .planning/phases/24-wizard-ux-polish/24-VERIFICATION.md && test -f .planning/phases/27-gsd-workflow-customization-via-patches/27-VERIFICATION.md && test -f .planning/phases/30-claude-md-idempotent-merge/30-VERIFICATION.md && ! test -f .planning/phases/21-continuation-prompt-loop-md/21-VERIFICATION.md && ! test -f .planning/phases/25-budget-aware-execution-gate/25-VERIFICATION.md && grep -l "shipped-release-backfill" .planning/phases/*/[0-9]*-VERIFICATION.md | wc -l | awk '$1 == 6 { exit 0 } { exit 1 }' && git log -1 --format=%s | grep -q "backfill v0.12 GSD bookkeeping" && ! git log -1 --format=%B | grep -qi "co-authored-by"</automated>
  </verify>
  <done>
- Exactly 7 VERIFICATION.md files exist under `.planning/phases/` (1 pre-existing + 6 new).
- All 6 new stubs have `verification_type: shipped-release-backfill` in frontmatter.
- Each stub's `requirements_verified` YAML list matches the phase's REQ-IDs.
- No VERIFICATION.md created for phases 21 or 25.
- Single git commit contains all Task 1+2+3 edits.
- Commit message mentions phase 21 and 25 as bookkeeping gaps to revisit.
- No Co-Authored-By line in the commit message.
- Working tree is clean after commit.
  </done>
</task>

</tasks>

<verification>
## Plan-Level Verification

Run all of these from the repo root after Task 3 completes:

1. **ROADMAP checkboxes:**
   ```
   grep -cE "^- \[x\] \*\*Phase (21|22|25|29):" .planning/ROADMAP.md
   ```
   Expected: `4`

2. **ROADMAP date notes on flipped phases:**
   ```
   grep -cE "\*\*Phase (21|22|25|29):.*\(completed 2026-04-15\)" .planning/ROADMAP.md
   ```
   Expected: `4`

3. **ROADMAP untouched phases:**
   ```
   grep -E "^- \[ \] \*\*Phase (26|28|31):" .planning/ROADMAP.md | wc -l
   ```
   Expected: `3`

4. **REQUIREMENTS traceability complete count:**
   ```
   awk '/^## Traceability/,0' .planning/REQUIREMENTS.md | grep -c "| complete |"
   ```
   Expected: `≥29` (24 flipped + 3 new + 4 SKL-01..04 pre-existing = 31; allow slack for variation)

5. **REQUIREMENTS UAT-deferred rows still pending:**
   ```
   awk '/^## Traceability/,0' .planning/REQUIREMENTS.md | grep -cE "^\| (ADR-02|SSR-01) \|.*\| pending \|"
   ```
   Expected: `2`

6. **VERIFICATION.md file count:**
   ```
   find .planning/phases -name "*-VERIFICATION.md" | wc -l
   ```
   Expected: `7`

7. **No VERIFICATION.md for skipped phases:**
   ```
   find .planning/phases -path '*21-*' -name "*-VERIFICATION.md"; find .planning/phases -path '*25-*' -name "*-VERIFICATION.md"
   ```
   Expected: empty output

8. **Git state clean + single commit:**
   ```
   git status --short   # expect empty
   git log -1 --format=%s   # expect "docs(planning): backfill v0.12 GSD bookkeeping for non-UAT phases"
   git log -1 --format=%B | grep -i "co-authored-by"   # expect empty
   ```

9. **No code/test files modified:**
   ```
   git show --stat HEAD | grep -E "^ (lib|bin|hooks|tests|patches)/" | wc -l
   ```
   Expected: `0`
</verification>

<success_criteria>
- [ ] Task 1 verify passes: 4 ROADMAP phases flipped with date notes; phases 26/28/31 untouched.
- [ ] Task 2 verify passes: ≥25 Traceability rows `complete`; ADR-02 and SSR-01 still `pending`; BUG-07/LIMIT-05/WF-01 rows added and `complete`.
- [ ] Task 3 verify passes: 7 VERIFICATION.md files total; 6 new stubs have `shipped-release-backfill`; no stubs for phases 21 or 25; single clean commit without Co-Authored-By.
- [ ] Plan-level verification items 1–9 all return expected values.
- [ ] Working tree is clean. No code, test, or production file touched.
- [ ] Milestone v0.12 is now audit-clean for `/gsd-complete-milestone` (modulo the two documented bookkeeping gaps: phases 21 and 25).
</success_criteria>

<output>
After completion, no SUMMARY.md is required (this is a `.planning/quick/` task, not a phase).
The single git commit IS the audit trail. The commit message documents:
1. What was backfilled (4 checkbox flips, 24 row flips + 3 row inserts, 6 stubs).
2. What was explicitly left pending (phases 26, 28, 31).
3. What gaps remain (phases 21, 25 — missing SUMMARY.md).

The parent agent should surface phases 21 and 25 as the next quick-task candidates
once the user is ready to fully close the v0.12 milestone.
</output>
